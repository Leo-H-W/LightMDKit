# LightMDKit - 仓库概览

> 本文档是项目知识库的索引和导航，用于快速了解项目结构和查找详细文档。

## 基本信息

| 项目名称 | LightMDKit |
|---------|--------|
| 项目简介 | 免安装的本地 Markdown 工作台：以 GitHub 风格渲染与编辑本地 Markdown 文件，文件不上传，支持目录导航、在线编辑、拖放打开与 Mermaid 图表 |
| 主要语言 | JavaScript（Node.js + 浏览器原生 JS，无 TypeScript） |
| 核心框架 | Express 4.18（后端）、marked 12（Markdown 解析）、CodeMirror 5（编辑器）、Mermaid 11（图表）、github-markdown-css 5（排版） |
| 分析版本 | 工作区代码，其内容已全部提交在 `main` 上，最新提交 `bf3f988` |
| 生成时间 | 2026-09-11 |

> ⚠️ **关于分析版本与行号口径**：本文档与 `doc/kb` 下的其余文档，描述的都是 **2026-09-11 的工作区代码**，
> 其内容已提交在 `main` 上（最新 `bf3f988`，此前经过 `f71e683` 侧边栏功能 → `dc57969` 现代模式增强 →
> 之后若干表格与新建文件的修复）。
> **核对行号请直接读工作区文件**（如 `sed -n '<行号>p' public/app.js`）；`main` 上更早的提交
> （如 `3bbca34`）不含这些能力，行号对不上。

## 模块功能介绍

### 核心模块

| 模块名称 | 功能描述 | 目录路径 | 相关技术 |
|---------|---------|---------|---------|
| 后端服务 | HTTP 服务器（默认端口 **3456**），托管前端静态资源；提供文件夹选择、加载目录、读取文件、刷新文件 4 个 API；含端口占用检测、后台守护进程拉起、自动打开浏览器 | `LightMDKit/server.js` | Express 4.18, Node.js (`child_process`/`net`/`os`) |
| 前端界面 | 单页应用：工具栏、侧边栏导航、正文渲染区、编辑器；负责文件夹句柄管理、文件渲染、TOC 生成、编辑与自动保存、拖放打开、新建文件、传统/现代双模式 | `LightMDKit/public/app.js` | 原生 JS（IIFE，约 2150 行）、File System Access API、IndexedDB |
| 渲染管线 | **前后端共用**的 Markdown 渲染模块：缩进保留、表格多行单元格合并、`~` 删除线分词补丁；UMD 双端导出 | `LightMDKit/public/md-render.js` | UMD 包装、marked 12 自定义 tokenizer（200 行） |
| 页面与样式 | 页面骨架（工具栏 + 侧边栏 + 内容区 + 拖放遮罩）与全部样式，含现代模式（`.live-preview` 作用域）的即时渲染规则 | `LightMDKit/public/index.html`、`LightMDKit/public/style.css` | HTML5, CSS3, CDN 引入（jsDelivr） |
| 打包脚本 | `pkg` 打包后改写 exe 的 PE 头 Subsystem 为 `WINDOWS_GUI`，消除双击时的命令行窗口 | `LightMDKit/scripts/set-windows-gui.js` | Node.js `fs`（直接操作 PE 二进制） |
| 启停脚本 | 一键启动（检查 Node 环境、缺依赖自动 `npm install`）与停止后台服务 | `LightMDKit/start.bat`、`start.sh`、`stop.bat`、`stop.sh` | Windows Batch / POSIX Shell |

### 公共模块

| 模块名称 | 功能描述 | 目录路径 |
|---------|---------|---------|
| 渲染管线 | 唯一被前后端同时消费的模块 —— `server.js` 通过 `require('./public/md-render.js')` 引用，浏览器通过 `<script src="md-render.js">` 引用。**修改 Markdown 语义时必须同时考虑两侧** | `LightMDKit/public/md-render.js` |
| 文件类型判定 | `isMarkdownName(name)` 判定 `.md` / `.markdown`，被目录扫描与拖放打开共用 | `LightMDKit/public/app.js:179` |
| UI 偏好持久化 | 界面模式、侧边栏视图等偏好统一以 `lightmdkit.` 前缀存入 `localStorage`，读写均 `try/catch` 降级 | `LightMDKit/public/app.js` |

## 项目结构

```
LightMDKit/
├── server.js                      # 后端入口：Express 服务 + 4 个 API + 守护进程管理 + 打开浏览器
├── package.json                   # 项目配置：依赖、start 与 build:win 脚本、pkg 打包目标与 assets
├── package-lock.json              # 依赖锁定文件
├── LICENSE                        # 开源许可证
├── README.md                      # 项目说明：功能特性、技术栈、快速开始、打包方式
├── feature-todo.md                # 需求待办 / 功能规划清单
├── test-sample.md                 # 手工测试样本：标题层级、代码块、表格、Mermaid 流程图与时序图
├── test-wide.md                   # 边界测试样本：15 列宽表格、超长代码行（验证横向滚动）
├── start.bat / start.sh           # 一键启动：检查 Node、缺 node_modules 时自动 npm install、启动服务
├── stop.bat / stop.sh             # 停止后台服务（taskkill / kill）
├── public/                        # 前端静态资源（由 Express 托管，同时内嵌进 exe）
│   ├── index.html                 # 页面骨架：工具栏按钮、侧边栏、内容区、拖放遮罩 + CDN 引用
│   ├── app.js                     # 前端全部逻辑（IIFE，2154 行）：句柄管理、渲染、TOC、编辑、拖放、双模式
│   ├── md-render.js               # 前后端共用的 Markdown 渲染模块（UMD，200 行）
│   └── style.css                  # 全部样式（1002 行）：布局、侧边栏、正文、编辑器、现代模式 .live-preview
├── scripts/
│   └── set-windows-gui.js         # 构建后处理：改写 exe 的 PE 子系统为 WINDOWS_GUI
├── dist/                          # 构建产物（由 npm run build:win 生成，不入源码树）
│   └── lightmdkit.exe             # pkg 打出的 Windows 绿色单文件（约 40MB，内嵌 public/**/* 与 Node 运行时）
├── agent-rules/
│   └── kb-commands/prompts/       # 知识库生成提示词模板（01 仓库概览 ~ 13 基于git记录优化）
├── doc/
│   └── kb/                        # 知识库文档（见下方索引）
│       ├── 仓库概览.md            # 本文档：知识库索引与导航
│       ├── 仓库架构.md            # 模块分层架构可视化
│       ├── 仓库依赖.md            # 依赖关系 Mermaid 图
│       ├── 外部接入指南.md        # 外部系统接入方式
│       ├── 基于git记录优化.md     # 从 14 个提交中提炼的修复复盘与工程经验
│       ├── .kb-meta.json          # 知识库元数据（生成时间、Git 版本、依赖清单）
│       ├── 业务知识库/            # 业务知识文档
│       │   ├── 核心业务领域.md
│       │   ├── 业务流程索引.md
│       │   └── 业务流程详解.md
│       ├── 技术知识库/            # 技术知识文档
│       │   ├── API索引.md
│       │   ├── 数据结构.md
│       │   ├── 代码编写指南.md
│       │   └── 排障指南.md
│       └── 测试知识库/            # 测试知识文档
│           └── 单元测试.md
├── .claude/                       # Claude Code 配置与命令（settings.local.json 已 gitignore）
├── .roo/                          # Roo Code 命令、规则与技能配置
└── openspec/                      # OpenSpec 规范驱动开发配置（config.yaml）
```

> **已排除**：`node_modules/`（依赖包）、`.git/`（版本库）。`.gitignore` 仅忽略 `node_modules/` 与 `.claude/settings.local.json` —— 注意 `dist/*.exe`（约 40MB）**是入库的**。

## 知识库文档索引

> 💡 根据你的任务类型，快速定位需要查看的文档

### 架构和依赖
- **[仓库架构](./仓库架构.md)**: 模块分层架构可视化
  - **适用场景**: 理解项目整体架构、新功能模块设计、跨模块开发
- **[仓库依赖](./仓库依赖.md)**: 依赖关系 Mermaid 图
  - **适用场景**: 了解模块依赖关系、评估变更影响范围

### 业务知识
- **[核心业务领域](./业务知识库/核心业务领域.md)**: 业务能力说明
  - **适用场景**: 理解业务规则、需求分析、业务功能开发
- **[业务流程索引](./业务知识库/业务流程索引.md)**: 所有流程概述
  - **适用场景**: 快速定位业务代码位置、了解业务流程概览
- **[业务流程详解](./业务知识库/业务流程详解.md)**: 核心流程深度分析
  - **适用场景**: 深入理解核心业务流程、修改业务逻辑、参考已有流程新增业务流程

### 技术知识
- **[API索引](./技术知识库/API索引.md)**: HTTP API 和 RPC 接口列表
  - **适用场景**: API开发、接口对接、快速查看接口实现
  - **注意**: 仅包含 HTTP 和 RPC 接口，公共库导出项请参考"外部接入指南"
- **[外部接入指南](./外部接入指南.md)**: 外部系统接入方式
  - **适用场景**: 外部系统集成、SDK使用、了解认证授权
  - **公共库项目**: 包含完整的导出函数、类、接口、常量列表
  - **服务项目**: 包含HTTP/RPC调用方式、认证流程
- **[代码编写指南](./技术知识库/代码编写指南.md)**: 代码规范
  - **适用场景**: 代码编写任务、配置变更、重构优化、新人入门
- **[数据结构](./技术知识库/数据结构.md)**: 数据库和中间件数据结构
  - **适用场景**: 数据库设计、数据迁移、缓存设计、消息队列集成
- **[排障指南](./技术知识库/排障指南.md)**: 故障处理手册
  - **适用场景**: 故障排查、问题诊断、运维支持
- **[基于git记录优化](./基于git记录优化.md)**: 从真实提交历史提炼的修复复盘与工程经验
  - **适用场景**: 修改 Markdown 渲染逻辑、了解历史踩坑（表格多行单元格、相对图片、`~` 删除线误判）、沿用本仓库的提交与缓存版本号规范

### 测试知识
- **[单元测试](./测试知识库/单元测试.md)**: 测试规范
  - **适用场景**: 单元测试编写、测试用例设计、提高代码覆盖率

## 主要能力一览

| 能力 | 说明 | 关键标识 |
|------|------|---------|
| 递归目录扫描 | 侧边栏文件浏览器递归进入子目录并按目录分组；有深度与数量上限，跳过无意义目录 | `SCAN_MAX_DEPTH`(=6)、`SCAN_MAX_FILES`(=3000)、`SCAN_SKIP_DIRS`（`node_modules`/`.git`/`dist` 等） |
| 文件标识为相对路径 | 文件记录为 `name` + `path`（相对目录根，`/` 分隔），解决不同子目录同名文件冲突 | `currentFiles[].path` |
| 路径归一化匹配 | 文档内链接的 `./sub/a.md`、`sub/a.md`、`sub\a.md` 统一口径后匹配同一记录 | `normalizePathForMatch()` |
| 侧边栏视图切换 | 文件列表 ↔ 当前文档目录（TOC）一键切换，选择记忆在 localStorage | `lightmdkit.sidebarView`、`SIDEBAR_VIEW_KEY` |
| 文件列表实时过滤 | 按文件名 / 相对路径过滤，当前文件高亮 | `fileFilter`、`fileListEl` |
| 新建文件 | 工具栏「新文件」按钮（在「新tab页」之前）：弹窗输入文件名，在当前文件夹创建；含 6 类校验与写入异常提示 | `btn-new-file`、`createNewFile()`、`validateNewFileName()` |
| 刷新 | 重新扫描目录刷新文件列表 + 重读当前文档；没有打开文件时也会刷新列表 | `refreshCurrentFile()` |
| 现代模式（Typora 式） | 单栏常驻编辑：标题 / 加粗 / 斜体 / 行内代码 / 引用 / 列表圆点 / 任务框 / 代码围栏 / 表格 均即时渲染；回车列表续行 | `.live-preview`、`applySurface()` |
| 表格伪渲染 | 编辑器里把表格画成带框线的表格（CM5 不认表格，靠自研扫块 + `markText` + 按列宽注入样式表）；光标落在表头行时展开分隔行便于核对 | `refreshTableMarks()`、`#cm-table-style`、`updateSepReveal()` |
| 表格单元格按键 | Tab 新增一行、Enter 跳到下一行同一格、Ctrl+Enter 单元格内插入 `<br>` 换行 | `tableTabKey`、`tableEnterKey`、`tableCtrlEnterKey` |

## 快速链接

- README: `LightMDKit/README.md`
- 主入口: `LightMDKit/server.js`
- 前端页面: `LightMDKit/public/index.html`
- 渲染模块: `LightMDKit/public/md-render.js`
- 配置文件: `LightMDKit/package.json`
- 构建脚本: `npm run build:win`（产物 `LightMDKit/dist/lightmdkit.exe`）
- 启动脚本: `LightMDKit/start.bat`（Windows）/ `LightMDKit/start.sh`（POSIX）
- 访问地址: <http://localhost:3456>

---

## 注意事项

1. **简洁性**：本文档作为索引，不包含详细技术内容
2. **路径规范**：所有文件路径使用相对路径格式 `项目目录/文件路径`。**必须**使用任务上下文中提供的 `项目目录` 值作为路径前缀，不要使用绝对路径，也不要省略项目目录前缀
3. **路径准确**：所有链接必须指向实际存在的文件
4. **版本追踪**：记录 Git Commit ID。本项目为 Git 仓库（主干 `main`，共 **26** 个提交，HEAD = `8edbf60`），本知识库的分析对象即 `main` 的 `HEAD`。历史提交详见 [基于git记录优化.md](./基于git记录优化.md)
5. **最后生成**：本文档应在其他知识库文档生成完成后执行
6. **避免冗余原则**:
   - 本文档是知识库的"唯一入口"，详细内容通过链接引用
   - 不在本文档展开技术实现细节，只做索引导航
7. **行号口径**：本文档中引用的行号均以 **2026-09-11 的工作区文件**为准，其内容已提交在 `main` 上（HEAD = `8edbf60`），工作区没有未提交的代码改动。核对可直接读工作区文件，例如 `sed -n '<行号>p' public/app.js`；**不要**用更早的提交（如 `3bbca34`）—— 那些版本不含侧边栏、现代模式表格渲染、新建文件等能力，行号会对不上。
