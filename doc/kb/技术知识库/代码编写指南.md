# LightMDKit - 代码编写指南

> 本文档总结了本项目的代码编写经验、规范和最佳实践。
> 所有条目均从当前代码中提炼，附真实代码与出处；**凡"代码实际写法"与"理想规范"不一致的地方，本文档如实标注为偏差，不做粉饰**。

## 1. 基本信息

| 项目名称 | LightMDKit |
|---------|--------|
| 主要语言 | JavaScript（Node.js 服务端 + 浏览器原生 JS 前端） |
| 核心框架 | Express 4.18（服务端）；前端无框架、无构建工具 |
| 项目驱动模式 | 代码驱动型（配置文件仅 `package.json` 1 个，不承载业务逻辑） |
| 分析版本 | 2026-09-11 工作区代码（已提交为 `8edbf60`，分支 `main`） |
| 生成时间 | 2026-09-11 |

> ⚠️ **分析对象是 2026-09-11 的工作区代码**，其内容已全部提交（`main`，截至 `bf3f988`）：
> `public/app.js` 2154 行、`public/index.html` 115 行、`public/style.css` 1002 行。
> 侧边栏文件浏览器、现代模式的列表/任务框/表格渲染与单元格按键、新建文件等能力都在其中。
> 本节以下所有行号引用，均以该快照为准；`git checkout` 到更早的提交（如 `3bbca34`）会因为
> 缺少这些能力而大面积对不上。

**代码规模**（不含 `node_modules/`，以工作区为准）：

| 文件 | 行数 | 职责 |
|-----|------|------|
| `LightMDKit/server.js` | 313 | Express 静态服务 + 4 个 REST 路由 + 守护进程管理 |
| `LightMDKit/public/app.js` | 1465 | 全部前端交互逻辑（单个 IIFE） |
| `LightMDKit/public/md-render.js` | 172 | Markdown 预处理与渲染（UMD，前后端共用） |
| `LightMDKit/public/style.css` | 767 | 全部样式（含现代模式 `.live-preview` 作用域） |
| `LightMDKit/public/index.html` | 99 | 页面骨架 + CDN 与本地脚本引入 |
| `LightMDKit/scripts/set-windows-gui.js` | 52 | 打包后修改 PE 子系统标志 |

整个项目只有 4 个 `.js` 源文件（`server.js`、`public/app.js`、`public/md-render.js`、`scripts/set-windows-gui.js`）。**没有模块打包、没有转译、没有 lint 配置**，改完直接刷新浏览器即可生效。

---

## 2. 文件放置规则

> 📁 完整目录树请参考 [仓库概览.md](../仓库概览.md#项目结构)

### 2.1 目录职责

```
LightMDKit/
├── server.js                 # 服务端唯一入口：静态托管 + REST 路由 + 守护进程
├── package.json              # 依赖与脚本（唯一配置文件）
├── start.bat / start.sh      # 启动脚本（编码 + 依赖检查 + node server.js）
├── stop.bat / stop.sh        # 停止脚本
├── public/                   # 前端静态资源，由 express.static 托管
│   ├── index.html            # 页面结构
│   ├── app.js                # 前端全部逻辑
│   ├── md-render.js          # Markdown 渲染（前后端共用）
│   └── style.css             # 全部样式
├── scripts/
│   └── set-windows-gui.js    # 打包后处理（PE 子系统改写）
├── dist/
│   └── lightmdkit.exe        # pkg 打包产物（**已纳入 git 版本管理**）
├── doc/kb/                   # 知识库文档
├── openspec/                 # OpenSpec 规范
├── agent-rules/              # Agent 提示词规则
└── test-sample.md, test-wide.md   # 渲染自测用例
```

### 2.2 新建文件位置规则

| 要新增的东西 | 放哪里 | 是否需额外注册 |
|------------|-------|--------------|
| 服务端接口 | 直接写在 `LightMDKit/server.js`（`main()` 调用之前） | 否 —— 无路由分层，全文件集中 |
| 服务端工具函数 | `LightMDKit/server.js` 中路由区之后、`main()` 之前（如 `sleep`/`isPortInUse`/`spawnDaemon` 的排布） | 否 |
| 前端新脚本文件 | `LightMDKit/public/` 下新建 `.js` | **是** —— 必须在 `index.html` 底部用 `<script src="x.js?v=1">` 引入，且**带 `?v=` 版本号** |
| 前端新逻辑（绝大多数情况） | 追加进 `LightMDKit/public/app.js` 的 IIFE 内 | 否 |
| Markdown 渲染规则改动 | `LightMDKit/public/md-render.js` | **是** —— 前后端共用，改完服务端 `require` 与浏览器 `<script>` 同时生效；**要同步升高 `index.html` 里的 `?v=`** |
| 新样式 | `LightMDKit/public/style.css` 追加 | **是** —— 要升高 `style.css?v=` |
| 打包/构建辅助脚本 | `LightMDKit/scripts/` | 是 —— 在 `package.json` 的 `scripts` 中挂载 |

**放 `public/` 下的文件无需写路由**：`LightMDKit/server.js:14` 用 `express.static(path.join(__dirname, 'public'))` 一次性托管整个目录。

### 2.3 本项目特有的硬约定：改前端资源必须升 `?v=`

`index.html:19`、`112`、`113` 三个本地资源都带查询串版本号：

```html
<link rel="stylesheet" href="style.css?v=25">
...
<script src="md-render.js?v=5"></script>
<script src="app.js?v=33"></script>
```

`index.html` 头部另有四行禁用缓存（`index.html:6-8`）：

```html
<meta http-equiv="Cache-Control" content="no-cache, no-store, must-revalidate">
<meta http-equiv="Pragma" content="no-cache">
<meta http-equiv="Expires" content="0">
```

**规则**：`index.html` 本身不缓存，但它引用的 `style.css` / `app.js` / `md-render.js` 会被浏览器强缓存。**每次改动这三个文件之一，必须把对应的 `?v=N` 加 1**，否则用户刷新后仍是旧代码（此问题已收录在 [排障指南](./排障指南.md) 4.2 节）。

CDN 资源则相反，**固定主版本、不写完整版本号**（`index.html:10-111`）：

```html
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/github-markdown-css@5/github-markdown.min.css">
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/codemirror@5/lib/codemirror.min.css">
<script src="https://cdn.jsdelivr.net/npm/mermaid@11/dist/mermaid.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/marked@12/marked.min.js"></script>
```

---

## 3. 代码分层职责

本项目**没有传统的 Controller / Service / DAO 分层**。真实的分层是"进程边界"分层：服务端（Node）与前端（浏览器）各只有 1 个逻辑文件，靠 REST 与 CDN 全局变量衔接。

### 3.1 服务端路由层（Express）

- **职责**：HTTP 请求处理、参数校验、文件系统读写、Markdown 渲染、错误响应
- **特点**：4 个路由全部平铺在 `server.js` 中，**无 Router 拆分、无中间件封装**；每个处理函数内自校验、自处理异常
- **典型签名**：`app.method('/api/xxx', (req, res) => { ... })`，全部为同步逻辑（唯一异步点是子进程回调）
- **参考代码**：`LightMDKit/server.js:19-94`（/api/select-folder）、`LightMDKit/server.js:97-168`（/api/load）、`LightMDKit/server.js:171-196`（GET /api/file）、`LightMDKit/server.js:199-224`（/api/refresh）

### 3.2 服务端进程管理层（守护进程）

- **职责**：端口占用检测 → 拉起后台守护进程 → 打开浏览器 → 主进程退出
- **特点**：与路由层同文件但物理隔离，位于文件末尾（`LightMDKit/server.js:226-311`），只依赖环境变量 `LIGHTMDKIT_DAEMON` 区分"启动器"与"守护进程"两种身份
- **参考代码**：`LightMDKit/server.js:265-311`

### 3.3 渲染共享层（UMD）

- **职责**：Markdown 预处理（缩进保留、表格续行合并、单波浪线删除线修正）与渲染
- **特点**：**同一份文件同时被 Node 与浏览器加载**，用 UMD 包装（`md-render.js:1-7`），是唯一一处"前后端复用"的代码
- **典型签名**：`renderMarkdown(markdown, marked)` —— `marked` 由调用方注入，避免模块内 `require`
- **参考代码**：`LightMDKit/public/md-render.js:1-7`、`LightMDKit/public/md-render.js:190-197`
- **服务端用法**：`LightMDKit/server.js:7` `const { renderMarkdown } = require('./public/md-render.js');`
- **前端用法**：全局 `MdRender`（`index.html:112` 引入），`LightMDKit/public/app.js:1121`

### 3.4 前端交互层（单个 IIFE）

- **职责**：DOM 操作、事件响应、File System Access API 读写、目录/内容渲染、模式切换、拖放打开
- **特点**：**全部逻辑封装在一个 `(function () { ... })()` 内**，不暴露任何对外 API（`app.js:1` 开头 / `app.js:2132` 结尾），也不使用 `'use strict'`（见 5.7 的偏差说明）
- **参考代码**：`LightMDKit/public/app.js:1-2154`

### 3.5 样式层（纯 CSS）

- **职责**：布局、GitHub 观感、状态样式、现代模式的即时渲染效果
- **特点**：**无 CSS 预处理器、无 CSS 变量**（全文没有 `:root` / `var(--x)`），颜色一律写字面色值；与 HTML 结构同序
- **参考代码**：`LightMDKit/public/style.css:1-1002`

### 3.6 层间调用规则

```
server.js ──require──▶ md-render.js          （服务端渲染，供 REST 接口使用）
                              ▲
index.html ──<script>──▶ md-render.js ── 全局 MdRender ──▶ app.js
```

- **前端不调用 REST 接口**：文件读写走浏览器 File System Access API（`showDirectoryPicker`），`server.js` 的 4 个接口是"替代或扩展"通道（README 已声明）。改前端功能时**不要顺手去调 `/api/load`**。
- **渲染入口唯一**：任何地方渲染 Markdown 都必须走 `MdRender.renderMarkdown(text, marked)`（`app.js:1108`、`1671`、`1699`），不要直接调 `marked.parse`，否则表格续行、单波浪线等修正会失效。

---

## 4. 命名规范

### 4.1 文件命名

| 层级 | 命名格式 | 示例 |
|-----|---------|------|
| 服务端入口 | 固定 `server.js` | `LightMDKit/server.js` |
| 前端脚本 | kebab-case（`md-render.js`） | `LightMDKit/public/md-render.js` |
| 前端主逻辑 | 固定 `app.js` | `LightMDKit/public/app.js` |
| 样式/页面 | 固定 `style.css` / `index.html` | `LightMDKit/public/style.css` |
| 构建脚本 | kebab-case | `LightMDKit/scripts/set-windows-gui.js` |
| 启动/停止脚本 | `动词.bat` / `动词.sh` 同名成对 | `LightMDKit/start.bat`、`LightMDKit/stop.sh` |

### 4.2 DOM 引用命名（`app.js:2-28`）

IIFE 顶部集中用 `const` 获取全部 DOM 引用，**以 `El` 后缀或 `btn` 前缀区分元素类型**：

```javascript
const btnRefresh = document.getElementById('btn-refresh');
const folderLabel = document.getElementById('folder-label');
const btnLoadFolder = document.getElementById('btn-load-folder');
const statusEl = document.getElementById('status');
const tocEl = document.getElementById('toc');
const contentEl = document.getElementById('content');
const btnToggleToc = document.getElementById('btn-toggle-toc');
const sidebar = document.querySelector('.sidebar');
const btnEdit = document.getElementById('btn-edit');
const editorEl = document.getElementById('editor');
const btnBack = document.getElementById('btn-back');
const btnNewTab = document.getElementById('btn-new-tab');
const resizeHandle = document.querySelector('.resize-handle');
const dropOverlay = document.getElementById('drop-overlay');
const btnMode = document.getElementById('btn-mode');
const btnViewFiles = document.getElementById('btn-view-files');
const btnViewToc = document.getElementById('btn-view-toc');
const filePanel = document.getElementById('file-panel');
const tocPanel = document.getElementById('toc-panel');
const fileListEl = document.getElementById('file-list');
const fileFilter = document.getElementById('file-filter');
```

| 对象类型 | 规范 | 示例 |
|---------|------|------|
| 按钮 | 有 `id` 的用 `btn` + PascalCase；ID 为 `btn-xxx` | `btnLoadFolder` ⇄ `#btn-load-folder` |
| 非按钮元素 | 名词 PascalCase + `El` 后缀 | `statusEl`、`tocEl`、`contentEl`、`editorEl`、`fileListEl` |
| 取不到 `id` 的元素 | `querySelector` + 名词 PascalCase | `sidebar`、`resizeHandle` |

**⚠️ 实际偏差（勿照抄，新增时请统一用 `El` 后缀）**：本项目并未 100% 遵守上述规则，以下 7 个引用既没有 `El` 后缀也不是 `btn` 前缀：`folderLabel`、`sidebar`、`resizeHandle`、`dropOverlay`、`filePanel`、`tocPanel`、`fileFilter`。新增 DOM 引用时**统一用 `xxxEl` / `btnXxx`**，不要继续扩散这种不一致。

### 4.3 变量与常量（`app.js:30-103`）

| 类型 | 规范 | 示例 |
|-----|------|------|
| 运行时可变的普通状态 | `let` + camelCase | `currentFiles`、`currentFile`、`currentMarkdownText`、`headingOffsets`、`fileHistory`、`sidebarView`、`dragDepth` |
| 布尔状态 | `is` / `has` 开头 | `isEditMode`、`isResizing`、`isWindows`（`LightMDKit/server.js:16`）、`hasDraggedFiles` |
| 常量 | `const` + UPPER_SNAKE_CASE | `PORT`、`DAEMON_ENV`、`SCAN_MAX_DEPTH`、`SCAN_MAX_FILES`、`SCAN_SKIP_DIRS`、`AUTOSAVE_INTERVAL_MS`、`MODE_STORAGE_KEY`、`SIDEBAR_VIEW_KEY`、`DROP_DB_NAME`、`DROP_STORE`、`DROP_TTL_MS`、`REAL_STRIKE_RE`、`DOUBLE_TILDE_DEL` |
| 元素/句柄类对象 | `let` + camelCase，初值 `null` | `cmEditor`、`currentFolderHandle`、`autosaveTimer`、`livePreviewTimer`、`observer`、`lastViewHeadingId` |
| 缓存 | 名词 + `Cache` | `imageUrlCache`（`Map`）、`headingCache` |

**每个状态变量上方必须有说明用途的注释**，这是本项目的硬习惯（`app.js:30-103` 中 20 余个状态变量无一例外）：

```javascript
  // { name: string, path: string, handle: FileSystemFileHandle }[]
  //   name = 文件名（文档内链接用 ./xxx.md 这种写法匹配时靠它）
  //   path = 相对当前目录根的路径，用 / 分隔（如「业务知识库/核心业务领域.md」），
  //          递归扫描子目录后 path 才是唯一标识 —— 不同子目录可能有同名文件
  let currentFiles = [];
  // 当前选中文件的相对路径（即 entry.path）
  let currentFile = null;
```

### 4.4 函数命名

**camelCase + 动词开头**，一词表达"做什么"：

| 语义 | 前缀 | 示例（含出处） |
|-----|------|--------------|
| 渲染/展示 | `render` | `renderFile`（`app.js:1098`）、`renderFileList`（`app.js:1023`）、`renderTocFromSource`（`app.js:374`）、`renderMermaid`（`app.js:1153`） |
| 加载/读取 | `load` | `loadFile`（`app.js:1269`）、`loadFolderHandle`（`app.js:1216`）、`loadModePreference`（`app.js:51`） |
| 更新局部状态 | `update` | `updateActiveTocItem`（`app.js:989`）、`updateActiveFileItem`（`app.js:1820`）、`updateLoadFolderButton`（`app.js:997`） |
| 用户动作 | 动词直述 | `selectFolder`（`app.js:1247`）、`refreshCurrentFile`（`app.js:1293`）、`toggleEditMode`（`app.js:1705`）、`setMode`（`app.js:1660`）、`setSidebarView`（`app.js:1797`）、`saveCurrentFile`（`app.js:1553`） |
| 查询/判断 | `is` / `get` / `has` / `find` | `isMarkdownName`（`app.js:179`）、`getTopVisibleHeadingId`（`app.js:313`）、`getHeadingsCached`（`app.js:352`）、`hasDraggedFiles`（`app.js:1943`）、`findEntryByPath`（`app.js:192`） |
| 收集/转换 | `collect` / `normalize` / `flatten` | `collectMarkdownFiles`（`app.js:234`）、`normalizePathForMatch`（`app.js:186`）、`flattenByDirectory`（`app.js:207`） |
| 包装/守卫函数 | 原文保存 + 同名重赋值 | `renderToc` 的真实实现先存进 `renderTocContent`，再把 `renderToc` 重赋值成带视图守卫的包装（`app.js:2130-2135`）—— 全项目唯一一例 |

**异步函数一律显式 `async`**，不加 `Async` 后缀（`saveCurrentFile`、`loadFile`、`selectFolder` 都是 `async`）。

**短小的回调参数用单字母**，是本项目的稳定习惯：`e`（事件）、`err`/`e`（错误）、`f`（file entry）、`h`（heading）、`m`（regex match）、`a`/`b`（排序比较）。

### 4.5 HTML ID 与 CSS 类名

**HTML ID：kebab-case，带类型前缀**（`index.html:22-102`）：

| 前缀 | 用途 | 示例 |
|-----|------|------|
| `btn-` | 按钮 | `btn-load-folder`、`btn-view-toc`、`btn-mode` |
| `*-panel` | 侧边栏面板容器 | `file-panel`、`toc-panel` |
| `*-list` | 列表容器 | `file-list` |
| `*-label` | 只读文本 | `folder-label` |
| `*-overlay` | 覆盖层 | `drop-overlay` |
| 无前缀 | 单一内容区 | `content`、`editor`、`toc`、`status`、`file-filter`、`file-count` |

**CSS 类名：kebab-case、块-元素式**（`style.css` 中共 41 个行首类选择器）：

```css
.file-list   .file-item   .file-name   .file-icon    /* 文件列表块 */
.toc         .toc-item    .toc-empty                 /* 目录块 */
.sidebar     .sidebar-header  .sidebar-title  .sidebar-panel  .sidebar-load  .sidebar-view-switch
```

**状态类独立命名、与基础类组合使用**：`.active`、`.hidden`、`.collapsed`、`.resizing`、`.live-preview`（`:263`、`:376`、`:593`、`:138`、`:134`、`:628`）：

```css
/* 正确：基础类 + 状态类组合 */
.file-item.active {
  background: #ddf4ff;
  border-left-color: #0969da;
}

/* 错误：把状态写进类名 */
.file-item-active { ... }
```

**⚠️ 偏差**：`.btn` 系列并不统一 —— 工具栏按钮是 `.btn`（`style.css:48`），侧边栏折叠按钮是 `.btn-toggle`（`:186`），加载文件夹按钮是 `.btn-load-folder`（`:210`），而视图切换按钮却把 btn 放在后面写成 `.view-btn`（`:245`）。新增按钮时请选最贴近的既有类，**不要发明第三种写法**。

---

## 5. JavaScript 编写规范

### 5.1 IIFE 结构模板

`app.js` 的内部组织顺序是稳定的六段式，新增代码请插入对应段落：

```javascript
(function () {
  // ① DOM 引用（const，集中声明）        app.js:2-28
  // ② 状态变量（let / const，带用途注释）  app.js:30-103
  // ③ 一次性初始化副作用（CodeMirror/Mermaid） app.js:99-151
  // ④ 工具函数（纯函数优先，无副作用）      app.js:163-367
  // ⑤ 业务函数（渲染 / 加载 / 模式 / 拖放） app.js:370-2000
  // ⑥ 事件绑定 + 启动初始化                app.js:846-1464
})();
```

**没有 `'use strict'`**（对比 `md-render.js:8` 有），详见 5.7。

### 5.2 注释规范（本项目最重要的特征）⭐

本项目的注释密度远高于同类小工具，且**注释回答的是"为什么"，不是"做了什么"**。这是项目的显式风格，新代码必须延续。

**特征一：解释"为什么这样写"而非"这行在干什么"**（`app.js:111-137`）：

```javascript
    cmEditor = CodeMirror.fromTextArea(editorEl, {
      // highlightFormatting 让语法标记带上 cm-formatting* class，现代模式靠它隐藏标记。
      // 传统模式没有对应 CSS 规则，所以开启它对现有外观零影响。
      //
      // strikethrough 必须关掉：CM5 自带的删除线把单个 ~ 也当定界符
      // （mode/markdown/markdown.js 里 `ch === '~' && stream.eatWhile(ch)` 后直接切换状态），
      // 于是「1~100、2~5」这种范围写法会被误判成删除线，两个 ~ 还会被当成标记隐藏掉，
      // 直接显示成「1100、25」。真正的 ~~...~~ 改由 markText 单独标注（见 refreshStrikeMarks）。
      // 关掉它不影响传统模式：style.css 本来就没有样式化 cm-strikethrough / cm-formatting-strikethrough。
      mode: { name: 'gfm', highlightFormatting: true, strikethrough: false },
      // 保留 'github'：npm 包里其实没有这个主题（见 index.html 顶部说明），
      // 它的作用是让容器带 cm-s-github 类，从而不启用 cm-s-default 的默认 token 配色。
      // 编辑器实际配色来自 style.css 的 .content-wrapper .cm-* 规则。
      theme: 'github',
```

**特征二：解释"为什么不用那个显而易见的方案"**（`app.js:198-202`）：

```javascript
  // 把扫描结果铺平成「目录 → 其下文件」的顺序。
  // 不能简单地对整条路径做 localeCompare —— 那样 `sub/deep/d.md`（'/'=0x2F）
  // 会排在 `sub/note.md`（'深'=0x6DF1）前面，子目录和文件交替插花，
  // 渲染分组时同一个目录就被拆成好几段、标题重复出现。
  // 这里按树逐层铺：某个目录自身的文件排完，再进它的子目录。
```

**特征三：标注"踩过的坑"与边界值**（`md-render.js:158-163`、`app.js:85-92`）：

```javascript
  // marked 12 的删除线规则允许单个 ~ 作为定界符：
  //   del: /^(~~?)(?=[^\s~])([\s\S]*?[^\s~])\1(?=[^~]|$)/
  // 于是中文文档里常见的范围写法（"1~100、2~5"）里两个不相干的 ~ 会被配对，
  // 中间的内容被渲染成删除线。这里覆盖 del 分词器，只认双波浪线 ~~。
  // marked 对自定义分词器的约定：返回 false 才回退到内置实现，返回 undefined
  // 表示“不匹配”，借此屏蔽掉内置的单波浪线删除线。
```

```javascript
  // 递归扫描的边界：避免误选 node_modules 之类的巨型目录时卡死界面。
  // 超过上限时停止深入，而不是报错 —— 已扫到的文件照常可用。
  const SCAN_MAX_DEPTH = 6;
  const SCAN_MAX_FILES = 3000;
```

**其他铁律**：

- **解释性的注释不写句号结尾、不用英文**（全文注释均为中文）；引用外部库行为时保留原文英文片段（如 `ch === '~' && stream.eatWhile(ch)`）。
- **CSS 里的"为什么"同样写注释**（`style.css:729-738`）：

```css
/*
 * 语法标记：默认隐藏。
 * 不能用 display:none —— 那会让 CodeMirror 算错光标位置与换行。
 * 这里用「缩到 1px + 负字距抵消宽度 + 透明」保留占位，光标定位照常。
 * （行高不会因此塌陷：每个 .CodeMirror-line 的 strut 由上面的 line-height 决定）
 *
 * 中间那层 .CodeMirror-line 是为了提权：标记元素除了 cm-formatting* 之外，
 * 还会同时带上所在块的正文类（如 # 标记会带 cm-header cm-header-1），
 * 而下面 .cm-header-1 的 font-size 与隐藏规则同权重。不加这一层的话，
 * 后写的 .cm-header-1 会把隐藏规则盖掉，# 就藏不住了。
 */
```

- **HTML 里也写**（`index.html:11-17`）：

```html
  <!--
    这里有意不引 CodeMirror 主题：codemirror@5 的 npm 包里 theme/ 只有 65 个主题、
    没有 github（引 theme/github.min.css 会 404）。编辑器配色由 style.css 里的
    .content-wrapper .cm-* 规则提供；app.js 里保留 theme:'github' 是为了让容器
    继续带 cm-s-github 类 —— 一旦摘掉，CodeMirror 会换成 cm-s-default，
    基础主题那批 .cm-s-default .cm-* 颜色就会生效，反而改变现有观感。
  -->
```

**特征四：段落用分隔线注释**（`app.js:1796`、`app.js:1880`，CSS `style.css:913-999` 用 `/* ---------- 标题 ---------- */`）：

```javascript
  // ---------------- 侧边栏视图：文件列表 / 当前文档目录 ----------------
  // ---------------- 拖放打开：拖入 .md 文件/文件夹时在新标签页打开 ----------------
```

**特征五："保持与…一致""必须…否则"这类约束要显式写出**，防止后来者破坏耦合关系（`app.js:327-329`、`app.js:426-427`、`app.js:1100-1103`）：

```javascript
  // 从 markdown 原文解析标题（现代模式用：那时没有渲染好的 DOM 可查）。
  // id 生成与 parseHeadingOffsets 完全同构 —— 同一个 generateHeadingId + 独立 Set，
  // 按出现顺序遍历 —— 因此两边算出的 id 天然对齐。
```

```javascript
  // 只认双波浪线的删除线（与 md-render.js 里 patchStrikethrough 的规则保持一致）。
```

### 5.3 状态管理

- **状态即模块级变量**，没有状态容器、没有发布订阅。任何函数都可以直接读写 `currentFile`、`currentMode` 等。
- **状态变更后必须显式调用"同步 UI"的函数**，而非依赖响应式。典型序列（`app.js:1146-1150`）：

```javascript
    applySurface();       // 决定浏览区/编辑器谁可见
    renderToc();          // 重建目录
    // 只更新高亮不重建列表：过滤框里正打字时重建会让输入框失焦
    updateActiveFileItem();
    renderMermaid();
```

- **`applySurface()` 是界面形态的唯一收口**（`app.js:1576-1626`）：传统-浏览 / 传统-编辑 / 现代 三种形态的显隐**只能**由它决定，禁止在业务函数里写 `xxx.style.display = ...`。原注释写得很明确：

```javascript
  // 三种界面形态（传统-浏览 / 传统-编辑 / 现代）的显隐统一由这里决定，
  // 不再散落在 toggleEditMode、loadFile、renderFile 各处分别写内联 style。
```

- **需要持久化的界面偏好**走 localStorage，键名以 `lightmdkit.` 为前缀，且**读取必须容错**（`app.js:47-54`）：

```javascript
  const MODE_STORAGE_KEY = 'lightmdkit.mode';
  let currentMode = loadModePreference();

  function loadModePreference() {
    try {
      return localStorage.getItem(MODE_STORAGE_KEY) === 'modern' ? 'modern' : 'traditional';
    } catch (e) {
      // 隐私模式/禁用存储时 localStorage 会抛异常，回退传统模式
      return 'traditional';
    }
  }
```

  写入同样要包 `try/catch`（`app.js:56-62`，注释：`// 存不下不影响本次使用`）。

- **高频重算结果要缓存**（`app.js:346-353`）：

```javascript
  // getValue() 是 O(n)，而光标移动很频繁，这里按文本内容缓存解析结果
  let headingCache = { text: null, list: [] };
  function getHeadingsCached(text) {
    if (headingCache.text !== text) {
      headingCache = { text, list: extractHeadings(text) };
    }
    return headingCache.list;
  }
```

- **状态声明的位置**：优先在顶部 ② 段集中声明；**与之强耦合、仅服务单个功能的临时状态允许就近声明**，但必须带注释。现有就近声明有：`headingCache`（`:337`）、`livePreviewTimer`（`:398`）、`strikeMarks`（`:413`）、`isResizing`（`:1159`）、`dragDepth`（`:1335`）、`observer`（`:1421`）。

### 5.4 异步与错误处理

**统一的前端错误处理三段式**（`app.js:1271-1274`、`app.js:1338-1341`、`app.js:1547-1551`、`app.js:2078-2081` 等 8 处）：

```javascript
    try {
      ...
    } catch (e) {
      console.error(e);
      setStatus(e.message, 'error');
    }
```

规则：

1. **底层异常必须先 `console.error(e)` 留痕，再 `setStatus(e.message, 'error')` 提示用户**。两件事都要做，不许只留其一。
2. **用户可见文案是中文**，但错误详情直接用底层 `e.message`（可能英文），**不做翻译**：`setStatus('保存失败: ' + e.message, 'error')`（`app.js:1549`）。
3. **面向用户的校验失败不抛异常，直接 `setStatus(..., 'error')` 后 `return`**（`app.js:1686-1689`）：

```javascript
    if (!currentFile) {
      setStatus('请先加载文件', 'error');
      return;
    }
```

4. **只有"调用方需要据此改变流程"时才 `throw`**。全项目仅一例：`saveCurrentFile` 保存失败后 `throw e`（`app.js:1550`），以便 `startAutosave` 捕获后停掉定时器（`app.js:1556-1564`）。
5. **静默模式用 `silent` 参数**，而非另写一个函数（`app.js:1531` `async function saveCurrentFile(silent = false)`）：自动保存传 `true`，不弹提示。
6. **可忽略的失败要写注释说明为什么可以忽略**（`app.js:1100-1112`、`app.js:1918`、`app.js:1932`）：

```javascript
      } catch (e) {
        // 忽略错误，回退到传入的句柄
      }
```

```javascript
    } catch (e) { /* IndexedDB 不可用时忽略 */ }
```

7. **取消操作不算错误**：`selectFolder` 单独识别 `AbortError`（`app.js:1244-1250`）。

```javascript
    } catch (e) {
      if (e.name === 'AbortError') {
        setStatus('已取消');
      } else {
        console.error(e);
        setStatus(e.message, 'error');
      }
      updateLoadFolderButton();
    }
```

### 5.5 事件绑定

- **绑定位置：就近原则** —— 事件监听紧跟其处理函数之后（`app.js:846` 紧跟 `refreshCurrentFile`，`app.js:1524` 紧跟 `updateBackButton`，`app.js:1754` 紧跟 `toggleEditMode`），最后一段（`app.js:2084-2086`）与启动初始化一起放在文末。
- **一行能写完就用箭头函数直接传引用**（`app.js:846`、`1804-1805`）：

```javascript
  btnRefresh.addEventListener('click', refreshCurrentFile);
  btnViewFiles.addEventListener('click', () => setSidebarView('files'));
  btnViewToc.addEventListener('click', () => setSidebarView('toc'));
```

- **列表项用事件委托**，不在渲染时为每项绑定（`app.js:1807-1811`）：

```javascript
  fileListEl.addEventListener('click', (e) => {
    const item = e.target.closest('.file-item');
    if (!item) return;
    openFileFromList(item.dataset.path);
  });
```

- **可选 DOM 元素绑定前判空**（`app.js:1756`、`1827`、`2084`）：

```javascript
  if (btnLoadFolder) {
    btnLoadFolder.addEventListener('click', selectFolder);
  }
```

### 5.6 本项目特有的两个模式

**模式一：函数装饰（在文末给已定义函数追加副作用）**（`app.js:2106-2113`）：

```javascript
  // 侧边栏不在「目录」视图时，重建目录毫无意义（还会把文件列表挤掉），
  // 直接返回；只有视图是目录时才重建内容并重建滚动高亮监听。
  const renderTocContent = renderToc;
  renderToc = function () {
    if (sidebarView !== 'toc') return;
    renderTocContent();
    setupTocObserver();
  };
```

设计意图（`app.js:458-460`）：调用点统一写 `renderToc()`，由装饰后的版本决定"当前是不是目录视图、要不要真的重建"，避免把视图判断散到每个调用点。

**模式二：节流重算用定时器 + 模式守卫**（`app.js:407-418`）：

```javascript
  // 现代模式编辑时目录与删除线标注都要跟着更新，防抖避免每个字符都重建
  let livePreviewTimer = null;
  function scheduleLivePreviewRefresh() {
    if (currentMode !== 'modern') return;
    if (livePreviewTimer) clearTimeout(livePreviewTimer);
    livePreviewTimer = setTimeout(() => {
      livePreviewTimer = null;
      if (currentMode !== 'modern') return;   // 定时器到期时模式可能已经切走
      renderToc();
      refreshStrikeMarks();
    }, 300);
  }
```

### 5.7 历史偏差（已修复）：`app.js` 的隐式全局

**该缺陷已修复，此处留档是为了说明「为什么 IIFE 里不能出现未声明的赋值」。**

`app.js:2130` 的 `renderToc = function () { ... }` 曾**没有 `let` / `const` / `function` 声明**，且 `app.js` 全文没有 `'use strict'`（对比 `md-render.js:8` 有）。当时：

- `renderToc` 实际是**挂在 `window` 上的全局变量**，与 5.1 节「IIFE 避免污染全局命名空间」的初衷相悖；
- 运行期能工作，仅因为所有调用点都在赋值语句之后才真正执行。**任何「在文件中部提前调用 `renderToc()`」的改动都会 `ReferenceError`**。

**修复方式**：已在 `app.js:44` 补上 `let renderToc;`（声明与赋值分离）；文末 `:1463-1468` 赋值时先用 `const renderTocContent = renderToc;` 留住被装饰目标，再重绑定为带视图守卫的版本。

**新增代码的正确做法**：若确需装饰，先声明再赋值。不要新增任何未声明的赋值 —— 本项目没有 `'use strict'` 兜底，这类错误不会被自动发现。

### 5.8 用户可见文案与图标

- **文案统一中文**：状态栏（`setStatus`）、空状态（`showEmptyState`）、按钮 `title`、错误提示均为中文。
- **图标用 HTML 实体或 `\u` 转义，不直接写 emoji**（避免文件编码问题）：

| 位置 | 写法 | 出处 |
|-----|------|------|
| HTML 中 | `&#128193;`（📁）、`&#9664;`/`&#9654;`（◀/▶）、`&#128269;`（🔍） | `index.html:25`、`44`、`53` |
| JS 中 | `'\u{1F4C1} 加载文件夹...'`、`'\u{1F4C4}'` | `app.js:995`、`app.js:1075` |

**⚠️ 偏差**：`index.html:75` 的空状态文案里直接写了字面 emoji `📁`，与上表规则不符。新增时请用实体形式。

`setStatus` 的第二参数只有三个合法值，**注意 `''` 是"普通信息"而非"无类型"**（`app.js:163-173`）：

```javascript
  function setStatus(msg, type = '') {
    statusEl.textContent = msg;
    statusEl.className = 'status ' + type;
    if (!msg) return;
    setTimeout(() => {
      if (statusEl.textContent === msg) {   // 期间若被新提示覆盖则不清理
        statusEl.textContent = '';
        statusEl.className = 'status';
      }
    }, 3000);
  }
```

- `''`：普通信息（灰）→ `'加载成功'` 之外的中性提示
- `'error'`：错误（红，`.status.error`）
- `'success'`：成功（绿，`.status.success`）

**例外**：需要往状态栏塞可点击链接时不能走 `setStatus`（它会 `textContent =` 覆盖），要像 `showNewTabLink`（`app.js:1960-1970`）那样直接操作 `statusEl`，并在注释里说明原因。

---

## 6. CSS 编写规范

### 6.1 组织顺序：严格按页面结构自上而下

`style.css` 的段落顺序与页面 DOM 顺序一一对应，新增样式请插入到对应段落，不要追加到文件末尾：

| 段 | 内容 | 行号 |
|---|------|------|
| ① | 全局重置（`*`）、`html, body`、`.app` | `style.css:1-19` |
| ② | 工具栏 `.toolbar` / `.toolbar-left` / `.toolbar-right` / `.btn` / `.folder-label` / `.status` | `style.css:21-94` |
| ③ | 主布局 `.main` | `style.css:97-103` |
| ④ | 侧边栏 `.sidebar` / `.resize-handle` / 折叠态 / `.sidebar-header` / `.btn-load-folder` / `.sidebar-view-switch` / `.sidebar-panel` | `style.css:104-284` |
| ⑤ | 文件列表 `.file-filter` / `.file-list-header` / `.file-list` / `.file-dir` / `.file-item` / `.file-empty` | `style.css:285-400` |
| ⑥ | 目录 `.toc` / `.toc-item` / `.toc-item.level-N` | `style.css:401-445` |
| ⑦ | 内容区 `.content-wrapper` / `.content-scroll` / `.markdown-body` / `.markdown-editor` / CodeMirror | `style.css:447-548` |
| ⑧ | Mermaid、平滑滚动、标题锚点 | `style.css:549-574` |
| ⑨ | 拖放遮罩 `.drop-overlay` | `style.css:575-601` |
| ⑩ | 新建文件弹窗 `.modal-*` / `.btn-primary` | `style.css:602-699` |
| ⑪ | **现代模式 `.live-preview` 全部规则** | `style.css:700-1002` |

每个段落之间用空行分隔，段落内的小节用 `/* ---------- 标题 ---------- */` 注释分隔（如 `style.css:913`、`926`、`964`）。

### 6.2 选择器规范

- 优先**类选择器**：`style.css` 全文**没有任何 `#id` 选择器**，样式只能挂在 HTML 的 `class` 上（`id` 仅供 `app.js` 取 DOM 引用用）。
- 层级深度**最多 3 层**，且第三层只出现在需要作用域隔离时（`.live-preview .CodeMirror-line .cm-formatting`，`:646`）。
- **状态类与基础类组合，不单独定义完整样式**（见 4.5）。
- **分组选择器换行书写**，一个选择器一行（`style.css:653-658`）：

```css
.live-preview .CodeMirror-line .cm-formatting-list,       /* - / * / 1. 列表符号 */
.live-preview .CodeMirror-line .cm-formatting-list-ul,
.live-preview .CodeMirror-line .cm-formatting-list-ol,
.live-preview .CodeMirror-line .cm-formatting-task,       /* [ ] 任务框 */
.live-preview .CodeMirror-line .cm-formatting-code-block, /* ``` 代码块围栏 */
.live-preview .CodeMirror-line .cm-formatting-escape {    /* 反斜杠转义 */
```

- **属性顺序**：`display`/定位 → 盒模型（含尺寸、内外边距）→ 排版（字体、行高、颜色）→ 背景 → 边框 → 其他（`transition` 等）。参考 `.btn`（`style.css:48-61`）与 `.file-item`（`style.css:350-365`）。
- **`display: flex` 的子项必写 `flex-shrink: 0` 或 `flex: 1`**，避免被内容挤压（`flex-shrink: 0` 出现于 `style.css:30`、`116`、`177`、`195`、`208`、`238`、`287`、`321`、`380`）。

### 6.3 颜色：GitHub Primer 色板，写字面色值

**不使用 CSS 变量**，全部写字面 hex。当前用到的色值（限定在这 16 个内取色，保证与 `github-markdown-css` 观感一致）：

| 色值 | 语义 | 出现次数 | 出处示例 |
|-----|------|---------|---------|
| `#1f2328` | 主文字色 | 10 | `style.css:11`、`:174` |
| `#24292f` | 次级文字色（旧版 Primer） | 3 | `style.css:58`、`:255`、`:300` |
| `#656d76` | 弱化文字 | 6 | `style.css:77`、`:84` |
| `#8c959f` | 更弱的文字 / 占位符 | 9 | `style.css:308`、`:330`、`:519` |
| `#59636e` | 现代模式下的次要文字 | 5 | `style.css:754`、`:688` |
| `#0969da` | 主题蓝（链接、激活、焦点） | 17 | `style.css:131`、`:263`、`:309` |
| `#0550ae` | 深蓝（编辑器内标题） | 1 | `style.css:524` |
| `#ddf4ff` | 选中背景蓝 | 3 | `style.css:227`、`:377`、`:438` |
| `#f6f8fa` | 浅灰背景（工具栏/表头） | 7 | `style.css:28`、`:176` |
| `#eff2f5` / `#e6eaef` | hover / active 灰 | 2 / 2 | `style.css:63`、`:67` |
| `#f3f8ff` | 主按钮 hover 背景 | 1 | `style.css:222` |
| `#d1d9e0` | 边框 | 10 | `style.css:29`、`:55` |
| `#cf222e` | 错误红（`.status.error`） | 1 | `style.css:89` |
| `#1a7f37` | 成功绿（`.status.success`） | 1 | `style.css:93` |
| `#8250df` | 紫色（图片语法标记） | 1 | `style.css:988` |
| `#ffffff` | 纯白 | 9 | `style.css:12` |

半透明色一律写成 `rgba()`，且必须使用主题蓝的 RGB 值 `9, 105, 218`（即 `#0969da`）：

```css
.box-shadow: 0 0 0 2px rgba(9, 105, 218, 0.15);   /* 焦点环，style.css:304 */
background: rgba(9, 105, 218, 0.08);              /* 拖放遮罩，style.css:582 */
background: rgba(9, 105, 218, 0.04);              /* 现代模式当前行，style.css:908 */
```

**⚠️ 偏差**：`#1f2328` 与 `#24292f` 是 Primer 两个版本的主文字色，项目里混用了（如 `.btn` 用 `#24292f`，`.sidebar-header` 用 `#1f2328`）。新增样式**优先用 `#1f2328`**（较新版本），不要新增第三种灰色。

### 6.4 现代模式必须做作用域隔离

所有现代模式（Typora 式即时渲染）的规则，**必须挂在 `.live-preview` 作用域下**，且通过 `.CodeMirror-line` 提权（原因见 `style.css:729-738` 的注释）：

```css
/* ✅ 正确：带 .live-preview 前缀 */
.live-preview .CodeMirror-line .cm-formatting { ... }

/* ❌ 错误：会污染传统模式的编辑界面 */
.CodeMirror-line .cm-formatting { ... }
```

`.live-preview` 类由 `app.js:1586` 的 `applySurface()` 加到 CodeMirror 根节点：

```javascript
      wrapper.classList.toggle('live-preview', isModern);
```

**反向兜底也必须写** —— 确保传统模式与改动前完全一致（`style.css:712-719`）：

```css
/*
 * 兜底：传统模式的编辑界面必须与改动前完全一致。
 * 基础 codemirror.css 会给 .CodeMirror-activeline-background 刷一层蓝底，
 * 万一 styleActiveLine 没能被正确关掉，这里也把它压回透明。
 */
.content-wrapper .CodeMirror:not(.live-preview) .CodeMirror-activeline-background {
  background: transparent;
}
```

### 6.5 已知偏差：兄弟选择器的使用不统一

`style.css:347-349` 明确写了"避免用兄弟选择器"，但 `style.css:145-148` 又用了：

```css
/* 隐藏目录栏后，主界面内容区扩大显示面积 */
.sidebar.collapsed ~ .content-wrapper .markdown-body,
.sidebar.collapsed ~ .content-wrapper .markdown-editor {
  min-width: 1200px;
}
```

```css
/* 同一分组内连续的文件项右侧对齐；这里只依赖 .file-item 自身样式，
   缩进由 padding-left 表达，避免用兄弟选择器（.file-dir ~ .file-item
   会匹配到后续所有分组，越界） */
```

**结论**：兄弟选择器**只在 `.sidebar.collapsed ~ .content-wrapper` 这种"结构唯一、后续元素确定"的场景可用**；涉及列表项、可能跨分组的场景一律用 padding 表达，不要用 `~`。

---

## 7. 服务端编写规范

### 7.1 路由写法模板

每个路由都是**平铺的 `(req, res) => {}`**，内部固定四步：**注释说明 → 取参 → 校验 → 处理/响应**。照抄 `app.js` 之外唯一的路由文件 `server.js`：

```javascript
// GET /api/file - read a specific file
app.get('/api/file', (req, res) => {
  const filePath = req.query.path;

  if (!filePath) {
    return res.status(400).json({ error: 'path query parameter is required' });
  }

  const resolvedPath = path.resolve(filePath);

  if (!fs.existsSync(resolvedPath)) {
    return res.status(404).json({ error: 'File does not exist: ' + resolvedPath });
  }

  const stats = fs.statSync(resolvedPath);
  if (!stats.isFile()) {
    return res.status(400).json({ error: 'Path is not a file: ' + resolvedPath });
  }

  try {
    const content = fs.readFileSync(resolvedPath, 'utf-8');
    const html = renderMarkdown(content, marked);
    res.json({ content, html, path: resolvedPath });
  } catch (err) {
    return res.status(500).json({ error: 'Failed to read file: ' + err.message });
  }
});
```
（出处：`LightMDKit/server.js:170-196`）

**要点**：

1. **路由前必须有 `// METHOD /path - 英文短说明` 单行注释**（`LightMDKit/server.js:18`、`96`、`170`、`198`）。全项目 4 个路由无一例外。
2. **入参一律 `path.resolve()` 后再校验**——`existsSync` → `statSync` → `isFile()/isDirectory()` 三连（`LightMDKit/server.js:104-113`、`178-187`、`206-215`）。
3. **提前返回用 `return res.status(...).json(...)` 一行写完**，不写 `else`。
4. **错误信息用英文短句 + `: ` + 具体值/`err.message`**（`LightMDKit/server.js:107`、`144`、`157`）。
5. **同步 FS API**（`existsSync`/`statSync`/`readFileSync`/`readdirSync`）是本项目的有意选择：单用户本地工具，无需为 I/O 做异步改造。**新增路由请沿用同步写法**。
6. **响应字段用 camelCase**（`folderPath`、`currentFile`、`filePath`）。
7. **只读接口用 `GET` + query（`?path=`），带副作用的用 `POST` + body**（`/api/refresh` 是 POST，因为语义上是"要求重扫"）。

### 7.2 错误处理规范

状态码使用是收敛的，只有 4 种：

| 状态码 | 触发条件 | 出处 |
|-------|---------|------|
| `400` | 必填参数缺失（`'folderPath is required'`）；传了文件但期望目录（或反之） | `LightMDKit/server.js:101`、`112`、`149`、`175`、`186`、`203`、`214` |
| `404` | 路径不存在 | `LightMDKit/server.js:107`、`144`、`181`、`209` |
| `500` | 系统能力/IO 失败（非 Windows 弹窗、PowerShell 失败、超时、目录不可读、文件不可读） | `LightMDKit/server.js:22`、`68`、`75`、`129`、`157`、`194`、`222` |
| `200`（默认） | 成功时**直接 `res.json(payload)`，不写 `res.status(200)`** | `LightMDKit/server.js:92`、`161`、`192`、`220` |

**唯一响应格式**：

```javascript
res.status(400).json({ error: 'folderPath is required' });   // 失败：只有 error 字段
res.json({ path: selectedPath });                            // 成功：业务字段
```

**取消语义不用错误码**：用户在弹窗里点"取消"时返回 `200` + `{ cancelled: true }`（`LightMDKit/server.js:79-81`），由前端判断，而不是 4xx。

**进程级错误**（无 HTTP 响应可写）走 `console.error` + `process.exit(1)`（`LightMDKit/server.js:280-288`）：

```javascript
  server.on('error', (err) => {
    if (err.code === 'EADDRINUSE') {
      console.error(`Port ${PORT} is already in use.`);
      process.exit(1);
    } else {
      console.error('Server error:', err.message);
      process.exit(1);
    }
  });
```

### 7.3 日志记录规范

**API**：只用 Node 内置 `console.*`，无日志库、无日志文件、无级别配置。

**实际用法分布（与"统一加 `[LightMDKit]` 前缀"的预期不符，如实记录）**：

| 场景 | 写法 | 出处 |
|-----|------|------|
| **接口被调用**（入口埋点） | `console.log('[LightMDKit] /api/select-folder requested')` | `LightMDKit/server.js:20` —— **全项目唯一带前缀的接口日志** |
| 服务启动成功 | `console.log(\`LightMDKit started at ${url}\`)` | `LightMDKit/server.js:301`（**无前缀**） |
| 端口占用提示 | `console.log('\n端口 3456 已被占用，LightMDKit 可能已在运行。')` | `LightMDKit/server.js:307-308`（**无前缀**，且是中文） |
| 子进程错误详情 | `console.error('PowerShell stderr:', stderr)` | `LightMDKit/server.js:74`（**无前缀**） |
| 服务/进程错误 | `console.error('Server error:', err.message)` | `LightMDKit/server.js:285`（**无前缀**） |
| 前端图片加载失败 | `console.warn('[LightMDKit] 图片加载失败:', rel, e)` | `app.js:1196` —— **前端唯一带前缀的日志** |
| 前端其他异常 | `console.error(e)`（裸错误对象） | `app.js:1248`、`1272`、`1293`、`1339`、`1548`、`2028`、`2042`、`2079` |
| 可降级的初始化失败 | `console.warn('CodeMirror/Mermaid init failed, falling back to textarea:', e)` | `app.js:156` |

**结论与新增代码建议**：

- 前缀 `[LightMDKit]` 目前**不是全局约定**，只在"接口入口日志"和"启动脚本"（`start.bat`/`start.sh`/`stop.bat` 的 echo）中稳定使用。**新增接口调用日志请沿用 `[LightMDKit] /api/xxx requested` 格式**；其余错误日志保持 `console.error('描述:', err.message)` 的"英文短语 + 冒号 + 细节"写法即可，不必强加前缀。
- **不要引入日志库**（如 `winston`/`pino`）—— 项目零额外依赖的取向很明确（`package.json` 只有 `express` + `marked` 两个运行时依赖）。

### 7.4 进程模型要点

- **端口硬编码** `const PORT = 3456;`（`LightMDKit/server.js:11`），**没有环境变量覆盖机制**。若要改端口，需同时改此处与 README、排障指南。
- **两种身份靠环境变量区分**（`LightMDKit/server.js:226-227`、`291-295`）：

```javascript
const DAEMON_ENV = 'LIGHTMDKIT_DAEMON';

async function main() {
  if (process.env[DAEMON_ENV] === '1') {
    runDaemon();
    return;
  }
  ...
```

- 主进程负责"检测端口 → 拉起守护进程（`spawnDaemon`）→ 等 2 秒 → 打开浏览器 → 退出"（`LightMDKit/server.js:297-311`）；守护进程只 `app.listen`，**不自动打开浏览器**（`LightMDKit/server.js:276-278` 有注释说明原因）。
- 平台分支集中在 `openBrowser`（`LightMDKit/server.js:233-233`）：`win32` / `darwin` / 其余 `xdg-open`。

---

## 8. 常用工具函数

### 8.1 前端（`public/app.js`）

| 功能 | 函数 | 位置 |
|-----|------|------|
| 状态栏提示（3s 自动消失） | `setStatus(msg, type = '')` | `LightMDKit/public/app.js:167-177` |
| 是否 Markdown 文件（扩展名判断） | `isMarkdownName(name)` | `LightMDKit/public/app.js:179-182` |
| 路径归一化（`\`→`/`、去 `./`、转小写） | `normalizePathForMatch(path)` | `LightMDKit/public/app.js:186-188` |
| 按路径/文件名查文件记录 | `findEntryByPath(path)` | `LightMDKit/public/app.js:192-200` |
| 递归扫描目录下所有 md | `collectMarkdownFiles(dirHandle, prefix, depth, out)` | `LightMDKit/public/app.js:234-258` |
| HTML 转义 | `escapeHtml(str)` | `LightMDKit/public/app.js:260-266` |
| 标题转 slug | `slugify(text)` | `LightMDKit/public/app.js:268-275` |
| 标题 ID 生成（同名去重，`-1`/`-2`） | `generateHeadingId(text, usedIds)` | `LightMDKit/public/app.js:277-287` |
| 标题 → 字符偏移表 | `parseHeadingOffsets(text)` | `LightMDKit/public/app.js:289-304` |
| 从 markdown 原文抽标题（现代模式） | `extractHeadings(text)` | `LightMDKit/public/app.js:330-348` |
| 带缓存的标题解析 | `getHeadingsCached(text)` | `LightMDKit/public/app.js:352-357` |
| 按字符偏移定位光标 | `jumpToOffset(offset)` | `LightMDKit/public/app.js:360-371` |
| 空状态展示 | `showEmptyState(msg)` | `LightMDKit/public/app.js:1093-1096` |
| 相对图片 → objectURL | `resolveImages()` | `LightMDKit/public/app.js:1173-1212` |
| 释放图片缓存 | `clearImageCache()` | `LightMDKit/public/app.js:1164-1169` |
| 统一界面显隐 | `applySurface()` | `LightMDKit/public/app.js:1614-1657` |
| 保存当前文件（可静默） | `saveCurrentFile(silent = false)` | `LightMDKit/public/app.js:1553-1574` |
| 目录高亮跟随滚动 | `setupTocObserver()` | `LightMDKit/public/app.js:2111-2126` |

**使用示例**：

```javascript
// 状态提示：type 只能是 '' / 'error' / 'success'
setStatus('加载成功', 'success');
setStatus('请先加载文件', 'error');

// 路径匹配：三处（文件列表过滤、文档内链接、拖放定位）统一走它，保证口径一致
const query = normalizePathForMatch(fileFilter.value.trim());
const visible = currentFiles.filter(f => normalizePathForMatch(f.path || f.name).indexOf(query) !== -1);

// 渲染 Markdown：必须走 MdRender，不能直接调 marked.parse
const html = MdRender.renderMarkdown(text, marked);
```

### 8.2 共享渲染（`public/md-render.js`，前后端都能用）

| 功能 | 函数 | 位置 |
|-----|------|------|
| **渲染总入口** | `renderMarkdown(markdown, marked)` | `LightMDKit/public/md-render.js:190-197` |
| 保留行首缩进（`&nbsp;` 替换） | `preserveIndent(markdown)` | `LightMDKit/public/md-render.js:10-57` |
| 合并表格跨行单元格（`<br>`） | `mergeTableCells(markdown)` | `LightMDKit/public/md-render.js:96-156` |
| 修正删除线只认 `~~` | `patchStrikethrough(marked)` | `LightMDKit/public/md-render.js:167-188` |

```javascript
// 服务端（server.js:7 已 require）
const { renderMarkdown } = require('./public/md-render.js');
const html = renderMarkdown(content, marked);

// 浏览器（index.html:112 引入，全局 MdRender）
const html = MdRender.renderMarkdown(text, marked);
```

**改这个文件前必读**：`renderMarkdown` 的三步顺序（`preserveIndent` → `mergeTableCells` → 带 `{ gfm: true, breaks: true }` 的 `marked.parse`）不可调换；`patchStrikethrough` 有 `WeakSet` 幂等保护（`md-render.js:165-171`），可安全重复调用。改动后**必须同步升高 `index.html` 的 `md-render.js?v=`**（见 2.3）。

### 8.3 服务端（`server.js`）

| 功能 | 函数 | 位置 |
|-----|------|------|
| 延时 | `sleep(ms)` | `LightMDKit/server.js:229-231` |
| 跨平台打开浏览器 | `openBrowser(url)` | `LightMDKit/server.js:233-249` |
| 端口占用检测（监听即测） | `isPortInUse(port)` | `LightMDKit/server.js:251-263` |
| 拉起后台守护进程 | `spawnDaemon()` | `LightMDKit/server.js:265-273` |
| 守护进程实际监听 | `runDaemon()` | `LightMDKit/server.js:275-289` |
| 启动编排 | `main()` | `LightMDKit/server.js:291-311` |

> 旧版文档提到的 `findPidByPort()` / `killProcess()` **在当前代码中已不存在**（`server.js` 现为纯 Node 端口探测，不再调 `netstat`+`taskkill`），引用时请以本节为准。

---

## 9. 代码生成与构建

### 9.1 唯一构建命令：`npm run build:win`

```json
  "scripts": {
    "start": "node server.js",
    "build:win": "pkg . --output dist/lightmdkit.exe && node scripts/set-windows-gui.js"
  },
```
（出处：`LightMDKit/package.json:7-10`）

| 项 | 说明 |
|---|------|
| 工具 | [`pkg`](https://github.com/vercel/pkg) 5.8.1（`devDependencies`） |
| 目标平台 | `node18-win-x64`（`LightMDKit/package.json:11-18` 的 `pkg.targets`） |
| 打包资源 | `public/**/*`（`pkg.assets`）—— **新增 `public/` 下的文件会被自动带上，但若新增其它目录的资源需同步维护 `assets`** |
| 产物 | `LightMDKit/dist/lightmdkit.exe`（**已纳入 git**：`git ls-files dist` 返回该文件；`.gitignore` 并未忽略 `dist/`） |

### 9.2 打包后处理脚本 `scripts/set-windows-gui.js`

**作用**：把 pkg 产物从 `WINDOWS_CUI`（子系统 3，双击弹出黑框）改为 `WINDOWS_GUI`（子系统 2，双击无命令行窗口）。

**这不是"代码生成"，而是二进制修补**，脚本本身要遵守的规范是"**每一步都校验，失败即 `process.exit(1)`**"（`LightMDKit/scripts/set-windows-gui.js:11-50`）：

```javascript
// PE header offset is stored at DOS header offset 0x3C
const peOffset = buf.readUInt32LE(0x3C);

// Validate PE signature
const peSig = buf.toString('ascii', peOffset, peOffset + 4);
if (peSig !== 'PE\x00\x00') {
  console.error('Invalid PE file: missing PE signature');
  process.exit(1);
}
```

要点：命令行参数可覆盖默认路径（`process.argv[2]`，`:4`）；**幂等**——已是 GUI 子系统时直接成功退出（`:40-43`）。

### 9.3 哪些文件不要手改

| 文件 | 说明 |
|-----|------|
| `LightMDKit/package-lock.json` | npm 自动生成，只通过 `npm install` 间接修改 |
| `LightMDKit/dist/lightmdkit.exe` | `npm run build:win` 产物，**不要手工编辑**；若要发布新版本，重新构建并提交 |

> 除上述两者，本项目**没有代码生成器**（无 OpenAPI codegen、无脚手架、无模板引擎）。`public/` 下的文件全部手写。

---

## 10. 本地持久化与数据兼容

**本项目不使用数据库**，没有 `migrations/`、`sql/` 目录。与"数据升级"等价的概念是**浏览器端持久化数据的键名与结构兼容**，规范如下。

### 10.1 localStorage

| 键 | 取值 | 定义位置 | 读写函数 |
|---|------|---------|---------|
| `lightmdkit.mode` | `'modern'` / `'traditional'` | `LightMDKit/public/app.js:48` | `loadModePreference()` / `saveModePreference()`（`:41-56`） |
| `lightmdkit.sidebarView` | `'toc'` / `'files'` | `LightMDKit/public/app.js:69` | `loadSidebarView()` / `saveSidebarView()`（`:62-77`） |

**规范**：

1. 键名前缀固定 `lightmdkit.`，用 `.` 分隔（**不是** `:` 或 `-`）。
2. **读取必须做白名单校验 + 默认值回退**，不能直接信任存储值：

```javascript
  function loadSidebarView() {
    try {
      return localStorage.getItem(SIDEBAR_VIEW_KEY) === 'toc' ? 'toc' : 'files';
    } catch (e) {
      // 隐私模式/禁用存储时 localStorage 会抛异常，回退文件列表
      return 'files';
    }
  }
```

3. **读写都要 `try/catch`**，存储不可用时静默降级，不能影响功能（`app.js:56-62`）。
4. **键名提为 `const XXX_KEY`**，不在业务代码里散写字面量。

### 10.2 IndexedDB（拖放句柄跨标签页传递）

| 项 | 值 | 位置 |
|---|----|------|
| 数据库名 | `'lightmdkit'` | `LightMDKit/public/app.js:1885` |
| 对象存储 | `'drops'` | `LightMDKit/public/app.js:1886` |
| 版本 | `1` | `LightMDKit/public/app.js:1891` |
| 记录有效期 | 24 小时（`DROP_TTL_MS`） | `LightMDKit/public/app.js:1887` |

**升级/兼容规范**（若将来要改结构）：

1. 结构变更**必须升高 `indexedDB.open(DROP_DB_NAME, 1)` 的版本号**，并在 `onupgradeneeded` 中判断 `objectStoreNames.contains` 后再建（`app.js:1870-1874`）——现有代码已是这个防御写法，直接沿用：

```javascript
  function openDropDb() {
    return new Promise((resolve, reject) => {
      const req = indexedDB.open(DROP_DB_NAME, 1);
      req.onupgradeneeded = () => {
        if (!req.result.objectStoreNames.contains(DROP_STORE)) {
          req.result.createObjectStore(DROP_STORE);
        }
      };
      req.onsuccess = () => resolve(req.result);
      req.onerror = () => reject(req.error);
    });
  }
```

2. **记录需带时间戳并主动清理**：写入时 `record.ts = Date.now()`（`app.js:1953`），启动时调 `idbPurgeExpired()`（`app.js:1922`）用游标删除过期记录（`app.js:1900-1919`）。
3. **清理失败必须吞掉**（`app.js:1918` `catch (e) { /* IndexedDB 不可用时忽略 */ }`），不能阻塞启动。

---

## 11. 配置文件编写规范

> 本项目是**代码驱动型项目**：配置文件只有 `LightMDKit/package.json` 一个，不承载业务逻辑，因此**不单独输出《配置文件编写指南》**，在此作为一节说明即可。

### 11.1 配置文件位置

| 配置类型 | 位置 | 命名格式 |
|---------|------|---------|
| 依赖与脚本 | `LightMDKit/package.json` | npm 标准 |
| 依赖锁定 | `LightMDKit/package-lock.json` | npm 自动生成 |
| 忽略规则 | `LightMDKit/.gitignore` | 每行一条，目录以 `/` 结尾 |
| 打包清单 | `LightMDKit/package.json` 的 `pkg` 字段 | 内嵌在 package.json |

非配置但常被误认为配置的：`start.bat`/`start.sh`/`stop.bat`/`stop.sh` 是**可执行脚本**（会检查 Node 环境、自动 `npm install`），不是配置文件，改动它们不需要重启服务。

### 11.2 配置文件格式

- **文件格式**：JSON（`package.json`），**不支持注释**
- **缩进**：2 空格
- **命名风格**：npm 字段用 camelCase（`devDependencies`）；包名用 kebab-case（`lightmdkit`）；脚本名用 `动词:平台` 或 `动词`（`start`、`build:win`）
- **`package.json` 顶层字段顺序**：`name` → `version` → `description` → `main` → `bin` → `scripts` → `pkg` → `dependencies` → `devDependencies`（新字段追加到对应分组，不要打乱现有顺序）

### 11.3 配置模板（当前实际内容）

**分析来源**：`LightMDKit/package.json`（唯一的 JSON 配置文件）

```json
{
  "name": "lightmdkit",
  "version": "1.0.0",
  "description": "Lightweight no-install local Markdown editor and viewer with GitHub-style rendering",
  "main": "server.js",
  "bin": "server.js",
  "scripts": {
    "start": "node server.js",
    "build:win": "pkg . --output dist/lightmdkit.exe && node scripts/set-windows-gui.js"
  },
  "pkg": {
    "targets": ["node18-win-x64"],
    "assets": ["public/**/*"]
  },
  "dependencies": {
    "express": "^4.18.2",
    "marked": "^12.0.0"
  },
  "devDependencies": {
    "pkg": "^5.8.1"
  }
}
```

| 字段 | 类型 | 必填 | 说明 |
|-----|------|------|------|
| `name` | string | 是 | `lightmdkit`（全小写，与品牌名 LightMDKit 的显示写法不同） |
| `version` | string | 是 | 语义化版本 |
| `description` | string | 否 | 英文一句话描述 |
| `main` / `bin` | string | 是 | 均为 `server.js` |
| `scripts.start` | string | 是 | `node server.js`，**不用 nodemon/并发工具** |
| `scripts.build:win` | string | 否 | 打包 + PE 子系统修补，链式 `&&` |
| `pkg.targets` | string[] | 否 | 固定 `node18-win-x64` |
| `pkg.assets` | string[] | 否 | 固定 `public/**/*` |
| `dependencies` | object | 是 | **只允许 `express` + `marked`**，新增运行时依赖需有充分理由 |
| `devDependencies` | object | 否 | 只放构建期依赖（当前仅 `pkg`） |

### 11.4 环境变量与敏感信息

| 变量 | 值 | 用途 | 位置 |
|-----|---|------|------|
| `LIGHTMDKIT_DAEMON` | `'1'` | 标记当前进程为守护进程（不是启动器） | `LightMDKit/server.js:227`、`292` |

- **这是本项目唯一的环境变量**，且只用于内部进程身份区分，**不用于配置端口等参数**（端口是 `LightMDKit/server.js:11` 的硬编码常量）。
- **敏感信息**：无。项目为纯本地工具，不发起任何外部网络请求（CDN 资源由浏览器直连），不收集用户数据，`.gitignore` 只需忽略 `node_modules/`。

### 11.5 新增/修改配置

**新增运行时依赖**（需谨慎，优先考虑能否用现有两个依赖实现）：

1. `npm install <package>`（自动改写 `package.json` + `package-lock.json`）
2. 在 `server.js` 顶部按现有顺序追加 `require()`（`LightMDKit/server.js:1-8`：内置模块在前，三方模块在后）
3. **若该依赖在前端也需要**，改用 `index.html` 的 CDN `<script>` 引入（固定主版本号），**不要添加到 `dependencies`** —— 前端没有打包步骤，`node_modules` 里的包无法直接被浏览器加载
4. `npm start` 验证服务可启动

**修改 `pkg` 打包范围**：改了 `public/` 之外的静态资源目录时，必须同步更新 `package.json` 的 `pkg.assets`，否则打包后运行时缺文件。改完执行 `npm run build:win` 重新出包。

**修改注意事项**：

- `package-lock.json` **不要手工编辑**，只通过 `npm install` / `npm ci` 变更
- 端口 3456 若要改，需同步更新 README.md、排障指南与本文档（没有配置项可改，只能改代码）
- 改完 `package.json` 的 `scripts` 后无需重启服务，改 `dependencies` 必须重新 `npm install`

---

## 12. 新增功能开发指引

以下指引均以**当前代码结构**（含侧边栏加载按钮、视图切换、现代模式、自动保存）为基准，示例可直接改造成实际代码。

### 12.1 如何加一个按钮

以在工具栏加"导出"按钮为例。

**第 1 步：HTML** —— 加到 `index.html` 的 `.toolbar-right` 内，**放在 `#status` 之前**（状态栏始终最右，`index.html:28-35`）：

```html
<button id="btn-export" class="btn" title="导出当前文件">导出</button>
```

**第 2 步：JS 取 DOM 引用** —— 追加到 `app.js:22` 之后（DOM 引用段的末尾），保持"id 用 `btn-` 前缀、变量用 `btn` + PascalCase"：

```javascript
  const btnExport = document.getElementById('btn-export');
```

**第 3 步：JS 写处理函数** —— 放在业务函数段（`app.js:370-2000`）中语义相近的位置，**函数内先做前置校验再干活**：

```javascript
  async function exportCurrentFile() {
    if (!currentFile) {
      setStatus('请先加载文件', 'error');
      return;
    }
    try {
      // 导出逻辑
      setStatus('已导出', 'success');
    } catch (e) {
      console.error(e);
      setStatus('导出失败: ' + e.message, 'error');
    }
  }
```

**第 4 步：JS 绑定事件** —— 紧跟函数之后（就近原则，见 5.5）：

```javascript
  btnExport.addEventListener('click', exportCurrentFile);
```

**第 5 步：CSS（可选）** —— 若沿用 `.btn` 则无需新样式；若要独立外观，**在 `style.css` 的工具栏段（`:21-94`）内追加**，类名用 kebab-case：

```css
.btn-export {
  color: #0969da;
  border-color: #0969da;
}
```

**第 6 步：升版本号** —— 改了 `app.js`（必改）和/或 `style.css`，把 `index.html` 里对应的 `?v=N` 加 1。

**若按钮要放在侧边栏**（像 `btn-load-folder`）：结构在 `index.html:40-69`，样式在 `style.css:207-228`；侧边栏按钮**不使用 `.btn` 类**，而是各自独立类名（`.btn-load-folder`、`.btn-toggle`、`.view-btn`），需在 `style.css` 侧边栏段内新写。

### 12.2 如何加一个事件

以键盘快捷键 `Ctrl+S` 保存为例。

**全局事件**加在文末"事件绑定 + 启动初始化"段（`app.js:2002-2131` 区域），与既有的 `document.addEventListener('dragenter'|'dragover'|'dragleave'|'drop')` 并列：

```javascript
  document.addEventListener('keydown', async (e) => {
    if (!(e.ctrlKey || e.metaKey) || e.key !== 's') return;
    e.preventDefault();
    if (!isEditMode) return;
    try {
      await saveCurrentFile();
    } catch (err) {
      // 保存失败已由 saveCurrentFile 内部提示
    }
  });
```

**规范要点**：

1. **先 `return` 掉不关心的分支**，不要写嵌套 `if`。
2. **必须 `e.preventDefault()` 的场景**：会触发浏览器默认行为（Ctrl+S 保存页面、拖放打开文件）。参考 `app.js:2009` 的注释 `// 必须阻止默认行为才允许 drop；仅针对文件拖拽，避免影响编辑器内的文本拖放`。
3. **全局监听要判断是否该抢事件**：例如 `fileFilter` 的 Esc 清空监听会 `e.stopPropagation()`（`app.js:1839-1845`），避免被上层监听再次处理。
4. **元素级事件优先用委托**（见 5.5），只在容器上绑一次。
5. **元素可能不存在时先判空**（`app.js:1827`）。

以在**文件列表项上加双击重命名**为例（元素级委托）：

```javascript
  fileListEl.addEventListener('dblclick', (e) => {
    const item = e.target.closest('.file-item');
    if (!item) return;
    startRename(item.dataset.path);
  });
```

### 12.3 如何加一个状态

以"是否显示空状态提示"或更实际的"编辑器字号缩放比"为例。

**第 1 步：声明** —— 加到 `app.js:30-103` 的状态段，**必须带用途注释**：

```javascript
  // 编辑器字号缩放比（1 = 默认），只影响现代模式
  let editorZoom = 1;
```

若状态是**持久化的界面偏好**，照 `sidebarView` 的写法三件套：常量键名 + load + save（`app.js:65-83`）：

```javascript
  const ZOOM_STORAGE_KEY = 'lightmdkit.editorZoom';
  let editorZoom = loadEditorZoom();

  function loadEditorZoom() {
    try {
      const raw = parseFloat(localStorage.getItem(ZOOM_STORAGE_KEY));
      return Number.isFinite(raw) && raw >= 0.5 && raw <= 3 ? raw : 1;
    } catch (e) {
      // 隐私模式/禁用存储时 localStorage 会抛异常，回退默认值
      return 1;
    }
  }

  function saveEditorZoom(zoom) {
    try {
      localStorage.setItem(ZOOM_STORAGE_KEY, String(zoom));
    } catch (e) {
      // 存不下不影响本次使用
    }
  }
```

**第 2 步：提供唯一的变更函数** —— 状态只在一处被写，UI 副作用集中在这里：

```javascript
  function setEditorZoom(zoom) {
    editorZoom = zoom;
    saveEditorZoom(zoom);
    applySurface();   // 界面显隐/样式统一收口（若有样式需刷新）
  }
```

**第 3 步：接入界面刷新收口** —— 若新状态影响面较大，扩展 `applySurface()`（`app.js:1614-1659`）而不是在业务函数里散写内联样式。它已有的职责就是"三种界面形态的唯一决定者"：

```javascript
    if (cmEditor) {
      const wrapper = cmEditor.getWrapperElement();
      wrapper.style.display = editorVisible ? '' : 'none';
      wrapper.classList.toggle('live-preview', isModern);
      ...
    }
```

**第 4 步：启动时恢复** —— 在文末启动段（`app.js:2115-2131`）应用一次，紧挨着 `applySurface()` 与 `setSidebarView(sidebarView)`：

```javascript
  applySurface();
  setSidebarView(sidebarView);
  applyZoom();        // ← 新增
```

**第 5 步：清理定时器/监听** —— 若状态持有定时器（照 `autosaveTimer`）或 `IntersectionObserver`（照 `observer`，`app.js:2088-2093`），必须在切换/停止时显式清理，防止泄漏。

### 12.4 如何加一个服务端 API

以"获取文件元信息"为例。

**第 1 步：在 `server.js` 加路由** —— 插入到 `/api/refresh`（`LightMDKit/server.js:199-224`）之后、`const url = ...`（`LightMDKit/server.js:226`）之前。**必须带 `// METHOD /path - 说明` 注释**，并沿用第四章的四步结构：

```javascript
// GET /api/file-info - get file metadata
app.get('/api/file-info', (req, res) => {
  const filePath = req.query.path;

  if (!filePath) {
    return res.status(400).json({ error: 'path query parameter is required' });
  }

  const resolvedPath = path.resolve(filePath);

  if (!fs.existsSync(resolvedPath)) {
    return res.status(404).json({ error: 'File does not exist: ' + resolvedPath });
  }

  const stats = fs.statSync(resolvedPath);
  if (!stats.isFile()) {
    return res.status(400).json({ error: 'Path is not a file: ' + resolvedPath });
  }

  try {
    res.json({
      path: resolvedPath,
      size: stats.size,
      mtime: stats.mtime
    });
  } catch (err) {
    return res.status(500).json({ error: 'Failed to read file info: ' + err.message });
  }
});
```

**第 2 步：加调用日志**（若这是新接口，照 `LightMDKit/server.js:20` 的格式）：

```javascript
  console.log('[LightMDKit] /api/file-info requested');
```

**第 3 步：前端调用**（可选；注意前端主体并不走 REST）：

```javascript
  async function fetchFileInfo(path) {
    const res = await fetch('/api/file-info?path=' + encodeURIComponent(path));
    if (!res.ok) {
      const data = await res.json().catch(() => ({}));
      throw new Error(data.error || '获取文件信息失败');
    }
    return res.json();
  }
```

**第 4 步：同步文档** —— 新增接口后必须更新 [API索引](./API索引.md) 的接口列表与详情，以及 README 的接口表。

**第 5 步：验证**：

```bash
curl "http://localhost:3456/api/file-info?path=D:/tmp/a.md"
curl -i "http://localhost:3456/api/file-info"          # 期望 400 + {"error":"path query parameter is required"}
```

### 12.5 如何加样式

1. **先定位段落**：按 6.1 的 `style.css` 十段结构，找到与页面位置对应的段，**插进该段内部**，不要追加到文件末尾。
2. **类名 kebab-case**，与既有块保持"块-元素"关系（如给 `.file-item` 加东西就叫 `.file-item-xxx`）。
3. **取色只能从 6.3 的 16 个 Primer 色值里选**，不要引入新色值；半透明用 `rgba(9, 105, 218, x)`。
4. **状态用独立类 + 组合**，不写进基础类名（4.5）。
5. **若样式只服务现代模式，必须加 `.live-preview` 前缀**（6.4），并考虑是否需要写"非现代模式的兜底"（参考 `style.css:716-718`）。
6. **涉及"为什么这么写"的非显然规则必须加注释**（5.2 特征一/三）。
7. **升 `index.html` 里的 `style.css?v=`**。

### 12.6 加功能时的自查顺序

```
1. 这是前端功能还是服务端功能？        → 前端优先（项目主线是浏览器 FS API）
2. 要新增状态吗？                     → 是则走 12.3：声明 + 变更函数 + 启动恢复
3. 会影响界面显隐吗？                 → 是则改 applySurface()，不要散写内联 style
4. 要渲染 Markdown 吗？               → 必须走 MdRender.renderMarkdown(text, marked)
5. 涉及路径比较吗？                   → 必须走 normalizePathForMatch / findEntryByPath
6. 事件绑定放哪？                     → 就近放在处理函数之后
7. 注释写了吗？                       → 解释"为什么"，不是"做了什么"
8. 改动的资源升 ?v= 了吗？            → style.css / app.js / md-render.js 三者之一必查
9. 新增接口更新 API索引.md 了吗？
```

---

## 13. 代码检查清单

提交或让 AI 生成代码前，逐项核对：

**文件与位置**

- [ ] 服务端代码写在 `LightMDKit/server.js`（未新建无意义的模块文件）
- [ ] 前端新增脚本放在 `LightMDKit/public/`，且已在 `index.html` 底部用 `<script src="x.js?v=1">` 引入
- [ ] 未手工修改 `package-lock.json` / `dist/lightmdkit.exe`
- [ ] 若新增了 `public/` 之外的静态资源，已同步更新 `package.json` 的 `pkg.assets`

**缓存与版本**

- [ ] 改动 `style.css` / `app.js` / `md-render.js` 后，`index.html` 中对应的 `?v=N` 已 +1
- [ ] 新增 CDN 依赖使用 `@主版本` 形式（如 `marked@12`），未写完整版本号

**命名**

- [ ] DOM 引用：`btn` + PascalCase（按钮）或名词 + `El` 后缀（其他），**未沿用 `folderLabel`/`fileFilter` 这类无后缀写法**
- [ ] 变量 camelCase；布尔量 `is`/`has` 前缀；常量 UPPER_SNAKE_CASE
- [ ] 函数 camelCase 且动词开头；异步函数用 `async`，不加 `Async` 后缀
- [ ] HTML id 用 kebab-case 带类型前缀；CSS 类名 kebab-case（且未把状态写进类名）

**前端代码**

- [ ] 代码写在 `app.js` 的 IIFE 内，未新增全局变量（**特别注意：不要写未声明的赋值**，参见 5.7 的 `renderToc` 偏差）
- [ ] 新状态变量有用途注释；持久化偏好用 `lightmdkit.xxx` 键名且读写都包 `try/catch`
- [ ] 错误处理完整：`console.error(e)` + `setStatus(e.message, 'error')` 两件事都做
- [ ] 用户可见文案为中文；图标用 HTML 实体或 `\u` 转义
- [ ] 渲染 Markdown 走 `MdRender.renderMarkdown(text, marked)`，未直接调 `marked.parse`
- [ ] 界面显隐改动收口在 `applySurface()`，未散写内联 `style.display`
- [ ] 事件绑定就近放置；列表项用事件委托；可选元素绑定前判空
- [ ] 定时器 / `IntersectionObserver` 在退出路径上有清理

**服务端代码**

- [ ] 路由有 `// METHOD /path - 说明` 注释
- [ ] 入参 `path.resolve()` 后完成 `existsSync` → `statSync` → `isFile()/isDirectory()` 校验
- [ ] 错误响应统一 `res.status(400|404|500).json({ error })`；成功响应直接 `res.json(...)` 不写 `res.status(200)`
- [ ] 校验失败用 `return res.status(...).json(...)` 提前返回，未写 `else`
- [ ] 新接口有 `console.log('[LightMDKit] /api/xxx requested')` 入口日志
- [ ] 未引入日志库或新的运行时依赖（除非有充分理由）

**CSS**

- [ ] 样式插入到 `style.css` 对应的页面结构段落内，未追加到文件末尾
- [ ] 取色使用 6.3 表中的 Primer 色值，未引入新色值
- [ ] 现代模式的规则带 `.live-preview` 作用域前缀
- [ ] 状态类与基础类组合使用；未用 `~` 兄弟选择器处理列表项
- [ ] 非显然的写法配了"为什么"注释

**注释（本项目重点）**

- [ ] 解释的是"为什么"而不是"做了什么"
- [ ] 踩过的坑、依赖的外部库行为、与其它文件的耦合约束已写明
- [ ] 多段落用 `// ---------------- 段落名 ----------------` 分隔

**文档同步**

- [ ] 新增/变更接口已更新 [API索引](./API索引.md)
- [ ] 数据/状态结构变更已更新 [数据结构](./数据结构.md)
- [ ] 用户可见行为变更已更新 `README.md` 与 `feature-todo.md`

---

## 相关文档

- [API索引](./API索引.md)
- [数据结构](./数据结构.md)
- [排障指南](./排障指南.md)
- [仓库概览](../仓库概览.md)
- [仓库架构](../仓库架构.md)
- [业务流程详解](../业务知识库/业务流程详解.md)
