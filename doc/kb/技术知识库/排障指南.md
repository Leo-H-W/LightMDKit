# 排障指南

> 本文档帮助开发人员快速定位、分析和解决 LightMDKit 运行过程中遇到的常见问题。

> **文档信息**：LightMDKit · 分支 `feat/sidebar-file-browser` · 提交 `f71e683` · 生成日期 2026-09-11

> **路径约定**：文中 `LightMDKit/xxx.js:NN` 表示项目下的相对路径与行号。`LightMDKit/` 是**逻辑项目前缀（项目名）**，不是磁盘上的目录 —— 仓库根目录本身就是项目根。`app.js` 指 `LightMDKit/public/app.js`，`style.css` 指 `LightMDKit/public/style.css`。

> **代码基线说明**：以下行号与 `?v=` 版本号取自 **2026-09-11 的工作区代码**，该状态已提交为 `f71e683`。若检出 `main` 的 `3bbca34`（不含侧边栏文件浏览器等功能），行号会有偏移（例如 `REAL_STRIKE_RE` 在 `3bbca34` 位于 `app.js:297`，工作区位于 `app.js:412`）。核对请读工作区文件。

---

## 0. 排障前必读：先确认运行形态

LightMDKit 有**三种运行形态**，同一现象在不同形态下根因完全不同（尤其是「改动不生效」「看不到日志」）：

| 形态 | 启动方式 | 前端资源来源 | 日志可见性 |
|------|---------|-------------|-----------|
| **源码模式** | `npm start` / `start.bat` / `start.sh`（内部执行 `node LightMDKit/server.js`，`LightMDKit/start.bat:28`、`LightMDKit/start.sh:20`） | 实时读取 `LightMDKit/public/` | 仅**前台**运行可见 |
| **打包 exe** | 双击 `LightMDKit/dist/lightmdkit.exe` | pkg 打包时内嵌的 `public/**/*` 快照（`LightMDKit/package.json:11-18`） | **完全不可见**（GUI 子系统 + 无控制台） |
| **守护进程** | 上述两种启动方式都会分离出一个后台进程（环境变量 `LIGHTMDKIT_DAEMON=1`） | 同左 | **不可见**（`spawn` 时 `stdio:'ignore'`，`LightMDKit/server.js:266-271`） |

> **排障第一原则**：默认启动方式是"父进程 fork 守护进程后退出"（`LightMDKit/server.js:299-305`），守护进程的 stdout/stderr 被丢弃（`stdio: 'ignore'`），项目**也没有写任何日志文件**。要看到错误输出，只能用前台模式启动：
>
> ```powershell
> # PowerShell（Windows）
> $env:LIGHTMDKIT_DAEMON='1'; node LightMDKit/server.js
> ```
> ```bash
> # Git Bash / macOS / Linux
> LIGHTMDKIT_DAEMON=1 node LightMDKit/server.js
> ```
> 该模式命中 `main()` 里的守护分支，直接执行 `runDaemon()`（`LightMDKit/server.js:292-295`）——不 fork、不自动打开浏览器，所有 `console.log/console.error` 都打在终端上。

---

## 1. 排障工具箱

| 工具名称 | 路径 | 描述 | 典型用法 |
|---------|------|------|---------|
| 前台启动（诊断核心手段） | `LightMDKit/server.js:292-295` | 以守护分支直接前台运行，唯一能看到服务端日志的方式 | `$env:LIGHTMDKIT_DAEMON='1'; node LightMDKit/server.js` |
| 启动脚本（Windows） | `LightMDKit/start.bat:9-28` | 检查 Node → 缺依赖则 `npm install` → `node server.js` | `start.bat` |
| 启动脚本（Unix） | `LightMDKit/start.sh:9-20` | 同上，Bash 版 | `./start.sh` |
| 停止脚本（Windows） | `LightMDKit/stop.bat:6-7` | `taskkill /F /IM lightmdkit.exe` + `taskkill /F /IM node.exe` | `stop.bat`（**注意：会杀掉机器上所有 node 进程**） |
| 停止脚本（Unix） | `LightMDKit/stop.sh:5-6` | `pkill -f "lightmdkit.exe"` / `pkill -f "node server.js"` | `./stop.sh` |
| GUI 子系统补丁 | `LightMDKit/scripts/set-windows-gui.js` | 改写 exe 的 PE 子系统字段，消除双击时的黑窗口 | `node LightMDKit/scripts/set-windows-gui.js dist/lightmdkit.exe` |
| 打包 | `LightMDKit/package.json:9` | `pkg . --output dist/lightmdkit.exe && node scripts/set-windows-gui.js` | `npm run build:win` |
| 端口/进程排查 | 系统命令 | 定位占用 3456 的进程 | `netstat -ano \| findstr :3456` + `tasklist /FI "PID eq <PID>"` |
| 服务连通性自测 | 系统命令 | 判断端口后面到底是不是 LightMDKit | `curl -s -o /dev/null -w "%{http_code}\n" http://localhost:3456/` |
| 后端接口自测 | `LightMDKit/server.js:97-224` | 绕过前端直接验证读文件链路 | `curl -s -X POST http://localhost:3456/api/load -H "Content-Type: application/json" -d '{"folderPath":"."}'` |
| 浏览器 DevTools | Console / Network / Application | 看前端异常、缓存命中、IndexedDB 拖放记录 | F12 → Console / Application → IndexedDB → `lightmdkit` → `drops` |
| 源码溯源 | `git log` / `git blame` | 定位"以前能用现在不能用"的改动 | `git log --oneline -5 -- LightMDKit/public/app.js` |

---

## 2. 日志系统

> **重要前提**：本项目**没有日志文件、没有日志框架、没有日志级别配置**。全部输出依赖 `console.*`，且默认启动路径（守护进程 / exe）会把它们丢弃。因此"看日志"= 用前台模式启动 + 浏览器 DevTools。

### 2.1 日志输出位置

| 场景 | 位置 | 备注 |
|------|------|------|
| 前台运行 `node LightMDKit/server.js` | 终端 stdout / stderr | **唯一完整的服务端日志通道**（`LightMDKit/server.js:20,74,282,285,301,307`） |
| 默认启动（`npm start` / `start.bat`） | 无 | 父进程打印 `LightMDKit started at ...` 后 `process.exit(0)`（`LightMDKit/server.js:301-304`）；子进程 `stdio:'ignore'`（`LightMDKit/server.js:268`），日志全部丢弃 |
| 双击 `dist/lightmdkit.exe` | 无 | GUI 子系统（`LightMDKit/scripts/set-windows-gui.js:37-50`）无控制台，且同样走守护进程，日志无处输出 |
| 浏览器端 | DevTools → Console | `console.warn` / `console.error`，见 2.2 |
| 用户可见提示 | 页面顶栏 `#status`（`LightMDKit/public/index.html:33`） | 由 `setStatus()` 写入，**3 秒后自动清空**（`app.js:153-163`），完整对照表见第 4 节 |
| 目录选择失败 | 服务端 stdout 的 `PowerShell stderr:`（`LightMDKit/server.js:74`） | 仅 Windows，`/api/select-folder` 的 PowerShell 报错 |
| `start.bat` / `stop.bat` | 命令行窗口 | 前缀 `[LightMDKit]`（`LightMDKit/start.bat:7,11,17,20,27`） |

### 2.2 关键日志标签

| 标签 / 消息 | 描述 | 示例值 | 适用场景 |
|------------|------|--------|----------|
| `[LightMDKit]` | 脚本层统一前缀 | `[LightMDKit] Starting server...` | 启动/停止脚本阶段 |
| `[LightMDKit] /api/select-folder requested` | 服务端收到目录对话框请求 | `LightMDKit/server.js:20` | 验证前端是否真的调了后端接口 |
| `PowerShell stderr:` | 目录对话框脚本失败原因 | `LightMDKit/server.js:74` | `/api/select-folder` 返回 500 时 |
| `Failed to open folder dialog` | PowerShell 退出码非 0（响应体） | `LightMDKit/server.js:75` | 同上 |
| `Folder dialog timed out` | 对话框 30s 未返回被强杀 | `LightMDKit/server.js:66-69` | 弹窗被其它窗口挡住/无人操作 |
| `Port 3456 is already in use.` | 守护进程内监听失败（端口占用） | `LightMDKit/server.js:282` | 服务起不来（仅前台模式可见） |
| `Server error: <msg>` | 其它监听错误 | `LightMDKit/server.js:285` | 权限/网卡异常 |
| `LightMDKit started at http://localhost:3456` | 父进程 fork 成功 | `LightMDKit/server.js:301` | 正常启动 |
| `端口 3456 已被占用，LightMDKit 可能已在运行。` | 父进程检测到端口占用 | `LightMDKit/server.js:307-308` | 重复启动（也可能被别的程序占用，见 3.1） |
| `[LightMDKit] 图片加载失败:` | 图片相对路径解析失败（附带 `rel` 与异常对象） | `app.js:711` | 图片不显示（3.8） |
| `CodeMirror/Mermaid init failed, falling back to textarea:` | 编辑器初始化失败（多为 CDN 未加载） | `app.js:149` | 现代模式不可用（3.13） |
| `Mermaid render error:` | 图表语法错误 | `app.js:662` | 图表不渲染 |
| 无标签的 `console.error(e)` | 各业务流程的异常对象 | `app.js:760, 784, 802, 841, 911, 1361, 1375, 1412` | 保存失败、加载失败、拖放失败等 |

### 2.3 采集日志的常用命令

```powershell
# PowerShell：前台启动并把输出同时落盘（便于回溯）
$env:LIGHTMDKIT_DAEMON='1'; node LightMDKit/server.js 2>&1 | Tee-Object -FilePath lightmdkit.log
```

```bash
# Git Bash：前台启动 + 只看错误
LIGHTMDKIT_DAEMON=1 node LightMDKit/server.js 2>&1 | grep -E "Port|Server error|stderr|失败|Error"
```

---

## 3. 常见故障诊断

### 3.0 现象 → 条目速查

| 现象 | 条目 |
|------|------|
| 浏览器打不开 / 提示端口占用 / 起了多份服务 | 3.1 |
| 双击 exe 弹出黑色命令行窗口 | 3.2 |
| 提示"浏览器不支持文件夹选择" | 3.3 |
| 选择目录后无反应 / 提示无 Markdown 文件 | 3.4 |
| **文件列表看不到子目录里的文件** | 3.5 |
| 改完内容没保存 / 自动保存不再触发 | 3.6 |
| 文档内链接点了没反应 | 3.7 |
| 图片裂图 | 3.8 |
| 现代模式下 `1~100` 显示异常 / 删除线 | 3.9 |
| 刷新后提示"当前文件已被移除" | 3.10 |
| 拖放没反应 / 新标签页被拦截 | 3.11 |
| 改了代码页面没变化 | 3.12 |
| 页面样式错乱 / `Cannot read properties of undefined` | 3.13（补充） |
| 子目录同名文件内容串位 | 3.14（补充） |

---

### 3.1 端口 3456 被占用 / 服务起不来 / 重复启动

**现象**
- 终端（前台模式）报 `Port 3456 is already in use.`；或父进程打印 `端口 3456 已被占用，LightMDKit 可能已在运行。`（`LightMDKit/server.js:282, 307`）
- 启动后浏览器打开的页面**不是** LightMDKit（被别人占用时）
- 双击 exe 毫无反应、也没有任何报错（GUI 子系统 + 日志被丢弃）
- 反复启动后打开多个标签页 / 出现多份服务进程

**可能原因**

| # | 原因 | 判断依据 |
|---|------|---------|
| 1 | 已有 LightMDKit 守护进程在运行（**正常行为**） | 端口被占时 `main()` 只打印提示、打开浏览器然后退出，不会重复启动服务（`LightMDKit/server.js:299-310`） |
| 2 | 3456 被**其它程序**占用 | `tasklist /FI "PID eq <PID>"` 显示的不是 `node.exe`/`lightmdkit.exe`；此时程序仍会打开浏览器，用户会误以为"已经跑起来了"（`LightMDKit/server.js:307-309` 未做身份校验） |
| 3 | 守护进程 fork 后启动失败 | 端口在 `isPortInUse()` 检查之后被抢占 → `EADDRINUSE` → `process.exit(1)`（`LightMDKit/server.js:280-288`），且该错误被 `stdio:'ignore'` 丢弃 |
| 4 | 启动竞态：浏览器先于服务就绪 | `spawnDaemon()` 后只 `sleep(2000)` 就 `openBrowser()`（`LightMDKit/server.js:302-303`）；首次运行 40MB 的 pkg exe 解包可能超过 2s → 页面提示"无法访问此网站" |
| 5 | 端口被安全软件/虚拟化层占用或本地监听被策略拦截 | 无监听但浏览器连不上；换端口可验证 |
| 6 | 残留进程未清理 | `stop.bat` 只按镜像名杀进程（`LightMDKit/stop.bat:6-7`）：以源码模式启动的守护进程是 `node.exe`，若与其它 Node 程序混在一起容易漏杀或误杀 |

**排查步骤**
1. 查端口占用：`netstat -ano | findstr :3456`（Unix：`lsof -i:3456`），记下 PID
2. 查 PID 身份：`tasklist /FI "PID eq <PID>"`（或 `wmic process where processid=<PID> get name,commandline`）
3. 确认端口后面是不是 LightMDKit：`curl -s -o /dev/null -w "%{http_code}\n" http://localhost:3456/`（正常返回 `200`），或直接看返回内容
4. **前台启动看真实报错**：`$env:LIGHTMDKIT_DAEMON='1'; node LightMDKit/server.js`
   - 打印 `Port 3456 is already in use.` 并在数秒内退出 → 端口确被占用（回到第 1 步）
   - 不打印任何错误且能访问 → 说明只是上次启动的竞态，刷新浏览器即可
5. 若是 exe 形态：改用源码模式复现（exe 无日志，见第 0 节）

**解决方案**
1. 已在运行：直接访问 <http://localhost:3456>，不要重复启动
2. 确实需要重启：Windows 下 `taskkill /F /IM lightmdkit.exe`，再用精确 PID 结束对应的 `node.exe`（**不要**直接 `taskkill /F /IM node.exe`，会误杀机器上其它 Node 程序）；Unix 用 `pkill -f "node server.js"`（`LightMDKit/stop.sh:6`）
3. 端口长期被别的程序占用：修改 `LightMDKit/server.js:11` 的 `PORT`（硬编码，无配置项、无环境变量覆盖）。改动后注意同步 README 与本文档中的 3456 引用
4. 首次启动/双击 exe 后连不上：手动 F5 刷新浏览器一次；仍失败则按第 4 步前台启动定位
5. **注意文档与实现的差异**：README 中"提示选择「重新运行」或「显示访问 URL」"的交互在当前 `main()` 中**不存在**（`LightMDKit/server.js:291-311` 只打印提示并打开浏览器），排查时不要按交互式提示去预期

**源码位置**：`LightMDKit/server.js:11`（PORT）、`226`、`227`（DAEMON_ENV）、`251-263`（`isPortInUse`）、`265-273`（`spawnDaemon`）、`275-289`（`runDaemon` 错误分支）、`291-311`（`main`）、`LightMDKit/stop.bat:6-7`、`LightMDKit/stop.sh:5-6`

---

### 3.2 双击 `lightmdkit.exe` 弹出命令行窗口

**现象**：双击 `LightMDKit/dist/lightmdkit.exe` 时弹出一个黑色控制台窗口；或反过来——服务像是起来了但看不到任何输出、出问题无从下手。

**可能原因**

| # | 原因 | 判断依据 |
|---|------|---------|
| 1 | exe 仍是 **CUI 子系统**（`pkg` 默认产出 `SUBSYSTEM_WINDOWS_CUI = 3`） | 补丁脚本检测到子系统为 3 时会改写为 GUI（`scripts/set-windows-gui.js:38, 45-50`）；已打过补丁会直接输出 `Already WINDOWS_GUI subsystem.`（`LightMDKit/scripts/set-windows-gui.js:40-43`） |
| 2 | 重新打包时**漏跑补丁** | `pkg` 产出新 exe 会覆盖已打补丁的文件；只有 `npm run build:win` 用 `&&` 串起了"打包 + 补丁"（`LightMDKit/package.json:9`），单独执行 `pkg .` 不会打补丁 |
| 3 | 补丁执行失败 | `EXE not found`（路径不对，脚本默认 `dist/lightmdkit.exe`，可用 `argv[2]` 指定，`LightMDKit/scripts/set-windows-gui.js:4-9`）；exe 正在运行导致 `fs.writeFileSync` 抛 `EBUSY/EPERM`（`LightMDKit/scripts/set-windows-gui.js:50`） |
| 4 | 打包产物不是合法 PE 文件 | `Invalid PE file: missing PE signature`（`LightMDKit/scripts/set-windows-gui.js:18-21`）或 `Unexpected optional header magic`（`LightMDKit/scripts/set-windows-gui.js:28-31`）→ 说明 pkg 打包失败或文件被截断 |

**排查步骤**
1. 直接运行补丁脚本看输出：`node LightMDKit/scripts/set-windows-gui.js dist/lightmdkit.exe`
2. 输出 `Changed subsystem to WINDOWS_GUI` → 之前确实没打补丁
3. 输出 `Already WINDOWS_GUI subsystem.` → 补丁已生效，若仍弹黑窗，确认双击的是不是同一个 exe（例如发布包里还有一份旧的）
4. 报 `EXE not found` / `Invalid PE file` → 先修复打包流程（`npm run build:win`）
5. 报 `EBUSY`/`EPERM` → 先完全退出正在运行的 LightMDKit（见 3.1 停止方式）再执行

**解决方案**
1. 统一用 `npm run build:win` 打包（`LightMDKit/package.json:9`），不要手动只跑 `pkg`
2. 打包完成后确认补丁输出为 `Changed subsystem to WINDOWS_GUI: <path>`（`LightMDKit/scripts/set-windows-gui.js:52`）
3. **理解代价**：切到 GUI 子系统后没有控制台，`console.log/console.error` 全部无处输出，加上守护进程 `stdio:'ignore'`（`LightMDKit/server.js:266-271`），exe 形态**完全没有日志**。任何"服务起不来/行为异常"的排查都必须切回源码模式前台启动（第 0 节）
4. 若被安全软件回滚过 exe，需要重新打补丁并加白名单

**源码位置**：`LightMDKit/scripts/set-windows-gui.js:4, 6-9, 14, 18-21, 28-31, 34-50, 52`、`LightMDKit/package.json:9`

---

### 3.3 浏览器不支持文件夹选择（`showDirectoryPicker` 缺失）

**现象**：点击左侧"📁 加载文件夹..."后，状态栏红字提示 `浏览器不支持文件夹选择，请使用 Chrome 或 Edge`（`app.js:748`），不弹出任何对话框。

**可能原因**

| # | 原因 | 判断依据 |
|---|------|---------|
| 1 | 非 Chromium 内核浏览器（Firefox / Safari） | `window.showDirectoryPicker` 为 `undefined` → 命中前置检查（`app.js:747-750`） |
| 2 | **非安全上下文**：用 `http://<局域网IP>:3456` 或 `http://<主机名>:3456` 访问 | File System Access API 只在 secure context 暴露；`localhost`/`127.0.0.1` 被浏览器视为安全，局域网 IP **不是** → 同样为 `undefined`。Console 执行 `[isSecureContext, typeof window.showDirectoryPicker]` 可确认 |
| 3 | Chromium 版本过旧（File System Access API 需 Chrome/Edge 86+） | Console 检查 `navigator.userAgent` |
| 4 | 内嵌 WebView / 企业策略禁用该 API | Console 中该函数缺失，且不受浏览器版本影响 |

**排查步骤**
1. Console 执行：`[isSecureContext, typeof window.showDirectoryPicker, location.origin]`
2. `location.origin` 不是 `http://localhost:3456` → 基本可判定为原因 2
3. `isSecureContext` 为 `false` → 原因 2；为 `true` 但仍无该函数 → 原因 1/3/4
4. 确认页面是由程序自动打开的（`openBrowser()` 打开的是 `http://localhost:3456`，`LightMDKit/server.js:226, 233-249, 301, 309`），排除用户手工输入了 IP

**解决方案**
1. 使用 Chrome / Edge 打开 <http://localhost:3456>（**不要**用局域网 IP、机器名访问）
2. 需要在别的机器上访问时，当前代码不含 HTTPS 支持，需自行加 HTTPS 反向代理（非安全上下文问题无法通过配置绕过）
3. 服务端虽然实现了 Windows 专用的 `POST /api/select-folder`（`LightMDKit/server.js:19-94`），但**前端没有把它作为降级回退**：`selectFolder()` 只依赖 `showDirectoryPicker`（`app.js:746-765`）。在必须用非 Chromium 浏览器的场景，只能另行调用该接口（详见 [API索引](./API索引.md) 与 [外部接入指南](../外部接入指南.md)）
4. 前端崩溃保护：即使编辑器初始化失败也会回退到 textarea，不会白屏（`app.js:99-151`）

**源码位置**：`LightMDKit/public/app.js:759-763`、`LightMDKit/server.js:19-94`、`LightMDKit/public/index.html:89-97`（第三方脚本）

---

### 3.4 用户取消文件夹选择（`AbortError`）与"目录下无 Markdown 文件"

**现象**
- 在系统对话框点"取消"：状态栏短暂显示 `已取消`（`app.js:757-758`）——**这是正常行为，不是故障**
- 选定目录后状态栏显示 `目录下无 Markdown 文件`（`app.js:733`），内容区显示"该目录下没有找到 Markdown 文件"（`app.js:732`）

**可能原因**

| # | 原因 | 判断依据 |
|---|------|---------|
| 1 | 用户取消（`AbortError`） | `selectFolder()` 显式区分：`e.name === 'AbortError'` → `已取消`；其它异常走 `setStatus(e.message, 'error')`（`app.js:756-762`） |
| 2 | 目录（含递归子目录）里确实没有 `.md`/`.markdown` | 文件计数 `#file-count` 为 0（`app.js:517-520`）；扩展名判定见 `app.js:165-168`（大小写不敏感，`.MD` 可用） |
| 3 | 文件都在**被跳过的目录**里 | `SCAN_SKIP_DIRS = { node_modules, .git, .svn, .hg, .idea, .vscode, dist, build }`（`app.js:84-86, 233-235`）——例如文档放在 `dist/` 下 |
| 4 | 文件位于第 7 层及更深的子目录 / 总数超过 3000 | 见 3.5（`SCAN_MAX_DEPTH=6`、`SCAN_MAX_FILES=3000`，`app.js:81-82, 221, 231`） |
| 5 | 选择的目录不可读（权限、OneDrive/网盘占位文件、受控文件夹访问） | `dirHandle.entries()` 抛异常，`e.message` 被写到状态栏（`app.js:756-762`） |
| 6 | 选择的路径被系统对话框当作"文件"或无效路径 | 前端不复核路径，直接 `loadFolderHandle` |

**排查步骤**
1. 先看空态文案区分处境：`当前目录下没有找到 Markdown 文件`（已加载目录但为空，`app.js:543-545`）vs `暂无内容，请先加载文件夹`（从未加载过，`app.js:545`）
2. 用资源管理器确认目录内容与扩展名（是否 `.md` 被存成 `.txt`、是否文件名是 `xxx.md.bak`）
3. Console 复现并看异常：`await window.showDirectoryPicker({mode:'readwrite'})`，观察抛出的 `name`（`AbortError` / `NotAllowedError` / `SecurityError`）
4. 若目录很大：确认目标文件深度是否 ≤ 6、总数是否可能超过 3000（见 3.5）
5. 点一次「刷新」（`app.js:789-844`）确认不是"加载后未重扫"

**解决方案**
1. 取消对话框属预期行为，无需处理；`updateLoadFolderButton()` 会把按钮文案恢复（`app.js:763, 509-515`）
2. 目录为空：换一个包含 md 的目录；或把文档移出 `dist`/`build`/`node_modules` 等被跳过目录
3. 扩展名问题：把文件改回 `.md` / `.markdown`
4. 权限问题：换一个当前用户可读写的目录；OneDrive 未下载到本地的"仅在线"文件也可能读取失败
5. 需要展示深层文档：加载更深一层的目录作为根（见 3.5 解决方案）

**源码位置**：`LightMDKit/public/app.js:85-90, 165-168, 233-238, 509-520, 539-548, 718-743, 746-765`

---

### 3.5 文件列表里看不到子目录中的文件

**现象**：某个子目录里的 `.md` 没有出现在左侧文件列表；点「刷新」后仍然看不到；`#file-count` 数量比预期少。

**可能原因**

| # | 原因 | 判断依据 / 限制值 |
|---|------|------------------|
| 1 | **深度超限**：`SCAN_MAX_DEPTH = 6`，`collectMarkdownFiles()` 在 `depth > 6` 时直接返回，不再深入 | `app.js:81, 221`。根为第 0 层，最多深入到第 6 层子目录 |
| 2 | **数量超限**：`SCAN_MAX_FILES = 3000`，达到上限后停止收集 | `app.js:82, 221, 231`。收集顺序 = 每层按名称升序（`app.js:228`）后按目录逐层铺平（`flattenByDirectory`，`app.js:193-215`），超出部分被**静默截断**，不报错 |
| 3 | **目录被跳过**：`SCAN_SKIP_DIRS` 里的目录名一律不下钻 | `app.js:84-86, 233-235`：`node_modules`、`.git`、`.svn`、`.hg`、`.idea`、`.vscode`、`dist`、`build`（本项目自带 `dist/`，若把文档放进去就会"消失"） |
| 4 | **扩展名不符**：只收集 `.md` / `.markdown` | `app.js:165-168, 236-238`（`.MD` 大写可用，`.md.txt` 不行） |
| 5 | 未重新扫描 | 新增文件后需点「刷新」（会重扫，`app.js:810`）或重新加载文件夹（`app.js:724`） |
| 6 | 过滤框有残留关键字 | 结果为空时提示 `没有匹配的文件`（`app.js:555-561`）；按 Esc 清空（`app.js:1150-1156`） |
| 7 | 符号链接 / 目录联接（junction）指向外部目录 | Chromium 对目录句柄的符号链接支持不一致（**需进一步确认**） |

**排查步骤**
1. 先排除最易误判的一条：清空过滤框（Esc），看 `#file-count` 是否变化
2. 数一数缺失文件的**相对深度**：`a/b/c/d/e/f/x.md` 是第 6 层（可见），再深一层就不可见
3. 查路径中是否有目录名命中跳过列表（尤其 `dist`、`build`）
4. 若目录很大，怀疑 3000 上限：把目标子目录单独作为根加载一次，看文件是否出现（出现即命中数量/深度限制）
5. 用 Console 交叉验证：直接在 Console 里对同一句柄做一次递归列举，与页面列表对比

**平台/来源差异（重要，容易张冠李戴）**

| 来源 | 扫描实现 | 是否递归 |
|------|---------|---------|
| 前端文件列表（浏览器选目录 / 拖入文件夹 / 刷新） | `collectMarkdownFiles()`（`app.js:220-244`） | **递归**，但受 6 层 / 3000 个 / 跳过规则限制 |
| 服务端 `POST /api/load` | `fs.readdirSync()` 单层过滤（`LightMDKit/server.js:118-127`） | **不递归**：只返回目录**顶层**的 `.md`，子目录文件一律不返回 |
| 服务端 `GET /api/file`、`POST /api/refresh` | 按指定路径读单文件（`LightMDKit/server.js:171-224`） | 与目录扫描无关 |

**解决方案**
1. 把文档目录挪浅（≤ 6 层）或直接加载更深一层的目录作为根（根变了，相对深度自然变小）
2. 大仓库：把文档目录单独加载，避开 3000 上限；或临时把 `SCAN_MAX_FILES` 调大（`app.js:82`，代价是扫描变慢、界面卡顿）
3. 把文档移出 `dist` / `build` / `node_modules` 等跳过目录
4. 调用服务端 `/api/load` 的程序要注意它**不递归**（`LightMDKit/server.js:118-127`），需要子目录文件请自行遍历或用前端
5. 刷新后新增文件会有提示：`目录已刷新，发现 N 个新文件`（`app.js:834-838`）

**源码位置**：`LightMDKit/public/app.js:85-90, 193-244（核心 220-244）`、`LightMDKit/server.js:118-127`

---

### 3.6 编辑后内容没保存 / 自动保存失效

**现象**
- 编辑后切回浏览或刷新，内容回到修改前；状态栏出现 `保存失败: xxx`（`app.js:912`）
- 现代模式下编辑几秒后内容"变回去"，或之后不再自动保存
- 状态栏出现 `当前文件来源不支持写回保存`（`app.js:900-902`）

**可能原因**

| # | 原因 | 判断依据 |
|---|------|---------|
| 1 | **来源不可写（拖入的单个文件）**：`initFromDropParam` 在没有目录句柄时用 `{ getFile: ... }` 伪造句柄（`app.js:1395-1397`），该对象没有 `createWritable` → `saveCurrentFile` 直接抛错（`app.js:899-903`） | 状态栏报 `当前文件来源不支持写回保存`（句柄无 `createWritable`） |
| 2 | **权限不足**：`createWritable()` 抛 `NotAllowedError`（用户拒绝写权限、目录只读、文件带只读属性、被其它程序独占） | `app.js:905-913` 捕获后 `保存失败: <message>`；目录选择时用的是 `mode:'readwrite'`（`app.js:753`），但权限仍可能被用户在弹窗中拒绝 |
| 3 | **自动保存静默停止（最隐蔽）**：定时器每 3s 触发一次（`AUTOSAVE_INTERVAL_MS = 3000`，`app.js:89, 917-928`），**首次失败即 `stopAutosave()`**（`app.js:922-926`）；若提示在 3 秒内被自动清空（`setStatus` 的定时清理，`app.js:157-162`），用户可能什么都没看到，之后再也不保存 | 状态栏 `保存失败: xxx` 只闪现 3 秒，之后不再出现任何保存动作 |
| 4 | **自动保存从未启动**：定时器只在进入编辑态时启动（传统模式 `app.js:1060`、现代模式 `app.js:1003`、启动时若偏好为现代模式 `app.js:1449-1452`）；回调开头还有 `if (!isEditMode) return` 守卫（`app.js:920`） | 磁盘文件 mtime 长时间不变，且全程没出现过 `已保存` |
| 5 | 现代模式因 CodeMirror 未初始化而被拒 | `编辑器未就绪，无法进入现代模式`（`app.js:979-982`）；若 localStorage 存的是 `modern` 但 CodeMirror 失败，启动时会静默退回传统模式（`app.js:1449-1456`） |
| 6 | **刷新/切换文件覆盖了未落盘的改动** | `renderFile()` 在编辑态无条件 `cmEditor.setValue(currentMarkdownText)`（`app.js:630-647`）→ 编辑器内容被磁盘内容顶掉 |
| 7 | 保存到了另一个同名文件 | 见 3.14 |

**排查步骤**
1. 看状态栏与 Console：保存失败路径会先 `console.error(e)`（`app.js:911`），Console 里能看到完整异常名（`NotAllowedError` / `TypeError` 等）
2. 判断文件来源：看顶栏 `#folder-label`——单文件拖入时其 `title` 为"浏览器安全限制，无法自动定位到文件所在目录"（`app.js:1405`），说明没有目录句柄
3. 触发一次**非静默**保存以便看到提示：编辑 → 点「浏览」（`app.js:1027-1035` 调用 `saveCurrentFile()`）
4. 到磁盘核对文件修改时间是否变化（`Get-Item <file> | Select LastWriteTime`）
5. 自动保存是否仍活着：修改内容后等 5 秒，再观察状态栏/磁盘；若首次失败过一次即被停掉，需重新进入编辑态才会重启定时器

**解决方案**
1. 拖入的单文件：改用「📁 加载文件夹」选择其所在目录后打开（句柄来自 `showDirectoryPicker({mode:'readwrite'})`，`app.js:753`，具备写能力）
2. 权限问题：重新选择文件夹并在浏览器权限提示中允许写入；检查文件只读属性、是否被 Typora/Word/同步盘占用
3. 自动保存停了：切一次编辑态（浏览 → 编辑，或传统 ↔ 现代）会重新 `startAutosave()`（`app.js:1060, 1003`），并先按第 1 步定位首次失败原因
4. 重要内容不要只依赖 3 秒自动保存：用「浏览」按钮触发一次显式保存并确认状态栏 `已保存`（`app.js:909）
5. 编辑中不要按「刷新」——它会用磁盘内容覆盖编辑器（`app.js:630-647`、`789-844`）

**源码位置**：`LightMDKit/public/app.js:92-93, 894-935（保存与自动保存）、1003, 1027-1035, 1060-1061, 1395-1409, 1449-1457`

---

### 3.7 文档内相对链接点击无反应

**现象**（三种表现，根因不同）
- a. 点了完全没反应，地址栏也没有变化
- b. 页面跳到了一个 404 / 空白页
- c. `#锚点` 链接点了不滚动

**可能原因**

| # | 原因 | 判断依据 |
|---|------|---------|
| 1 | **目标不在 `currentFiles` 中**：`findEntryByPath()` 两次匹配都失败（`app.js:865-872`）→ 不调用 `preventDefault()`，交给浏览器 → 浏览器按页面地址解析成 `http://localhost:3456/xxx.md` → Express 静态目录里没有 → **404**（表现 b） | 地址栏跳到 `http://localhost:3456/xxx.md` 并返回 404（请求落到了 Express 静态目录） |
| 2 | **跨目录 / 上级目录**：`../other/a.md` 中的 `..` 不会被归一化——`normalizePathForMatch()` 只做 `\`→`/`、去掉开头 `./`、转小写（`app.js:172-174, 865`）→ 匹配必然失败 | 目标文件能在左侧文件列表中找到，但该链接点击无效 |
| 3 | **锚点链接**：`#xxx` 被显式跳过交给浏览器原生跳转（`app.js:861`）；但 **marked 12 不生成标题 id**（实测 `# 标题` → `<h1>标题</h1>`，`node_modules/marked/lib/marked.cjs` 中已无 `headerIds` 相关实现），标题 id 只在侧边栏切到「目录」视图时由 `_renderTocCurrentView()` 补上（`app.js:463-466`）；而 `renderToc` 被视图守卫包住（`if (sidebarView !== 'toc') return`，`app.js:1441-1446`）→ **默认「文件列表」视图下锚点必然不跳**（表现 a/c） | 把侧边栏切到「目录」视图后，同一个锚点链接可以跳转 |
| 4 | 目标是非 md 文件（图片、pdf、txt）：`findEntryByPath` 只在 `currentFiles`（仅含 md）里找 → 未命中时 `loadFile()` **静默 return，不给任何提示**（`app.js:769-770`）（表现 a） | 点击后状态栏没有任何提示（`loadFile()` 静默返回） |
| 5 | 外部链接 / `mailto:` 被跳过，由浏览器接管（`app.js:861`） | 属正常行为 |
| 6 | 现代模式下链接是编辑器里的**源码文本**，点击不会跳转（`contentEl` 被隐藏，`app.js:940-944`；链接拦截只挂在 `contentEl` 上，`app.js:855`） | 设计如此，切回传统模式即可点击 |
| 7 | href 含 `#` 或 `?`：查询串与锚点会被截断（`app.js:865` 的 `href.split(/[?#]/)[0]`）→ 目标名可能因此对不上 | — |

**排查步骤**
1. DevTools → Elements 找到该 `<a>`，读出真实 `href`
2. 点击后看状态栏：**完全没有提示** → 命中 `loadFile()` 的静默返回（`app.js:769-770`）；出现 `读取文件...` 后报错 → 是读取阶段失败（走 `app.js:783-786`）
3. 看地址栏是否变成 `http://localhost:3456/xxx.md` → 说明未被拦截，走了浏览器默认跳转（原因 1/2）
4. 核对目标能否在左侧文件列表里找到（匹配规则：大小写不敏感、`\` 与 `/` 等价、可省 `./`，`app.js:172-174`）
5. 若是锚点：把侧边栏切到「目录」视图再点一次——能跳即命中原因 3

**解决方案**
1. 链接写成**相对于当前文件**的同目录 / 子目录路径（`./a.md`、`sub/a.md`），不要用 `../`（当前实现不解析上级目录）
2. 目标不在已加载目录内：先「加载文件夹」把包含目标的目录设为根，或直接从文件列表打开
3. 锚点：切到「目录」视图让标题补上 id（临时绕过）；根治需在渲染后统一生成 heading id（当前代码只在目录视图下做，属功能缺口）
4. 需要支持 `../`、跨目录跳转、非 md 文件时，属功能增强而非配置问题（改动点：`app.js:172-174` 的归一化与 `855-881` 的点击处理）
5. 现代模式下点击链接无效属预期，切换回传统模式浏览

**源码位置**：`LightMDKit/public/app.js:176-190（路径匹配）、767-787（loadFile 静默返回）、855-881（链接拦截）、940-944、1441-1446（目录视图守卫）、463-466（heading id 生成）`、`LightMDKit/public/md-render.js:166-169`

---

### 3.8 图片不显示

**现象**：`![x](./img/a.png)` 渲染成裂图/占位图；Console 出现 `[LightMDKit] 图片加载失败: <rel> <err>`（`app.js:711`）。

**可能原因**

| # | 原因 | 判断依据 |
|---|------|---------|
| 1 | **没有目录句柄**：`resolveImages()` 第一行 `if (!currentFolderHandle) return;`（`app.js:676`）→ 拖入单个文件的场景**一张图片都不解析** | `#folder-label` 的 title 显示「浏览器安全限制，无法自动定位到文件所在目录」 |
| 2 | **图片不在已加载目录内**：`../img/a.png` 会被拆成 `["..","img","a.png"]` 后逐级 `getDirectoryHandle`（`app.js:694-702`），`..` 必然失败 → 抛错被捕获 → 保留原 `src` → 裂图（`app.js:709-712`） | Console 报 `[LightMDKit] 图片加载失败: ../img/a.png NotFoundError` |
| 3 | 绝对路径（`C:\x\a.png`、`file:///...`）不被跳过白名单覆盖（白名单只有 `http(s):`、`data:`、`blob:`、`#`、`//`，`app.js:680`）→ 被当成相对路径解析 → 找不到 | DevTools 里该 `img.src` 仍是 `C:\...` 原值，也没有发出 blob 请求 |
| 4 | 大小写不一致或含特殊字符：`%20` 会被 `decodeURIComponent`（`app.js:685-688`），非法编码则保持原样 → 可能无法命中文件 | Network 面板中请求的文件名与磁盘上的实际大小写/字符不一致 |
| 5 | 现代模式下不解析图片（显式跳过，`app.js:626-628`）：现代模式没有可见的渲染面，图片以源码形式留在编辑器中；切回传统模式会重新解析（`app.js:1010`） | 切回传统模式后同一张图片能正常显示 |
| 6 | **图片缓存未失效**：`imageUrlCache` 只在 `loadFolderHandle()` 时清空（`app.js:719` 调用 `clearImageCache`，`app.js:666-671`）；「刷新」路径 `refreshCurrentFile()` **没有清缓存**（`app.js:789-844`）→ 磁盘上替换了图片，页面仍显示旧图 | 磁盘上已替换图片，但页面仍显示旧图；重新「加载文件夹」后即正常 |
| 7 | 在没有目录句柄时，浏览器按页面地址请求 `http://localhost:3456/a.png` → 404 | DevTools → Network 可见 |

**排查步骤**
1. Console 找 `[LightMDKit] 图片加载失败:`，其中的 `rel` 就是程序尝试解析的相对路径（`app.js:711`）
2. DevTools → Elements 检查 `<img>` 的 `src`：
   - 仍是相对路径 → 解析失败，保留了原值（`app.js:709-712`）
   - 是 `blob:` → 解析成功，问题在别处（CSS、尺寸、图片本身损坏）
3. 确认当前是否为"拖入单文件"模式（`#folder-label` 的 `title`，`app.js:1405`）
4. 确认图片相对深度（支持多级如 `img/sub/a.png`，`app.js:694-703`）

**解决方案**
1. 用「加载文件夹」加载包含图片的目录，图片路径相对**该目录根**书写
2. 不要用 `../`，把图片放进已加载目录树内
3. 图片在磁盘上被替换后要看最新效果：重新「加载文件夹」（触发 `clearImageCache`，`app.js:719`）或关闭再打开页面
4. 现代模式下看不到图片属设计如此，切回传统模式查看（`app.js:1010`）

**源码位置**：`LightMDKit/public/app.js:99-101, 626-628, 666-671, 675-714（核心）、719, 789-844（未清缓存）`

---

### 3.9 现代模式下 `1~100` 显示异常 / 删除线问题

**现象**（两层，需分别定位）
- **预览 / 传统模式**：`范围 1~100、2~5` 被渲染成 `范围 1<del>100、2</del>5`（历史缺陷，见 `feature-todo.md` 第 5 条，已在 `3bbca34` 修复）
- **现代模式编辑器**：范围里的两个 `~` 及其之间的文字被加上删除线、甚至两个 `~` 被隐藏，显示成 `1100、25`

**机制与现状（理解后可做回归验证）**

| 层 | 机制 | 位置 |
|----|------|------|
| 预览渲染 | marked 12 的默认 `del` 规则是 `^(~~?)`——**单个 `~` 也算定界符**（`node_modules/marked/lib/marked.cjs:1170`）。项目用 `patchStrikethrough()` 覆盖 `tokenizer.del`，只认 `~~`；不匹配时返回 `undefined`（**不会**回退到内置规则，字符按普通文本输出，`md-render.js:143-164`） | `md-render.js:134-169` |
| 编辑器 | CM5 的 markdown mode 把单个 `~` 当删除线 → 构造 CodeMirror 时显式关闭 `strikethrough: false`（`app.js:111`）；真正的 `~~…~~` 由 `refreshStrikeMarks()` 用 `markText` 标注 `cm-md-strike` / `cm-md-strike-mark`（`app.js:420-444`），样式在 `style.css:699-717` | `app.js:111, 420-444`（`mode.strikethrough:false` + `markText` 标注） |
| 一致性 | 两处正则形状一致：`REAL_STRIKE_RE`（`app.js:412`）与 `DOUBLE_TILDE_DEL`（`md-render.js:140`） | `app.js:412`、`md-render.js:140`（两处正则形状一致） |

**实测结果**（用项目自带 `renderMarkdown` + marked 12.0.2 跑出，可作为回归基线）

| 输入 | 输出 | 结论 |
|------|------|------|
| `范围 1~100、2~5 与 3~7 之间` | `<p>范围 1~100、2~5 与 3~7 之间</p>` | 正常，不再被渲染成删除线 |
| `这是 ~~删除~~ 文字` | `这是 <del>删除</del> 文字` | 正常 |
| `这是 ~~~三个~~~ 波浪线` | 原样输出 | 定界符紧邻 `~` 不满足 `[^\s~]` 条件 |
| `~~ a ~~` | 原样输出 | 定界符内侧有空格不构成删除线 |
| `~~这里是\n跨行的删除线~~` | `<del>这里是<br>跨行的删除线</del>` | **跨行仍会成对**，而编辑器 `refreshStrikeMarks()` 按行遍历（`app.js:424-443`）不会标注 → 编辑器与预览**表现不一致**（已知差异） |
| 表格内 `1~2` / `~~x~~` | `1~2` 正常 / `<del>x</del>` | 正常 |

**可能原因（出现异常时）**

| # | 原因 | 判断依据 |
|---|------|---------|
| 1 | `md-render.js` 补丁未生效 | 补丁依赖 `marked.use`，且用 `WeakSet` 去重（`md-render.js:141-147`）；`index.html:95` 引用的是浮动主版本 `marked@12`，CDN 版本变化可能影响规则 |
| 2 | 浏览器缓存了旧脚本（`index.html:96` 的 `?v=4` 未升） | 见 3.12 |
| 3 | 现代模式样式未生效（`index.html:19` 的 `?v=17` 未升，或容器缺 `live-preview` 类） | `live-preview` 由 `applySurface()` 添加（`app.js:949`），样式规则在 `style.css:609-717` |
| 4 | CodeMirror 未初始化，回退 textarea → 现代模式即时渲染整体不可用 | `app.js:148-151`、`979-982` |

**排查步骤**
1. 在页面 Console 里直接验证渲染层：`MdRender.renderMarkdown('范围 1~100、2~5', marked)`，对比实测表
2. DevTools → Network 确认 `md-render.js?v=4`、`style.css?v=17` 加载的是最新内容（`index.html:19, 96`）
3. 现代模式下选中范围内文字，用 Elements 面板确认是否带 `cm-md-strike` 类 → 带类说明是标注/CSS 问题，不带类说明是 CM5 mode 配置问题
4. 光标移到该行：现代模式会还原源码（`.CodeMirror-activeline` 规则，`style.css:665-676`），可确认实际字符

**解决方案**
1. 渲染层异常：确认 `md-render.js` 已更新（`renderMarkdown` 内部会先执行 `patchStrikethrough`，`md-render.js:166-169`），并升级 `?v=4`
2. 样式层异常：升级 `index.html:19` 的 `?v=17` 后硬刷新（Ctrl+F5）
3. 跨行 `~~` 的不一致是已知限制；若要完全对齐需把 `refreshStrikeMarks()` 改为跨行扫描（**需进一步确认**实现方式与性能影响）

**源码位置**：`LightMDKit/public/md-render.js:134-169`、`LightMDKit/public/app.js:110-115, 410-444, 949`、`LightMDKit/public/style.css:695-717`、`LightMDKit/public/index.html:19, 96`、`LightMDKit/feature-todo.md`（第 5 条）

---

### 3.10 刷新后当前文件"已被移除"

**现象**：点「刷新」后内容区被清空并显示"当前文件已被移除"，状态栏红字 `当前文件已被移除`（`app.js:820-824`）。

**可能原因**

| # | 原因 | 判断依据 |
|---|------|---------|
| 1 | 文件真的被删除、重命名或移动 | 资源管理器确认 |
| 2 | **文件被移出扫描范围**：深度 > 6、命中 `SCAN_SKIP_DIRS`、或总数超 3000 → 新扫描结果里没有它，于是被判为"已移除"（判定逻辑 `files.some(f => f.path === currentFile)`，`app.js:815`） | 见 3.5；`#file-count` 会明显变化 |
| 3 | 文件扩展名被改（`.md` → `.md.bak`、`.md.txt`） | `app.js:165-168, 236-238` |
| 4 | 刷新路径不对：拖入单文件时没有目录句柄，走的是另一分支（只重读当前文件，不会出现该提示） | `app.js:794-806`，此时状态栏显示 `已刷新` |
| 5 | 扫描过程抛错（目录被删除、权限变化、网盘目录掉线） | 走 catch → 状态栏显示 `e.message`（`app.js:840-843`） |
| 6 | 内容串位（子目录同名文件），表现为"内容对不上"而非本提示 | 见 3.14 |

**排查步骤**
1. 确认磁盘上文件是否还在、路径是否变化
2. 看左侧 `#file-count`：数量骤降 → 扫描被截断或跳目录（原因 2）
3. Console 观察是否有扫描异常；
4. 把目标目录单独作为根重新加载，若文件出现 → 确认是扫描范围问题

**解决方案**
1. 文件被移走：重新「加载文件夹」到新位置，或把它放回原目录
2. 扫描范围问题：按 3.5 处理（移浅 / 移出跳过目录 / 缩小目录规模）
3. **重要提醒**：该提示会同时清空内容区（`showEmptyState('当前文件已被移除')`，`app.js:821`），但不代表磁盘内容丢失——重新加载目录即可恢复
4. 注意刷新会重扫整个目录（`app.js:810`，受 6 层/3000 限制），大型目录下刷新有明显延迟属正常

**源码位置**：`LightMDKit/public/app.js:802-857（判定 815、提示 820-824）、220-244、610-619`

---

### 3.11 拖放打开失败 / 新标签页被拦截

**现象**
- 拖拽时有"松开鼠标，在新标签页打开…"蒙层（`app.js:1336-1352`），但松手后没有新标签页
- 状态栏出现"新标签页被拦截，请 点击打开"（`app.js:1271-1280`）
- 状态栏 `拖放打开失败: xxx`（`app.js:1362`）、`打开拖入内容失败: xxx`（`app.js:1413`）
- 状态栏 `仅支持拖入 .md / .markdown 文件或文件夹`（`app.js:1331`）
- 内容区提示 `拖放数据不存在或已过期，请重新拖入文件`（`app.js:1378`）

**可能原因**

| # | 原因 | 判断依据 |
|---|------|---------|
| 1 | **弹窗被拦截（最常见）**：`openDropInNewTab()` 在 `await idbPut(...)` 之后才调用 `window.open`（`app.js:1287-1289`），异步等待已脱离用户手势上下文 → 浏览器弹窗拦截器拦截；代码对此有兜底链接，但需要用户手动点（`app.js:1271-1280`） | 状态栏出现「新标签页被拦截，请 点击打开」并带一个兜底链接 |
| 2 | 拖入内容不是文件（如选中文本、页面内链接） | `hasDraggedFiles()` 要求 `dataTransfer.types` 包含 `Files`（`app.js:1254-1256`） |
| 3 | 拖入的不是 `.md`/`.markdown`（txt、图片、zip） | `isMarkdownName()` 不通过 → 不打开（`app.js:1314`），最终提示 1331 |
| 4 | 浏览器不支持 `DataTransferItem.getAsFileSystemHandle()`（Firefox/Safari） | 回退到 `item.getAsFile()`，走 Blob 分支（`app.js:1300-1313, 1318-1324`）：**能打开，但拿不到所在目录** |
| 5 | IndexedDB 不可用（隐私模式、禁用存储、配额） | `idbPut` 抛错（`app.js:1213-1221`）→ 被 drop 处理捕获 → `拖放打开失败: ...`；注意 `Promise.all(tasks)`（`app.js:1329`）会让任一子任务失败拖垮整批 |
| 6 | 新标签页读不到记录：记录已过期（`DROP_TTL_MS = 24h`，`app.js:1198, 1233-1252`）或被浏览器清理 | 提示 `拖放数据不存在或已过期，请重新拖入文件`（`app.js:1377-1380`） |
| 7 | 记录里的目录句柄权限失效（浏览器重启后句柄权限通常不持久） | 新标签页 `loadFolderHandle()` 时 `dirHandle.entries()` 抛错 → `打开拖入内容失败: ...`（`app.js:1411-1414`）；不同浏览器行为不一致（**需进一步确认**） |
| 8 | 拖入的是快捷方式（`.lnk`）或压缩包内的虚拟条目 | 拿不到有效句柄 |

**排查步骤**
1. 状态栏文案对号入座；提示"被拦截"时点状态栏里的「点击打开」链接（`app.js:1274-1279`）
2. 允许站点弹窗：地址栏右侧弹窗拦截图标 → 始终允许 `http://localhost:3456`
3. DevTools → Application → IndexedDB → `lightmdkit` → `drops`：确认记录是否写入成功（`app.js:1196-1197, 1213-1221`）
4. DevTools → Console 看异常：`console.error` 出现在 `app.js:1361`（拖放整体失败）、`1375`（读取记录失败）、`1412`（打开拖入内容失败）
5. 最小化复现：只拖 1 个 `.md` 文件；若单文件可以、文件夹不行 → 属目录句柄/权限问题（原因 7）

**解决方案**
1. 弹窗被拦截：允许本站弹窗；或使用状态栏的兜底链接；或先单击页面再进行拖放（保留手势上下文）
2. 拖入单文件后"未定位所在目录"是浏览器安全限制（拿不到父目录，`app.js:1393-1409`）：改用「加载文件夹」把该目录加载进来，即可获得完整文件列表与可写句柄
3. IndexedDB 不可用：用普通窗口（非隐私模式）重试
4. 也可以把文件先复制进已加载目录，再点「刷新」发现它（`app.js:810, 834-838`）

**源码位置**：`LightMDKit/public/app.js:1213-1355（关键 1282-1291）、1335-1364、1367-1415`

---

### 3.12 静态资源缓存导致改动不生效

**现象**：改了 `public/app.js` / `style.css` / `md-render.js`（或服务端逻辑），刷新页面后行为没变，像"改动没生效"。

**可能原因**

| # | 原因 | 判断依据 |
|---|------|---------|
| 1 | **跑的是打包后的 exe（最常见）**：`pkg` 打包时把 `public/**/*` 作为 assets 固化进 exe（`LightMDKit/package.json:11-18`），运行时 `express.static(path.join(__dirname, 'public'))`（`LightMDKit/server.js:14`）指向的是**快照内**的副本 → 修改源码目录的 `public/` 永远不会生效，必须重新 `npm run build:win`（`LightMDKit/package.json:9`） | 任务管理器里进程名是 `lightmdkit.exe` 而不是 `node.exe` |
| 2 | **浏览器缓存**：`index.html` 用查询串做版本号——`style.css?v=17`（`index.html:19`）、`md-render.js?v=4`（`index.html:96`）、`app.js?v=21`（`index.html:97`）。改了文件却没升版本号时，同一 URL 可能命中浏览器内存/磁盘缓存 | Network 面板该资源 Size 列显示 `(memory cache)` 或 `(disk cache)` |
| 3 | Express 静态资源默认缓存策略：`express.static` 未传任何 options（`LightMDKit/server.js:14`），默认 `Cache-Control: public, max-age=0` + ETag / Last-Modified。`max-age=0` 下浏览器通常会带 `If-None-Match` 回源校验，源文件变了应能拿到新内容；但若中间有反向代理/网关，可能命中网关缓存（**需进一步确认**部署环境） | 响应头为 `Cache-Control: public, max-age=0` 且带 ETag（未配置自定义缓存头） |
| 4 | HTML 自身的缓存控制靠 meta（`index.html:6-8` 的 `no-cache, no-store, must-revalidate`）。meta 只对当前文档生效，且不能覆盖真实响应头；Express 未对 HTML 单独设置缓存头 | 页面源码里有该 meta，但响应头里并没有 `no-cache`（两者不能互相覆盖） |
| 5 | 第三方 CDN 资源被缓存（`index.html:10, 18, 89-95`：github-markdown-css / codemirror / mermaid / marked，均为浮动主版本号如 `@5`、`@12`） | 请求 URL 指向 `cdn.jsdelivr.net`，且不受本地 `?v=` 版本号控制 |
| 6 | 服务端 `server.js` 修改后没重启（守护进程不会热重载） | 进程启动时间早于文件修改时间 |

**排查步骤**
1. 先确认运行形态：任务管理器里是 `lightmdkit.exe` 还是 `node.exe`？是 exe 就必须重新打包（原因 1）
2. DevTools → Network → 勾选 **Disable cache** 后刷新，看目标资源的 Status 与 Size：
   - `(memory cache)` / `(disk cache)` → 命中浏览器缓存（原因 2）
   - `304` → 已回源校验且认为未变（检查是不是改的不是同一份文件）
   - `200` → 拿到新内容，问题在别处
3. 对照磁盘内容：`curl -s http://localhost:3456/app.js` 输出与 `public/app.js` 里的新代码是否一致（例如 `curl -s http://localhost:3456/app.js | grep "<刚改的关键字>"`）
4. 确认服务进程的启动时间晚于代码修改时间（否则重启服务）

**解决方案**
1. 源码模式：改完 `public/` 下任意文件后，**同步把 `index.html` 对应 `?v=` 数字加 1**（`index.html:19, 96, 97`），再 Ctrl+F5 硬刷新
2. exe 形态：`npm run build:win` 重新打包（别忘了 GUI 子系统补丁，见 3.2），退出旧进程后再运行新 exe
3. 排查期临时手段：DevTools 勾选 Disable cache；或 Network 面板右键请求 → Clear browser cache
4. 改了 `server.js` 必须重启服务：`stop.bat` / `pkill -f "node server.js"` 后重新启动（守护进程不会自动加载新代码）

**源码位置**：`LightMDKit/public/index.html:6-8, 19, 96, 97`、`LightMDKit/server.js:14`、`LightMDKit/package.json:9, 11-18`

---

### 3.13（补充）CDN 依赖加载失败（离线 / 内网 / 被拦截）

**现象**：页面能打开但排版错乱；点击文件后正文空白、状态栏报 `Cannot read properties of undefined (reading 'parse')` 之类的错误；点「现代模式」提示 `编辑器未就绪，无法进入现代模式`（`app.js:980`）；Console 有 `CodeMirror/Mermaid init failed, falling back to textarea:`（`app.js:149`）。

**原因**：五个第三方库全部走公网 CDN（`index.html:10, 18, 89-95`），项目**没有本地兜底副本**：

| 缺失的库 | 后果 | 依据 |
|---------|------|------|
| `marked` | 渲染直接失败：`MdRender.renderMarkdown(text, undefined)` 中 `patchStrikethrough` 原样返回（`md-render.js:144`），随后 `undefined.parse(...)` 抛 TypeError（`md-render.js:168`），被 `loadFile` 的 catch 捕获并打到状态栏（`app.js:783-786`） | `md-render.js:143-169` |
| `CodeMirror` | 初始化抛错被兜住，`cmEditor = null`（`app.js:99-151`）→ 回退 textarea，传统模式仍可用；现代模式被拒（`app.js:979-982`）；若 localStorage 偏好为 modern，启动时静默退回传统（`app.js:1449-1456`） | `app.js:102-151` |
| `mermaid` | 图表不渲染，其余功能正常（`typeof mermaid === 'undefined'` 直接返回，`app.js:656`） | `app.js:655-664` |
| `github-markdown-css` | 排版退化（无报错） | `index.html:10` |

**排查步骤**：DevTools → Network 过滤 `cdn.jsdelivr.net`，看是否失败/超时/被代理拦截；Console 执行 `[typeof marked, typeof CodeMirror, typeof mermaid]`。

**解决方案**：需要离线/内网使用时，把这几个库下载到 `public/vendor/` 并把 `index.html` 的 CDN 地址改为本地相对路径（同时升 `?v=`）；否则需保证浏览器可访问 `cdn.jsdelivr.net`。当前代码无离线兜底，属已知限制。

**源码位置**：`LightMDKit/public/index.html:10, 18, 89-95`、`LightMDKit/public/app.js:103-155, 655-664, 979-982`、`LightMDKit/public/md-render.js:143-169`

---

### 3.14（补充）打开子目录中的文件时内容可能串位（同名文件）

**现象**（**已修复**，保留此条是为说明根因，便于后续改动时不再踩同一个坑）：根目录与子目录存在同名 `.md`（如 `a.md` 与 `sub/a.md`），从文件列表打开 `sub/a.md` 时，曾显示/保存为根目录那个 `a.md` 的内容。

**原因与依据**：`renderFile()` 曾用**文件名（不含子目录）**从目录根重新获取句柄：`currentFolderHandle.getFileHandle(fileEntry.name)`；而 `collectMarkdownFiles()` 写入的 `entry.name` 只有 basename，只有 `entry.path` 才包含子目录前缀。根目录存在同名文件时，`getFileHandle(name)` 会成功返回**根目录那个文件**的句柄，随后 `handle.getFile()` 读到的就是它；若根目录没有同名文件则抛 `NotFoundError`，被 catch 忽略并回退到扫描时的正确句柄 —— 因此**只在同名场景暴露**。

**排查步骤**
1. 确认"内容不对"的文件在根目录是否有同名文件
2. 对照文件列表分组标题（渲染自 `entry.path`）与 `currentFile`
3. 在根目录与子目录各放一个同名 `a.md`、内容不同，打开子目录那个实测确认

**解决方案**：**已在 `LightMDKit/public/app.js:616-628` 修复** —— 重取句柄改为沿 `entry.path` 用 `/` 拆段、逐级 `getDirectoryHandle` 再 `getFileHandle`，任一步失败则回退到扫描时的句柄。另建议避免在根目录与子目录放同名文档，以减少选择歧义。

**源码位置**：`LightMDKit/public/app.js:616-628`（修复点）、`224-248`（`entry.path` 的来源）

---

## 4. 状态栏提示语速查表

> `setStatus(msg, type)`（`app.js:153-163`）会把提示写入顶栏 `#status`，**3 秒后自动清空**（含 error）。排查时若提示一闪而过，请对照 Console 的 `console.error` 或本文对应条目。

| 提示语 | 类型 | 触发位置 | 含义与处理 |
|--------|------|---------|-----------|
| `浏览器不支持文件夹选择，请使用 Chrome 或 Edge` | error | `app.js:748` | 见 3.3：换 Chromium 浏览器，或改用 `localhost` 访问（非安全上下文同样触发） |
| `等待选择文件夹...` | 默认 | `app.js:752` | 对话框已弹出，等待用户操作 |
| `已取消` | 默认 | `app.js:758` | 用户取消了目录选择（`AbortError`），非故障 |
| （对话框异常时的 `e.message`） | error | `app.js:761` | 如 `NotAllowedError`、`SecurityError`，见 3.3 / 3.4 |
| `加载成功` | success | `app.js:755` | 目录已加载并选中了文件 |
| `该目录下没有找到 Markdown 文件`（内容区） | 默认 | `app.js:732` | 见 3.4 / 3.5 |
| `目录下无 Markdown 文件` | 默认 | `app.js:733` | 同上 |
| `读取文件...` | 默认 | `app.js:771` | 正在读取，若随后报错见 3.6 / 3.13 |
| `文件已加载` | success | `app.js:782` | 正常 |
| `请先加载文件` | error | `app.js:791`（刷新）、`app.js:1024`（编辑） | 未加载任何文件时点「刷新」或「编辑」 |
| `刷新中...` | 默认 | `app.js:796, 807` | 正在重扫目录 |
| `已刷新` | success | `app.js:800`（无目录句柄分支）、`app.js:838` | 刷新完成 |
| `当前文件已被移除` | error | `app.js:822` | 见 3.10，注意内容区会被清空 |
| `目录已刷新，发现 N 个新文件` | success | `app.js:836` | 正常，N 为净增数量 |
| `当前文件来源不支持写回保存` | error | `app.js:901` | 见 3.6：拖入的单文件没有可写句柄 |
| `已保存` | success | `app.js:909` | 显式保存成功 |
| `保存失败: <message>` | error | `app.js:912` | 见 3.6，`<message>` 常为 `NotAllowedError` 等 |
| `编辑器未就绪，无法进入现代模式` | error | `app.js:980` | CodeMirror 未初始化（CDN 失败等），见 3.13 |
| `仅支持拖入 .md / .markdown 文件或文件夹` | 默认 | `app.js:1331` | 见 3.11 |
| `拖放打开失败: <message>` | error | `app.js:1362` | 见 3.11 |
| `已在新标签页打开 <文件名>` | success | `app.js:1389` | 拖入文件成功，已定位到所在目录 |
| `已在新标签页打开拖入的文件夹` | success | `app.js:1391` | 拖入文件夹成功 |
| `已打开拖入的文件（浏览器限制未定位所在目录，可手动"加载文件夹"）` | 默认 | `app.js:1409` | 浏览器安全限制，属预期（见 3.11） |
| `打开拖入内容失败: <message>` | error | `app.js:1413` | 见 3.11（常见于句柄权限失效） |
| `新标签页被拦截，请 点击打开`（内含链接） | 特殊 | `app.js:1271-1280` | 见 3.11，点状态栏里的链接可打开 |

**内容区 / 列表区的非状态栏文案**

| 文案 | 位置 | 含义 |
|------|------|------|
| `暂无内容，请先加载文件夹` | `index.html:61`、`app.js:545` | 尚未加载目录 |
| `当前目录下没有找到 Markdown 文件` | `app.js:544` | 已加载目录但扫描结果为空（见 3.4 / 3.5） |
| `没有匹配的文件` | `app.js:558` | 过滤框无命中，按 Esc 清空 |
| `当前文件没有标题目录` | `app.js:365, 458` | 文档里没有 `#` 标题 |
| `当前文件已被移除`（内容区） | `app.js:821` | 见 3.10 |
| `拖放数据不存在或已过期，请重新拖入文件` | `app.js:1378` | 见 3.11 |
| `暂无内容` | `app.js:607` | 空状态占位 |

---

## 5. 排障命令与 Console 片段速查

### 5.1 服务端

```powershell
# 1) 前台启动（诊断第一步，唯一能看到服务端日志）
$env:LIGHTMDKIT_DAEMON='1'; node LightMDKit/server.js

# 2) 端口与进程
netstat -ano | findstr :3456
tasklist /FI "PID eq 12345"

# 3) 服务自检
curl -s -o /dev/null -w "%{http_code}`n" http://localhost:3456/
curl -s -X POST http://localhost:3456/api/load -H "Content-Type: application/json" -d "{\"folderPath\":\".\"}"
curl -s "http://localhost:3456/api/file?path=README.md"

# 4) 停止
taskkill /F /IM lightmdkit.exe
# 仅结束目标 PID，避免误杀其它 node 程序
Stop-Process -Id 12345 -Force
```

```bash
# macOS / Linux
lsof -i:3456
pkill -f "node server.js"
LIGHTMDKIT_DAEMON=1 node LightMDKit/server.js
```

### 5.2 浏览器 Console 自检片段

```js
// 环境能力自检：非安全上下文 / 浏览器不支持 一目了然
[isSecureContext, location.origin, typeof window.showDirectoryPicker,
 typeof marked, typeof CodeMirror, typeof mermaid]
// 期望（Chrome 打开 http://localhost:3456）：[true, "http://localhost:3456", "function", "object", "function", "object"]

// 渲染层自检：验证 ~ 删除线规则（见 3.9）
MdRender.renderMarkdown('范围 1~100、2~5 与 ~~删除~~', marked)

// 确认页面加载的是哪个版本的 marked
// DevTools → Network → 过滤 marked → 查看实际 URL（index.html:95 为浮动主版本 marked@12）
```

### 5.3 关键排查入口

| 想看什么 | 去哪里看 |
|---------|---------|
| 前端异常堆栈 | DevTools → Console（保存/加载/拖放失败都会 `console.error`） |
| 拖放传递的记录 | DevTools → Application → IndexedDB → `lightmdkit` → `drops`（`app.js:1196-1197`） |
| 资源是否命中缓存 | DevTools → Network（看 Size 列的 `(memory cache)`/`(disk cache)` 与状态码） |
| 编辑器是否生效 | DevTools → Elements 搜 `CodeMirror`、`live-preview`、`cm-md-strike` |
| 服务端是否在跑 | `curl -s -o /dev/null -w "%{http_code}" http://localhost:3456/` |

---

## 6. 排障最佳实践

### 6.1 排障原则

- **先定形态，再谈根因**：源码模式 / exe / 守护进程三者的资源来源与日志能力完全不同（第 0 节）。"改了不生效""没有日志"八成是形态问题，而不是代码问题。
- **先拿日志，再猜原因**：默认启动方式把日志丢进了黑洞（`stdio:'ignore'` + GUI 子系统），务必先用 `LIGHTMDKIT_DAEMON=1` 前台启动复现一次。
- **区分"设计如此"与"缺陷"**：取消选择显示 `已取消`、现代模式不显示图片、拖入单文件不定位目录、锚点链接在文件列表视图下不跳——都是当前实现下的预期行为，先排除这些再定位缺陷。
- **保留现场**：报错前先截图状态栏、复制 Console 堆栈、记下 DevTools Network 里该资源的 URL 与状态码。
- **对照可复现基线**：`~` 相关的回归直接用 3.9 的实测表比对；文件扫描相关问题先量深度与数量（6 层 / 3000 个）。

### 6.2 预防措施

- **改前端资源必升版本号**：`index.html:19/96/97` 的 `?v=` 是唯一的缓存失效手段，建议纳入提交前自检清单。
- **打包后必跑 GUI 补丁**：统一用 `npm run build:win`（`LightMDKit/package.json:9`），不要单独执行 `pkg .`；打包完确认输出 `Changed subsystem to WINDOWS_GUI`（`LightMDKit/scripts/set-windows-gui.js:52`）。
- **不要用 `taskkill /F /IM node.exe`**（`LightMDKit/stop.bat:7`）：会杀掉机器上所有 Node 程序，建议改为按 PID 精确结束，或让服务写入 PID 文件。
- **补充日志能力**：当前无日志文件、无级别、无请求记录。建议至少为服务端错误与前端上报增加文件日志，否则 exe 形态的问题基本无法事后追溯。
- **文档同步**：README 中"再次启动时提示选择「重新运行」/「显示访问 URL」"与当前 `main()` 实现不符（`LightMDKit/server.js:291-311` 无交互提示）；历史文档中出现的 `findPidByPort()` 在当前源码中**不存在**（同名函数已不在 `server.js` 中）。改动启动逻辑后请同步更新 README 与知识库，避免排障时被误导。
- **仓库体积**：`dist/lightmdkit.exe`（约 40 MB）当前被 git 跟踪，每次打包都会产生大对象，建议评估改为 `.gitignore` + 发布产物。
- **已知边界写进使用说明**：6 层 / 3000 文件扫描上限（`app.js:81-82`）、跳过目录清单（`app.js:84-86`）、不解析 `../`（`app.js:172-174`）、服务端 `/api/load` 不递归（`LightMDKit/server.js:118-127`）——建议在 README 的"使用说明"里明确，减少误报为故障。

### 6.3 支持渠道

- 功能待办与已知缺陷：`LightMDKit/feature-todo.md`
- 使用说明与接口清单：`LightMDKit/README.md`、[API索引](./API索引.md)
- 问题反馈：仓库 issue / 项目维护者

---

## 7. 相关文档和支持

- **业务流程详解**: [业务流程详解](../业务知识库/业务流程详解.md)
- **业务流程索引**: [业务流程索引](../业务知识库/业务流程索引.md)
- **API 索引**: [API索引](./API索引.md)
- **数据结构**: [数据结构](./数据结构.md)
- **仓库架构**: [仓库架构](../仓库架构.md)
- **仓库依赖**: [仓库依赖](../仓库依赖.md)
- **代码编写指南**: [代码编写指南](./代码编写指南.md)
- **外部接入指南**: [外部接入指南](../外部接入指南.md)

---

> 排障指南应持续更新，沉淀排障经验。新增故障场景请同时补充：现象、可复现步骤、根因（附源码位置）、解决方案。
