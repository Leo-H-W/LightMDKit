# 单元测试文档

> 本文档从代码仓库中提取，说明如何编写和运行单元测试。
>
> **本文档的特殊性**：LightMDKit 是一个纯粹的"浏览器端 + 静态服务"项目，**当前没有任何自动化测试**（无测试框架、无测试文件、无 CI）。因此本文档分为两部分：
> - **第一部分（第 1–7 章）**：如实描述测试现状，并给出"若要开始写测试，该从哪切入"的指导。
> - **第二部分（第 8 章）**：**人工验证清单**——在自动化测试缺位的情况下，按功能模块列出的可落地回归验证步骤。
>
> 凡标注「**现状**」的均为已在仓库中核实的事实，标注「**建议**」的为尚不存在、需要新建的内容。

---

## 1. 测试框架

| 项目 | 值 |
|-----|---|
| **主框架** | 无（项目当前未配置任何测试框架） |
| **Mock 库** | 无 |
| **依赖位置** | `LightMDKit/package.json` |

**核实结论（现状）**：

- `LightMDKit/package.json:7-10` 的 `scripts` 中只有 `start` 与 `build:win`，**没有 `test` 脚本**。
- `LightMDKit/package.json:23-25` 的 `devDependencies` 中只有 `pkg`（打包工具），**没有 jest / mocha / vitest / jasmine / ava / cypress / playwright 等任何测试相关依赖**。
- 运行时依赖只有 `express` 与 `marked`（`LightMDKit/package.json:19-22`）。
- 实际执行 `npm test` 的输出（已实测）：

  ```
  npm error Missing script: "test"
  npm error To see a list of scripts, run:
  npm error   npm run
  ```

- 遍历 `node_modules/.bin` 只有 `pkg`、`marked`、`jsesc`、`parser`、`mime` 等构建/运行时工具，**无任何测试运行器可执行文件**。

> **建议**：由于本项目主打「免安装、零构建」（见 `LightMDKit/package.json:4` 的 description），引入 Jest/Vitest 会带来几十个传递依赖，与项目定位不符。**推荐使用 Node 18+ 内置的 `node:test` + `node:assert`**，无需安装任何依赖即可运行单元测试（本文档第 5 章的命令已在 Node v24.13.0 环境下实测可用）。

---

## 2. 测试文件组织

### 2.1 目录结构（现状）

项目当前 **没有** 单元测试文件，也没有 `test/`、`tests/`、`__tests__/` 目录。经 `git log --all --name-only` 检索**全部提交历史**，测试相关路径从未出现过，即项目从未有过测试。

仓库的真实结构如下（**注意 `test-*.md` 不是测试代码**）：

```
LightMDKit/
├── server.js                  # Express 静态服务 + 守护进程 + 4 个 REST API
├── package.json               # 无 test 脚本、无测试依赖
├── public/
│   ├── index.html             # 页面骨架 + CDN 脚本引入
│   ├── app.js                 # 前端全部逻辑（IIFE，约 1465 行）
│   ├── md-render.js           # UMD 模块：Markdown 预处理与渲染（可被 Node require）
│   └── style.css
├── scripts/
│   └── set-windows-gui.js     # 打包后修改 PE 子系统标志
├── agent-rules/kb-commands/   # 知识库生成的提示词模板
├── doc/kb/                    # 知识库文档（本文档所在位置）
├── dist/                      # pkg 打包产物
├── test-sample.md             # ⚠ 人工验证用样例，非测试代码
├── test-wide.md               # ⚠ 人工验证用样例，非测试代码
└── node_modules/
```

**关于 `LightMDKit/test-sample.md` 与 `LightMDKit/test-wide.md`（现状）**：

这两个文件是**手写的人工验证素材**，不是自动化测试。用途如下，可在回归时直接加载查验证渲染效果：

| 文件 | 覆盖内容 |
|-----|---------|
| `LightMDKit/test-sample.md` | 标题层级（用于目录 TOC）、代码块、GFM 表格、Mermaid 流程图、Mermaid 时序图 |
| `LightMDKit/test-wide.md` | 15 列宽表格（横向滚动）、超长代码行（横向滚动） |

### 2.2 命名规范（建议）

项目当前无测试，以下为**建议**规范（遵循 Node.js 社区惯例，并与本项目的文件命名风格一致）：

- **测试文件**：`{源文件名}.test.js`，集中放置于项目根的 `test/` 目录。
  - 例：`test/md-render.test.js` 对应 `public/md-render.js`
  - 例：`test/server.test.js` 对应 `server.js`
- **测试函数**：使用中文断言语句，形如 `test('当{条件}时应{预期行为}', ...)`。
  - 本项目代码注释全部为中文，测试名沿用中文可读性更好。

> **建议**：`app.js` 是浏览器 IIFE，在完成重构（拆分纯函数并导出）之前**不建议**为它编写单元测试，理由见第 6 章。

---

## 3. 运行测试

### 3.1 运行全部测试（现状：不可用）

```bash
npm test
```

> **现状**：该命令**当前会失败**并报 `Missing script: "test"`。需先按第 4 章添加 `test` 脚本后才可用。

### 3.2 运行特定测试（现状：不可用）

```bash
npm test -- md-render.test.js    # 按文件
npm test -- -t "范围"            # 按用例名
```

> **现状**：同上，依赖 npm 脚本，当前不可用。

### 3.3 查看覆盖率（现状：不可用）

```bash
npm test -- --coverage
```

> **现状**：**项目未配置任何覆盖率工具**（无 nyc / c8 / istanbul 依赖），该命令当前不可用。若需覆盖率，`node:test` 可配合 Node 内置的 `--experimental-test-coverage` 标志使用（无需安装依赖）。

### 3.4 当前**可立即执行**的验证命令（现状：可用）

以下命令**无需修改 `package.json`、无需安装任何依赖**，在当前环境下已实测通过。可用于临时验证 `md-render.js` 的行为：

```bash
# 直接打印渲染结果，肉眼比对
node -e "const{renderMarkdown}=require('./public/md-render.js');const{marked}=require('marked');console.log(renderMarkdown('范围 1~100 与 2~5',marked))"

# 使用 Node 内置测试运行器（Node 18+）
node --test test/md-render.test.js
```

已实测输出示例：

```
$ node -e "..."
<p>范围 1~100 与 2~5</p>          # 单个 ~ 未被误判为删除线
$ node -e "..."
<p>这是 <del>删除</del> 文本</p>   # 双波浪线正常渲染
```

---

## 4. 测试代码模板

> **现状**：项目**没有任何测试文件可供提取模板**。以下模板为**建议**，基于仓库中真实存在的 `public/md-render.js` 编写，并已在 Node v24.13.0 下实测运行通过。

### 4.1 基础测试模板

**参考源文件**：`LightMDKit/public/md-render.js:1-8`（UMD 包装，支持 Node `require`）、`LightMDKit/public/md-render.js:171`（导出清单）

```javascript
// test/md-render.test.js
const test = require('node:test');
const assert = require('node:assert');
const { marked } = require('marked');
const { renderMarkdown } = require('../public/md-render.js');

test('当文档出现范围写法时应保持原样而非渲染为删除线', () => {
  const html = renderMarkdown('范围 1~100、2~5', marked);
  assert.ok(!html.includes('<del>'), '单个 ~ 不应产生 <del>');
});

test('当使用双波浪线时应渲染为删除线', () => {
  const html = renderMarkdown('~~删除~~', marked);
  assert.ok(html.includes('<del>删除</del>'));
});
```

> 该模板的实测运行结果：`pass 2 / fail 0`。

### 4.2 Mock 使用模板

**现状**：项目**没有任何 Mock 代码**。`md-render.js` 的设计使得大多数场景**无需 Mock**——依赖通过参数注入（`LightMDKit/public/md-render.js:166` 的 `renderMarkdown(markdown, marked)` 接收 `marked` 实例），而非在模块内 `require`。

因此**建议**的"打桩"方式是参数注入而非 Mock 框架：

```javascript
// 用最小替身验证"依赖被正确注入并调用"，不需要 jest.mock
test('renderMarkdown 应把预处理后的文本交给 marked.parse', () => {
  const calls = [];
  const fakeMarked = {
    use: () => {},
    parse: (text) => { calls.push(text); return '<p>stub</p>'; },
  };
  const { renderMarkdown } = require('../public/md-render.js');

  const html = renderMarkdown('  foo\n    bar', fakeMarked);   // 行首缩进会被预处理

  assert.strictEqual(html, '<p>stub</p>');
  assert.strictEqual(calls.length, 1);
  assert.ok(calls[0].includes('&nbsp;'), '缩进应已转换为 &nbsp;');
});
```

> **注意（现状）**：`LightMDKit/public/md-render.js:143-164` 的 `patchStrikethrough` 会调用 `marked.use(...)` **原地修改传入的 marked 实例**，而 `marked` 是模块单例。用 `patchedMarked`（一个 `WeakSet`，`LightMDKit/public/md-render.js:141`）防止重复打补丁，因此**补丁效果会跨测试用例累积**。编写测试时需注意这一点，不要假设每个用例拿到的是"未打补丁"的 marked。

---

## 5. 如何编写新测试

### 5.1 步骤 1：确认测试对象是否可测

**首先检查目标模块是否能被 Node `require`**。本项目的可测性分成三档：

| 模块 | 可测性 | 原因 |
|-----|-------|------|
| `LightMDKit/public/md-render.js` | ✅ **易测** | UMD 包装（`:1-8`）导出 4 个函数（`:171`），仅依赖注入的 `marked`，无 Node/浏览器 API |
| `LightMDKit/server.js` | ⚠ **中等** | 依赖 `fs`/`child_process`/`express`；且 `main()` 在模块加载时直接执行（`:313`），`require` 会真的启动服务并尝试打开浏览器 |
| `LightMDKit/public/app.js` | ❌ **难测** | 浏览器 IIFE（`:1`），**无任何导出**，依赖 DOM / CodeMirror / Mermaid / File System Access API / localStorage / IndexedDB |

已验证 `md-render.js` 可被 Node 直接加载：

```
$ node -e "console.log(Object.keys(require('./public/md-render.js')).join(', '))"
preserveIndent, mergeTableCells, patchStrikethrough, renderMarkdown
```

> 这也是 `LightMDKit/server.js:7` 的 `require('./public/md-render.js')` 能work的原因——同一份渲染逻辑同时服务浏览器端与 Node 端。

### 5.2 步骤 2：选择推荐参考位置

由于项目无现存测试，**建议**按下表选择被测源码作为参照：

| 参考位置 | 适用于 | 建议的测试切入点 |
|---------|-------|----------------|
| `LightMDKit/public/md-render.js:10-57` (`preserveIndent`) | 纯字符串变换 | 缩进转换、围栏代码块内缩进不被改写、结构性缩进（列表/引用/标题）保持原样 |
| `LightMDKit/public/md-render.js:59-132` (`splitTableCells`/`isDelimiterRow`/`mergeTableCells`) | 多行状态机 | 表格多行单元格合并为 `<br>`、表格边界判定 |
| `LightMDKit/public/md-render.js:134-164` (`patchStrikethrough`) | 分词器补丁 | **单 `~` 不产生 `<del>`**、双 `~~` 产生 `<del>`、重复调用不重复打补丁（`WeakSet`） |
| `LightMDKit/server.js:100-130` (`POST /api/load` 参数校验分支） | HTTP 接口 | 缺少 `folderPath` → 400；目录不存在 → 404；路径是文件 → 400 |
| `LightMDKit/server.js:171-196` (`GET /api/file`) | HTTP 接口 | 缺少 `path` → 400；指向目录 → 400；文件不存在 → 404 |

### 5.3 步骤 3：编写测试

1. 修改测试名为 `当{条件}时应{预期行为}`
2. 准备输入数据（**纯函数优先直接传字符串，无需 fixture 文件**）
3. 注入依赖（参考 `LightMDKit/public/md-render.js:166` 的参数注入写法，而非 Mock 框架）
4. 执行被测代码
5. 断言结果（用 `node:assert` 的 `strictEqual` / `ok` / `throws`）

### 5.4 步骤 4：运行验证

```bash
# 单文件（无需改 package.json，Node 18+ 直接可用）
node --test test/md-render.test.js

# 若已按建议添加 "test": "node --test" 脚本
npm test
```

> **若要为 `server.js` 接口写测试**，需先做可测性改造（**现状：尚未改造**）：
> - 把 `LightMDKit/server.js:313` 的 `main();` 改为 `if (require.main === module) main();`
> - 把 `app`（`LightMDKit/server.js:10`）通过 `module.exports` 导出
> - 之后可用 `node:test` + `fetch`（Node 18+ 内置）发起请求，**无需 supertest**；端口建议改为 `0`（随机端口）以避免与 `LightMDKit/server.js:11` 硬编码的 `3456` 冲突

---

## 6. 测试规范

### 6.1 命名规范（建议）

| 项目 | 规范 |
|-----|-----|
| 测试目录 | `test/`（位于项目根，与 `public/`、`scripts/` 平级） |
| 测试文件 | `{源文件名}.test.js`，如 `md-render.test.js` |
| 测试分组 | `describe('{模块或函数名}', () => { ... })` |
| 测试用例 | `test('当{条件}时应{预期行为}', () => { ... })` |
| 否定用例 | `test('当{异常输入}时应{异常行为}', () => { ... })` |

### 6.2 测试结构（建议）

推荐 **AAA 模式**（Arrange-Act-Assert），并显式加注释分段：

```javascript
test('当表格单元格跨行时应合并为同一行并用 <br> 连接', () => {
  // Arrange - 准备输入（表格首格内容跨了两行）
  const input = '| a | b |\n| --- | --- |\n| 第一行\n续行 | 值 |';

  // Act
  const { mergeTableCells } = require('../public/md-render.js');
  const result = mergeTableCells(input);

  // Assert - 续行被并回首格，两行合成一行（注意：行数会减少，不是保持不变）
  assert.strictEqual(result, '| a | b |\n| --- | --- |\n| 第一行<br>续行 | 值 |');
});
```

> 上述断言中的期望字符串已实测验证（输入 4 行 → 输出 3 行）。另附一个已验证的 `preserveIndent` 期望值，可直接用作回归基线——它同时覆盖了"缩进转换"与"结构性缩进豁免"两个分支：
>
> ```javascript
> preserveIndent('  普通文本\n  - 列表项\n  # 标题\n  | 表格 |')
> // => '&nbsp;&nbsp;普通文本\n  - 列表项\n  # 标题\n  | 表格 |'
> //    第 1 行普通文本的缩进被转为 &nbsp;；
> //    后三行分别是列表 / 标题 / 表格，属结构性行，缩进原样保留
> ```

### 6.3 Mock 原则（建议）

- **优先参数注入，而非 Mock 框架**：`md-render.js` 已按此设计（`LightMDKit/public/md-render.js:166`），保持这一风格即可，无需引入 Mock 库。
- **不 Mock 业务逻辑**：`preserveIndent`、`mergeTableCells` 这类纯函数应真实调用，只对输入做变量控制。
- **不 Mock `marked`**：`marked` 是已安装的真实依赖，且 `~` 修复的**正确性只在与真实 marked 12 交互时才成立**（补丁针对的是 marked 12 的内置 `del` 规则，见 `LightMDKit/public/md-render.js:134-139` 的注释）。**若用假 marked，这个 bug 的回归测试会完全失效。**
- **测试隔离注意**：`patchStrikethrough` 修改的是 marked 单例（见 4.2 节说明），涉及该补丁的用例需接受跨用例的补丁累积。

### 6.4 为什么 `app.js` 难测（现状与改造建议）

`LightMDKit/public/app.js` 是一个立即执行函数（`:1` 的 `(function () {` 到 `:1465` 的 `})();`），**不导出任何符号**，且模块级直接执行了 DOM 查询（`:2-22`）与副作用调用（`:1458-1464`）。它依赖以下浏览器专有 API：

| 依赖 | 位置 | 测试障碍 |
|-----|------|---------|
| `document.querySelector` 等 DOM API | `LightMDKit/public/app.js:2-22` | 需 jsdom |
| File System Access API（`showDirectoryPicker`） | `LightMDKit/public/app.js:766` | 无 Node 实现，必须整体打桩 |
| `FileSystemDirectoryHandle.entries()` | `LightMDKit/public/app.js:228` | 需构造异步迭代器替身 |
| `CodeMirror`（CDN 全局变量） | `LightMDKit/public/app.js:106` | 需真实或替身编辑器实例 |
| `mermaid`（CDN 全局变量） | `LightMDKit/public/app.js:133` | 同上 |
| `localStorage` | `LightMDKit/public/app.js:47` 等 | 需 jsdom 或替身 |
| `IndexedDB` | `LightMDKit/public/app.js:1222-1274` | 需 fake-indexeddb |

> **建议**：与其为 `app.js` 搭 jsdom + 全套 API 替身，**性价比更高的路径是先做小范围重构**——把下列已经是纯函数的逻辑抽到独立模块并导出，即可直接单元测试，同时不影响浏览器行为：
>
> | 待抽取函数 | 当前位置 | 抽出的收益 |
> |---|---|---|
> | `normalizePathForMatch` | `LightMDKit/public/app.js:176-178` | 覆盖反斜杠 / `./` 前缀 / 大小写归一化 |
> | `flattenByDirectory` | `LightMDKit/public/app.js:197-219` | 覆盖"子目录文件不插花、目录标题不重复"这一易错点（`:188-192` 注释记录了一次真实踩坑） |
> | `slugify` / `generateHeadingId` | `LightMDKit/public/app.js:258-277` | 覆盖重复标题的 `-1`/`-2` 去重 |
> | `isMarkdownName` | `LightMDKit/public/app.js:169-172` | 覆盖 `.md`/`.markdown`/大小写 |
> | `parseHeadingOffsets` | `LightMDKit/public/app.js:279-318` | 覆盖标题 → 字符偏移映射（编辑/浏览位置同步的基础） |
>
> 在这些函数被抽出并导出之前，它们的正确性**只能靠第 8 章的人工验证清单保障**。

---

## 7. 常见测试场景示例

> **现状**：项目无测试文件，故本节**无实际代码可引用**。以下为**建议**的测试场景与真实源码位置的对应关系，供编写时定位被测逻辑。

### 7.1 成功场景测试

**参考**：`LightMDKit/public/md-render.js:166-169`（`renderMarkdown` 主入口）

建议断言：
- 普通段落 → 包在 `<p>` 中
- `~~文本~~` → 含 `<del>文本</del>`（已实测）
- 传入 `gfm: true, breaks: true`，单个换行应产生 `<br>`

### 7.2 错误处理测试

**参考**：`LightMDKit/server.js:100-113`（`/api/load` 的参数校验分支）

建议覆盖的分支（每个分支都对应源码中一条真实的 `return`）：

| 输入 | 期望状态码 | 源码位置 |
|-----|-----------|---------|
| 无 `folderPath` | 400 | `LightMDKit/server.js:100-102` |
| `folderPath` 指向不存在的路径 | 404 | `LightMDKit/server.js:106-108` |
| `folderPath` 指向一个文件 | 400 | `LightMDKit/server.js:110-113` |

### 7.3 边界条件测试

**参考**：`LightMDKit/public/md-render.js:10-57`（`preserveIndent` 的边界判定）

该函数最易出错的边界（每条都对应源码中的一次分支判断）：

- 围栏代码块**内**的行首缩进**不应**被转换（`:30-33`）
- 列表项 `- foo` 前的缩进**不应**被转换（`:40-43`，`isStructural` 判定）
- 标题 `# foo` / 引用 `> foo` / 表格 `| foo` 前的缩进**不应**被转换（`:41-46`）
- 普通文本缩进**应**转换为 `&nbsp;`，其中 Tab 转 4 个（`:51-53`）

---

## 8. 人工验证清单

> **背景**：项目无自动化测试，以下清单是**当前唯一可用的回归手段**。所有步骤均从源码核实，标注了对应的实现位置。
>
> **通用前置条件**：
> 1. 执行 `npm install` 后运行 `npm start`，浏览器打开 <http://localhost:3456>
> 2. 使用 **Chrome / Edge**（文件夹选择依赖 File System Access API，见 `LightMDKit/public/app.js:760`）
> 3. 建议准备一个含**多级子目录**、**同名文件**、**中文文件名**的测试目录

### 8.1 启动与服务

| 编号 | 操作步骤 | 预期结果 | 实现位置 |
|-----|---------|---------|---------|
| S-1 | `npm start` | 控制台输出 `LightMDKit started at http://localhost:3456`，约 2 秒后自动打开浏览器 | `LightMDKit/server.js:299-305` |
| S-2 | 服务已启动时再次 `npm start` | 提示"端口 3456 已被占用"，打开浏览器但**不重复拉起进程** | `LightMDKit/server.js:307-310` |
| S-3 | 执行 `stop.bat`（或 `stop.sh`）后再访问 | 服务已停止，页面无法打开 | `stop.bat` / `stop.sh` |
| S-4 | 断网后打开页面 | 页面骨架可用，但 CDN 资源（marked / CodeMirror / Mermaid / github-markdown-css）加载失败，编辑器回退为 textarea（`app.js:148-151` 捕获异常） | `LightMDKit/public/index.html:10-19,89-95` |

### 8.2 加载文件夹与递归扫描

| 编号 | 操作步骤 | 预期结果 | 实现位置 |
|-----|---------|---------|---------|
| F-1 | 点击「📁 加载文件夹...」，选择一个含 Markdown 的目录 | 目录名显示在顶栏；加载后按钮文案变为「📁 更换文件夹...」 | `LightMDKit/public/app.js:759-778`、`:509-515` |
| F-2 | 在目录选择框中点「取消」 | 状态栏提示"已取消"，无报错 | `LightMDKit/public/app.js:770-771` |
| F-3 | 选择**空目录** | 提示"该目录下没有找到 Markdown 文件" | `LightMDKit/public/app.js:744-747` |
| F-4 | 选择含**子目录**的目录（如 `doc/kb/`） | 子目录中的 `.md` 也出现在文件列表中，并按目录分组 | `LightMDKit/public/app.js:224-248`、`:535-603` |
| F-5 | 目录中放入 `node_modules/`、`.git/`、`dist/`、`build/`、`.svn/`、`.hg/`、`.idea/`、`.vscode/`，各放一个 `.md` | 这些目录下的 `.md` **均不出现**在文件列表 | `LightMDKit/public/app.js:88-90`、`:233-235` |
| F-6 | 构造 **7 层以上**嵌套目录，深层放 `.md` | 深度 ≤ 6 的文件出现；超过 6 层的**不出现**（`depth > SCAN_MAX_DEPTH` 直接返回） | `LightMDKit/public/app.js:85`、`:221` |
| F-7 | 目录中放入 **3000 个以上** `.md` | 扫描在 3000 个处停止，界面不卡死（超出部分不显示，已扫到的可正常打开） | `LightMDKit/public/app.js:86`、`:221`、`:231` |
| F-8 | 准备 `.md`、`.markdown`、`.MD`、`.Markdown`、`.txt` 文件 | 前四种出现，`.txt` 不出现 | `LightMDKit/public/app.js:169-172` |
| F-9 | 准备两个**不同子目录下的同名文件** A，分别点击 | 首次点击 A 打开子目录 1 的版本，第二次点击打开子目录 2 的版本 | `LightMDKit/public/app.js:26-27`、`:178-186` |

### 8.3 文件列表视图

| 编号 | 操作步骤 | 预期结果 | 实现位置 |
|-----|---------|---------|---------|
| L-1 | 观察列表分组标题 | 根目录下的文件归入「根目录」；子目录文件有其目录名作标题 | `LightMDKit/public/app.js:578-585` |
| L-2 | 点击列表中某个文件 | 该文件在右侧渲染，列表项高亮（`.active`） | `LightMDKit/public/app.js:587-603`、`:1131-1135` |
| L-3 | 在过滤框输入子目录名（如 `技术`） | 只显示路径中含该关键词的文件（能匹配子目录名，不只匹配文件名） | `LightMDKit/public/app.js:554-557` |
| L-4 | 在过滤框输入不存在的关键词 | 显示"没有匹配的文件" | `LightMDKit/public/app.js:559-564` |
| L-5 | 过滤后使同一目录的文件在结果中被拆散（如输入 `a` 匹配到 `sub/` 下部分文件） | 每切换一个目录都重新出现目录标题，不会出现"文件不知在哪层目录"的情况 | `LightMDKit/public/app.js:576-585` |
| L-6 | 在过滤框中连续打字 | **输入框不失去焦点**（只更新高亮，不重建整个列表） | `LightMDKit/public/app.js:663-664`、`:1129-1135` |
| L-7 | 在过滤框按 `Esc` | 过滤条件被清空，列表恢复完整 | `LightMDKit/public/app.js:1172-1178` |
| L-8 | 点击侧边栏的「文件列表 / 目录」 | 两个面板互相切换；刷新页面后仍停留在上次选择的面板 | `LightMDKit/public/app.js:1130-1149`、`:59-77` |
| L-9 | 观察「文件列表」标题右侧的数字 | 显示当前扫描到的文件总数 | `LightMDKit/public/app.js:521-524` |
| L-10 | 未加载文件夹时观察列表区 | 提示"暂无内容，请先加载文件夹"；加载了空目录时提示"当前目录下没有找到 Markdown 文件"（两种文案不同） | `LightMDKit/public/app.js:543-552` |

### 8.4 传统模式（编辑 / 浏览分离）

| 编号 | 操作步骤 | 预期结果 | 实现位置 |
|-----|---------|---------|---------|
| T-1 | 默认进入页面 | 编辑按钮显示「编辑」，正文为渲染后的 HTML | `LightMDKit/public/app.js:985-987`、`:939-944` |
| T-2 | 点击「编辑」 | 切换到编辑器，按钮变为「浏览」，行号可见 | `LightMDKit/public/app.js:1042-1107` |
| T-3 | 在编辑器中修改内容后点击「浏览」 | **先保存到磁盘**再切回浏览态，预览显示修改后的内容 | `LightMDKit/public/app.js:1049-1059` |
| T-4 | 浏览态滚动到某个二级标题，再点「编辑」 | 编辑器定位到该标题对应的行（不是文档开头） | `LightMDKit/public/app.js:1072-1105` |
| T-5 | 编辑状态下点击左侧另一个文件 | **自动退出编辑模式**并切回浏览态，渲染新文件 | `LightMDKit/public/app.js:787-792` |
| T-6 | 保存后点「刷新」 | 内容不变，状态栏提示"已刷新" | `LightMDKit/public/app.js:802-857` |
| T-7 | 从只读位置加载文件夹后编辑并保存 | 状态栏提示"保存失败: ..."，不静默丢失 | `LightMDKit/public/app.js:932-936` |

### 8.5 现代模式（Typora 式即时渲染）

| 编号 | 操作步骤 | 预期结果 | 实现位置 |
|-----|---------|---------|---------|
| M-1 | 点击「现代模式」 | 「编辑」按钮**消失**，直接进入可编辑状态，**不显示行号** | `LightMDKit/public/app.js:985-994`、`:946-954` |
| M-2 | 在现代模式下直接打字 | 无需点任何按钮即可编辑；光标所在行有高亮 | `LightMDKit/public/app.js:975-976` |
| M-3 | 打字后观察侧边栏「目录」面板 | 约 300ms 防抖后目录自动更新 | `LightMDKit/public/app.js:401-412` |
| M-4 | 在现代模式下移动光标 | 目录中对应标题高亮跟随光标（而非跟随滚动） | `LightMDKit/public/app.js:147-151`、`:384-398` |
| M-5 | 刷新页面 | **仍处于现代模式**（偏好已存入 localStorage 的 `lightmdkit.mode`） | `LightMDKit/public/app.js:42-60`、`:1449-1457` |
| M-6 | 在现代模式下观察 Mermaid 代码块 | 以**源码形式**留在编辑器中，**不渲染成图**（现代模式没有独立预览面） | `LightMDKit/public/app.js:668-671` |
| M-7 | 点击「传统模式」切回 | 停在**浏览态**，预览重新渲染，Mermaid 图恢复显示，图片重新解析 | `LightMDKit/public/app.js:1026-1034`、`:1016` |
| M-8 | 断网（CodeMirror 未加载）后点「现代模式」 | 提示"编辑器未就绪，无法进入现代模式"，模式不切换 | `LightMDKit/public/app.js:1001-1004` |

### 8.6 编辑模式 3 秒自动保存

| 编号 | 操作步骤 | 预期结果 | 实现位置 |
|-----|---------|---------|---------|
| A-1 | 进入编辑模式（或现代模式），修改内容后**什么都不做**，等待 | 约 3 秒后文件被自动写回磁盘；用外部编辑器/资源管理器查看修改时间会变化 | `LightMDKit/public/app.js:939-950`、`:89`、`:1060`、`:1003` |
| A-2 | 持续编辑不点保存，等待 10 秒以上 | 期间多次自动保存（每 3 秒一次），不会堆积成一次 | `LightMDKit/public/app.js:941` |
| A-3 | 切回浏览模式（传统模式点「浏览」） | 自动保存定时器**停止**（此后修改不再写盘） | `LightMDKit/public/app.js:1051`、`:930-935` |
| A-4 | 现代模式切回传统模式 | 自动保存定时器停止 | `LightMDKit/public/app.js:1028` |
| A-5 | **拖入单个文件**打开（无目录句柄、不可写）后进入编辑模式并修改 | 自动保存静默失败一次后**停止定时器**，不反复弹出错误提示 | `LightMDKit/public/app.js:921-925`、`:920-926` |
| A-6 | 在两个**同名不同目录**的文件间切换并各自编辑 | 分别保存回各自的子目录，不串写 | `LightMDKit/public/app.js:918-920` |
| A-7 | 手动点「浏览」（非自动保存路径）保存 | 状态栏提示"已保存" | `LightMDKit/public/app.js:931` |

### 8.7 拖放打开文件 / 文件夹

| 编号 | 操作步骤 | 预期结果 | 实现位置 |
|-----|---------|---------|---------|
| D-1 | 从资源管理器拖 `.md` 文件到页面上方 | 出现遮罩层"松开鼠标，在新标签页打开..."；松开后**新标签页**打开该文件 | `LightMDKit/public/app.js:1357-1386` |
| D-2 | 拖入一个**文件夹** | 新标签页加载整个目录，文件列表可用 | `LightMDKit/public/app.js:1329-1334` |
| D-3 | 拖入 `.txt` / `.png` 等非 Markdown 文件 | 提示"仅支持拖入 .md / .markdown 文件或文件夹"，不打开新标签页 | `LightMDKit/public/app.js:1352-1354` |
| D-4 | 先把目录 A 加载进来，再从 A 的**子目录**拖一个 `.md` 进页面 | 新标签页加载 A，并**高亮定位到该子目录下的对应文件** | `LightMDKit/public/app.js:1282-1290`、`:1317-1324`、`:1383-1392` |
| D-5 | 拖入一个**不在**当前已加载目录中的单独 `.md` 文件 | 新标签页只打开这一个文件（受浏览器安全限制无法定位父目录）；顶栏有对应提示 | `LightMDKit/public/app.js:1415-1432` |
| D-6 | 在浏览器中拦截弹窗后拖放 | 状态栏出现可点击的「点击打开」链接兜底 | `LightMDKit/public/app.js:1293-1302`、`:1289-1290` |
| D-7 | 拖放打开后等待（或手动删除 IndexedDB 中的 `lightmdkit/drops` 记录）再访问带 `?drop=` 的 URL | 提示"拖放数据不存在或已过期，请重新拖入文件" | `LightMDKit/public/app.js:1389-1402`、`:1198`（TTL 24 小时）、`:1233-1253`（过期清理） |
| D-8 | 在编辑器中**选中文本并拖动** | **不出现**拖放遮罩（只响应文件拖拽，不影响文本拖放） | `LightMDKit/public/app.js:1276-1278`、`:1343-1345` |
| D-9 | 拖拽鼠标在页面内移入移出多次后离开 | 遮罩正常消失，不残留（`dragDepth` 计数归零） | `LightMDKit/public/app.js:1357-1374` |

### 8.8 文档内相对链接跳转与「返回」历史栈

| 编号 | 操作步骤 | 预期结果 | 实现位置 |
|-----|---------|---------|---------|
| H-1 | 在文档中写 `[链接](sub/a.md)` 并点击 | 在**当前页**打开 `sub/a.md`（不跳转、不刷新整个页面） | `LightMDKit/public/app.js:868-903` |
| H-2 | 从 `sub/a.md` 中点击 `[x](../other.md)` | 相对**当前文件所在目录**解析后打开 `other.md`；若失败则退回相对根目录解析 | `LightMDKit/public/app.js:876-893` |
| H-3 | 点击 `http://`、`https://`、`mailto:`、`#anchor`、`//` 开头的链接 | **不被拦截**，保持浏览器默认行为 | `LightMDKit/public/app.js:874` |
| H-4 | 点击 `[x](a.md#某章节)` 或 `[x](a.md?v=1)` | 去掉 `#`/`?` 后缀后仍能正确匹配并打开 | `LightMDKit/public/app.js:865` |
| H-5 | 点击指向**中文文件名**的链接（如 `[x](核心业务领域.md)`） | 能正确打开（URL 编码被解码后匹配） | `LightMDKit/public/app.js:865` |
| H-6 | 点击 `[x](sub\a.md)`（反斜杠）或在 Windows 上大小写不一致 | 仍能匹配打开（路径归一化：反斜杠转 `/`、去 `./`、忽略大小写） | `LightMDKit/public/app.js:174-178` |
| H-7 | 依次点击若干链接，然后点「← 返回」 | 逐级回退到上一个文档（LIFO 栈），内容正确 | `LightMDKit/public/app.js:909-914`、`:93` |
| H-8 | 刚加载文件夹、尚未跳转过时观察「← 返回」 | 按钮为**禁用**状态 | `LightMDKit/public/app.js:905-907`、`index.html:29` |
| H-9 | 从**左侧文件列表**依次点击 3 个文件，再点「← 返回」 | 与链接跳转共用同一历史栈，可逐级回退 | `LightMDKit/public/app.js:528-535` |
| H-10 | 点击指向**不存在文件**的相对链接 | 不被拦截，无错误提示、页面不异常 | `LightMDKit/public/app.js:894` |
| H-11 | 点击指向**当前文件自身**的链接 | 不产生重复的历史记录 | `LightMDKit/public/app.js:897-900` |

### 8.9 Markdown 渲染要点

| 编号 | 操作步骤 | 预期结果 | 实现位置 |
|-----|---------|---------|---------|
| R-1 | 输入 `范围 1~100、2~5` | **渲染为普通文本，不出现删除线**（已实测输出 `<p>范围 1~100 与 2~5</p>`） | `LightMDKit/public/md-render.js:134-164` |
| R-2 | 输入 `~~删除~~` | 渲染为 `<del>删除</del>`（已实测） | `LightMDKit/public/md-render.js:140` |
| R-3 | **现代模式**下输入 `1~100` 与 `~~删除~~` | 源码中 `1~100` 不被标删除线、波浪线不被隐藏；`~~删除~~` 内容显示删除线、定界符被淡化 | `LightMDKit/public/app.js:414-448`、`:111`（CM5 自带 strikethrough 已关闭） |
| R-4 | 在 `test-sample.md` 中查验证 Mermaid 流程图与时序图 | 两者均渲染为图形（传统模式浏览态） | `LightMDKit/public/app.js:668-676`、`LightMDKit/public/index.html:94` |
| R-5 | 构造一个**单元格内换行**的表格 | 跨行的单元格内容合并回同一行，用 `<br>` 表示换行 | `LightMDKit/public/md-render.js:70-132` |
| R-6 | 用 `test-wide.md`（15 列宽表格）验证 | 表格出现**横向滚动条**，不撑破页面 | `LightMDKit/test-wide.md` |
| R-7 | 用 `test-wide.md` 的超长代码行验证 | 代码块出现横向滚动条 | `LightMDKit/test-wide.md` |
| R-8 | 写一段以空格/Tab 开头的**普通文本** | 缩进被保留（转为 `&nbsp;`） | `LightMDKit/public/md-render.js:34-54` |
| R-9 | 写一段以空格开头的**列表项/标题/引用/表格**行 | 缩进结构保持原样（不转 `&nbsp;`），列表/标题正常渲染 | `LightMDKit/public/md-render.js:40-49` |
| R-10 | 在**围栏代码块内**写缩进 | 缩进原样保留，不被转换为 `&nbsp;` | `LightMDKit/public/md-render.js:30-33` |
| R-11 | 在段落中**单次换行**（不空行） | 渲染为 `<br>`（因启用了 `breaks: true`） | `LightMDKit/public/md-render.js:168` |
| R-12 | 文档中相对路径引用图片 | 图片正常显示（通过目录句柄读取并转 objectURL） | `LightMDKit/public/app.js:688-729` |
| R-13 | 写两个**同名**标题，观察目录与跳转 | 两个标题都能点击跳转（id 自动去重为 `x` 和 `x-1`） | `LightMDKit/public/app.js:267-277` |
| R-14 | 滚动正文，观察侧边栏「目录」 | 当前章节高亮跟随滚动位置 | `LightMDKit/public/app.js:1443-1459` |

### 8.10 侧边栏折叠与拖拽调宽

| 编号 | 操作步骤 | 预期结果 | 实现位置 |
|-----|---------|---------|---------|
| B-1 | 点击侧边栏标题右侧的 ◀ | 侧边栏折叠为窄条，按钮变为 ▶ | `LightMDKit/public/app.js:1117-1127`、`index.html:45` |
| B-2 | 折叠后再点 ▶ 展开 | 侧边栏展开；若折叠前手动调过宽度，展开后恢复为**默认宽度**（折叠时清除了内联宽度） | `LightMDKit/public/app.js:1119-1124` |
| B-3 | 拖动侧边栏右边缘的分隔条 | 侧边栏宽度随鼠标实时变化 | `LightMDKit/public/app.js:1180-1203`、`index.html:39` |
| B-4 | 尝试把宽度拖到小于 200px 或大于 500px | 宽度被**钳制**在该范围内，不越界 | `LightMDKit/public/app.js:1196-1202` |
| B-5 | 在侧边栏**已折叠**状态下拖动分隔条 | 不产生任何效果（折叠态下拖拽被忽略） | `LightMDKit/public/app.js:1184` |
| B-6 | 拖动过程中观察鼠标指针 | 指针变为 `col-resize`；松开后恢复正常，且页面文字**不被选中** | `LightMDKit/public/app.js:1187-1188`、`:1183-1189` |

### 8.11 刷新

| 编号 | 操作步骤 | 预期结果 | 实现位置 |
|-----|---------|---------|---------|
| RF-1 | 在外部新建一个 `.md` 文件后点「刷新」 | 新文件出现在列表，状态栏提示"目录已刷新，发现 N 个新文件" | `LightMDKit/public/app.js:846-852` |
| RF-2 | 在外部**删除**当前正在浏览的文件后点「刷新」 | 提示"当前文件已被移除"，正文区显示空状态 | `LightMDKit/public/app.js:827-837` |
| RF-3 | 未加载任何文件时点「刷新」 | 提示"请先加载文件" | `LightMDKit/public/app.js:803-806` |
| RF-4 | **拖入单个文件**（无目录句柄）后点「刷新」 | **仅重新读取当前文件**，不提示"请先加载文件夹"，提示"已刷新"（修复于提交 `4aeacad`） | `LightMDKit/public/app.js:807-819` |
| RF-5 | 在外部修改当前文件内容后点「刷新」 | 正文更新为磁盘上的最新内容 | `LightMDKit/public/app.js:839-844` |

---

## 9. 与源码核实的差异说明

编写本文档时，以下细节经核实与"想当然的理解"不同，特此记录，避免后续误用：

1. **`POST /api/load` 不是递归扫描**。`LightMDKit/server.js:118-127` 使用的是 `fs.readdirSync`（**仅一层**），不进入子目录。**前端默认并不调用该接口**——它走的是浏览器 File System Access API，由 `LightMDKit/public/app.js:224-248` 的 `collectMarkdownFiles` 做**递归扫描**（含跳过目录与深度/数量上限）。服务端那套 REST API 是"替代或扩展"用途（见 `README.md` 的「服务端 API」小节）。因此验证递归扫描、跳过目录、深度/数量上限时，**必须通过浏览器界面验证，不能通过调用 `/api/load` 验证**。

2. **`md-render.js` 不是完全纯的模块**。`preserveIndent`（`:10`）与 `mergeTableCells`（`:72`）是纯字符串函数，但 `renderMarkdown`（`:166`）需要外部传入 `marked` 实例，且 `patchStrikethrough`（`:143`）会**原地修改**该实例（带 `WeakSet` 去重，`:141`）。测试时需注意补丁的跨用例累积效应。

3. **`server.js` 在被 `require` 时会立即执行 `main()`**（`LightMDKit/server.js:313`），会真的拉起守护进程并尝试打开浏览器。因此**当前无法直接 `require` 它来做接口测试**，需先按 5.4 节的改造建议处理。

---

> 📋 测试文件统计: 0 个测试文件
>
> **文档版本**：项目 LightMDKit ｜ 分支 `feat/sidebar-file-browser` ｜ 提交 `f71e683` ｜ 生成日期 2026-09-11
