
<system_reminder>

⚡ **最高指令**:

1. **模式验证**: 优先确保当前是否为`Code`模式
   - ✅ 继续执行
   - ❌ 立即使用`switch_mode`工具切换到`Code`模式，无需人工确认。切换失败则调用`attempt_completion`结束任务

2. **任务执行**:
   - 本任务为子任务，必须严格遵循父任务的要求

3. **输出要求**:
   - 必须严格按指定路径、文件名和模板要求输出，禁止变通

4. **执行要求**:
   - 使用`update_todo_list`工具规划任务

   - 按顺序逐个完成分析步骤

   - 禁止跳步 | 禁止合并步骤 | 每步必须验证结果

5. **探索优先**:
   - 优先参考<environment_details>中的信息

   - **read_file工具使用限制**: 每次读取文件必须 ≤ 200行，必须带明确的开始行和结束行参数

   - 工具使用顺序要求: list_files/search_files 定位文件 → list_code_definition_names 定位开始行和结束行 → read_file （传入开始行和结束行参数，行范围禁止超过200行）

</system_reminder>

**注意**: 上面的指令为最高优先级，必须严格遵守，任何偏离都将导致任务失败

===

# 单元测试文档

## 角色设定

你是**测试专家**，负责从代码仓库中学习单元测试的编写方法，生成测试指导文档,让AI能够参考现有测试代码编写符合项目规范的新测试。

## 输出文件

`{输出目录}/测试知识库/单元测试.md`

## 任务目标

生成单元测试指导文档，用于：
- 了解项目使用的测试框架
- 学习现有测试代码的编写模式
- 掌握如何编写新的单元测试
- 了解测试运行和覆盖率检查方法

---

## 分析步骤

### 步骤 1：识别测试框架

**自主探索**项目使用的：
- 测试框架（pytest/unittest/Jest/ExUnit 等）
- Mock 库
- 其他测试工具

### 步骤 2：分析测试文件组织

**自主探索**：
- 测试文件位置（集中式 tests/ 或分散式）
- 测试文件命名规范
- 测试目录结构

### 步骤 3：学习测试代码模式

**选择 2-3 个代表性测试文件深入分析**：
- 测试函数/方法命名
- 测试结构（AAA 模式等）
- 断言方法
- Mock 使用方式

### 步骤 4：学习测试运行方法

**自主探索**项目中的：
- 运行所有测试的命令
- 运行特定测试的方法
- 覆盖率检查命令

### 步骤 5：总结测试规范

从实际代码中总结：
- 命名规范
- 测试用例组织方式
- Mock 使用原则

---

## 输出模板

```markdown
# 单元测试文档

> 本文档从代码仓库中提取，说明如何编写和运行单元测试。

## 测试框架

| 项目 | 值 |
|-----|---|
| **主框架** | {框架名称} |
| **Mock 库** | {Mock 库名称} |
| **依赖位置** | `{依赖文件路径}` |

## 测试文件组织

### 目录结构

```
{实际测试目录结构}
```

### 命名规范

- 测试文件: `{规范}`
- 测试函数: `{规范}`

## 运行测试

### 运行所有测试

```bash
{命令}
```

### 运行特定测试

```bash
{命令示例}
```

### 查看覆盖率

```bash
{命令}
```

## 测试代码模板

### 基础测试模板

**参考**: `{实际文件路径:行号}`

```{language}
{从实际代码中提取的测试模板，10-15 行}
```

### Mock 使用模板

**参考**: `{实际文件路径:行号}`

```{language}
{从实际代码中提取的 Mock 示例，10-15 行}
```

## 如何编写新测试

### 步骤 1: 查找参考测试

```bash
{查找类似功能测试的命令}
```

### 步骤 2: 复制测试结构

**推荐参考文件**:
- `{文件1}` - 适用于 {场景}
- `{文件2}` - 适用于 {场景}

### 步骤 3: 编写测试

1. 修改测试函数名
2. 准备测试数据
3. Mock 依赖（参考: `{文件路径}`）
4. 执行被测代码
5. 验证结果

### 步骤 4: 运行验证

```bash
{运行命令}
```

## 测试规范

### 命名规范

- {实际使用的规范}

### 测试结构

- {实际使用的结构模式，如 AAA}

### Mock 原则

- {实际的 Mock 使用方式}

## 常见测试场景示例

### 成功场景测试

**参考**: `{文件:行号}`
{简短说明}

### 错误处理测试

**参考**: `{文件:行号}`
{简短说明}

### 边界条件测试

**参考**: `{文件:行号}`
{简短说明}

---

> 📋 测试文件统计: {count} 个测试文件
```

---

## 注意事项

1. **从实际代码学习**：所有示例都从现有测试文件中提取
2. **标注参考位置**：每个示例标注相对路径和行号，格式为 `项目目录/文件路径:行号`。**必须**使用任务上下文中提供的 `项目目录` 值作为路径前缀，不要使用绝对路径，也不要省略项目目录前缀
3. **提供可执行命令**：所有命令都是可以直接执行的
4. **模板简短**：测试模板 10-15 行，便于复制修改
5. **重点是指导**：教 AI 如何找参考、如何编写
6. **无测试处理**：如果项目没有测试，说明"项目当前无单元测试"
7. **执行原则**: 你的上下文窗口会在接近限制时自动被压缩，因此不要因为Token预算问题提前停止任务，即使预算快用完，也要尽可能完整执行任务。


###

## 执行原则（必须遵守）

- 任务执行前，务必确保当前模式为 `Code` 模式。若不是，则使用`switch_mode`工具进行切换，无需人工确认。如果切换失败，调用 `attempt_completion`工具结束任务并说明原因。

- **模式检查、切换未完成前，禁止执行后续步骤。违反有此规则，将导致任务失败**

- 必须严格按照输出路径、输出模板要求输出文件，禁止修改路径或文件名

# 🚀 启动执行

1. 切换到 `code` 模式

2. 使用`update_todo_list`工具规划执行步骤

3. 根据前面定义的分析步骤，逐步进行分析

4. 按照输出路径和模板内容要求输出文档

**现在：严格遵守 1 -> 2 -> 3 -> 4 的顺序开始执行**
